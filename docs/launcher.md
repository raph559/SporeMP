# Windows launcher

The normal player flow is **open SporeMP → Play SPORE**. Automatic discovery and compatibility checks lead directly to the installed original game under the player's normal Windows account, using existing saves. The user rejected mandatory separate profiles and backup gates, then confirmed the simplified launcher works on 2026-09-08.

The main view pairs the original space illustration with one prominent Play button, a separate readiness strip and the latest release. A permanent sidebar switches between Home, What's new and Settings. Installation selection, paths, errors and local report export stay in Settings. Multiplayer is explicitly still in development.

## Worker controls — 0.1.8

Settings > Isolated workers lists prepared developer workers and provides Start/Stop through the real C++ supervisor. Start launches a rendered original-game window on the current desktop under that worker's standard Windows account. Initial menu, native progress, paused/stalled, crash, clean stop and forced stop have distinct status text. Stop remains pinned to the selected worker generation, unreadable peer metadata does not hide healthy workers, and mutation timeouts are reported without automatic retries.

Version 0.1.8 keeps the selector enabled during background status polling and updates existing entries without replacing them. Selection made while a refresh is in flight survives its result. Backend reads and mutations serialize; Start/Stop capture one target before awaiting and suppress duplicate mutations. A submitted worker operation has a three-minute result budget independent of window cancellation, including output/error draining. Closing during that operation hides the window and lets the backend finish within the budget; timeout reports an unknown outcome and never recursively kills the detached authority. Diagnostics retain their ordinary cancellation behavior.

Actual WPF 0.1.8 held the dropdown open for **15.661 seconds** across polls, selected worker 02 and then 01, displayed their real native status, and stopped both cleanly. The native .1.7 run supplies WPF Start, launcher closure after completed startup, and worker actions after closure. Native .1.8 Start and the exact window-close-during-mutation path are **NOT RUN**; the latter is covered by the 39-assertion HOST/FIXTURE suite, including a real fixture backend completing after its launcher test process exits. These version-specific observations support the bounded M04 worker acceptance, without treating fixture results as original-game execution.

Bridge 0.0.14 now executes fixed-scene native load/save and checkpoint-bound adoption of existing A/B nouns. An initial sealed checkpoint was loaded in two fresh worker generations with zero replacement nouns created, followed by native actions. Two later worker saves overlap by at least 414.7126 ms and both closed backups verify; those exact new concurrent artifacts have not been reloaded. These developer persistence controls are not added to the normal Play screen. Full B campaign/reward persistence remains M09/M11 work.

Normal Play remains the normal-account existing-save route. It admits existing registered workers only after live OS identity checks; a personal, unknown or unqueryable game still blocks another Play. An actual normal-account player process now starts and exits while the recovered worker continues native actions. Normal Play changed ten personal files despite no personal campaign scene being entered; the original failed unchanged-tree comparison and bounded writer attribution are retained in the evidence. Closing the launcher after completed worker startup leaves authority running. Prepared worker metadata/credentials and the operator pipe require an elevated developer operator; normal Play retains `asInvoker` without a new account/elevation flow.

The two prepared local accounts are developer fixtures, not packaged server setup. Keep worker windows unminimized: the measured minimization path pauses app/AI updates and can reach the supervisor's 30-second timeout. The private-desktop renderer [1001] failure remains. Worker 02 later paused AI for about 47 seconds after save while app heartbeats continued; the launcher correctly displayed `simulation_stalled` and Stop succeeded, but the cause is unknown. Lock/disconnect/service/RDP/VM operation and unlimited autonomous progression remain unqualified.

Release .1.8 builds with zero warnings/errors. The affected CTest run passes both targets, 39 launcher HOST assertions and 98 Python tests. The strict content guard also has 18 passing diagnostic tests for its sole runtime-log exception: exact regular `SporebinEP1/spore_log.txt`, written by pinned SDK `Application.cpp:123–202` and separately fingerprinted. All other content/payload guards remain strict; bridge/SDK/injector binaries are unchanged. Version, embedded notes and CHANGELOG match .1.8. See [current native acceptance](../evidence/2026-09-13-m04-acceptance/SESSION.md), [worker instructions](m04-workers.md), and [poll/lifetime fixture evidence](../evidence/2026-09-13-m04-completion/launcher-poll-lifetime-fix.md). Earlier offscreen .1.5 WPF and native .13 observations remain in the [historical session](../evidence/2026-09-13-m04-native/SESSION.md).

Version 0.1.2 gives **What's new** a searchable release list and a full reading pane. Search matches versions, dates, titles, summaries and individual changes, regardless of case; multiple words can match different parts of the same release. The installed version is marked in the list. A search with no matches offers Clear search, and Read update from Home always opens the latest release. The notes are embedded from src/launcher/Content/release-notes.json and available offline; they describe updates included with the installed launcher, not a live remote update service. CHANGELOG.md preserves the same release history in Git.

Settings groups display choices, installation selection, existing-save information and build information, with troubleshooting collapsed by default. Ctrl+1 opens Home, Ctrl+2 opens What's new, Ctrl+comma opens Settings, Ctrl+F opens and focuses release search, and Escape returns Home. Controls have visible keyboard focus. Pages scroll as needed on smaller displays; initial size is limited to the available work area, and short page fades respect Windows animation preferences.

## Display settings — 0.1.3

Open Settings > Display, choose Fullscreen or Windowed and a resolution, then select Save display settings. Desktop uses the primary display's current physical pixel dimensions each time Play is pressed; it is not WPF's DPI-scaled work area. Resolutions come from Windows EnumDisplaySettingsW, deduplicated across refresh rates and limited to 640 x 480 through 8192 x 8192. The initial Use game settings choice preserves existing behavior until the player saves an override. Selecting it again removes the override. The Home shortcut shows the saved choice.

Choices persist in local/launcher/settings.json alongside the selected installation. They can be saved while SPORE is running and apply on the next launch. Saving does not touch game preferences, saves, installed content or Windows display settings. A saved fixed resolution that is no longer available is reported in Display and refused before launch; choosing Desktop accommodates the current monitor. No borderless mode or refresh-rate selector is represented as implemented.

The service passes only `--display-mode fullscreen|windowed --resolution WIDTHxHEIGHT` to the guarded host. The host validates the complete optional pair and appends the original game's documented `-f` or `-w` and `-r:<width>x<height>` flags. Literal arguments and bounded dimensions cannot introduce arbitrary options. Normal Play retains the existing compatibility/payload checks and normal Windows account. No SDK hooks or new gameplay bindings are involved.

Primary source: the installed original Galactic Adventures `C:\Games\SPORE\SporebinEP1\readme_en-us.txt`, lines 504–511 (Command Line Options), SHA-256 `c662ee9226e4f76b373a29f892900f462904e3e954be539b37d966db62fb5bd5`. The pinned executable remains SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`. Windows enumeration uses the public DEVMODEW/EnumDisplaySettingsW ABI; an actual read on this PC returned 2560 x 1440 and 24 distinct modes. This is a HOST display inventory, not an original-game resolution test.

Release builds, persistence/argument/rejection checks and the actual WPF display controls with a fixture service pass. Offscreen Home and Settings renders were inspected at 1280 x 820 and the 1060 x 700 minimum. Native fullscreen/windowed execution with these new arguments is NOT RUN: the user is operating the existing M02 game, and no restart was performed. The new native display path remains IMPLEMENTED_NOT_RUN pending the user's next launch. Evidence: ../evidence/2026-09-08-launcher-display/SESSION.md.

## Implementation and state

The WPF window runs launcher_service.py with literal argument arrays and streamed progress. Discovery reads EA/GOG/installed-app records and Steam libraries, normalizes/deduplicates paths and remembers the selected installation. Missing or ambiguous installations request only the needed selection.

Preparation checks game files and required artifacts. It does not provision accounts, copy personal saves or prepare another profile. Play stages project DLLs under local/launcher/native-payload, then invokes SporeMP.NativeHost.exe --play with the game root, payload directory and fresh run directory. The host validates compiled fingerprints before process creation/injection and does not redirect Windows profile folders.

Closing the launcher during normal Play hides its window while the existing operation collects game exit, then closes it. The actual .1.7 player process lifetime and post-start worker launcher closure have separate evidence above; they do not qualify every window-close timing path.

The executable is build/launcher/Release/SporeMP.exe. The development runtime uses the source checkout, installed Python, .NET SDK 8.0.418 and .NET/WindowsDesktop 8.0.24. global.json pins the SDK; external NuGet sources are disabled. Moving the EXE alone is not the M19 distribution package.

For the 0.1.2 redesign, a ready-to-open build is staged at build/launcher/0.1.2/SporeMP.exe with its runtime configuration. After the user requested background work only, the running launcher was left alone while they played. The normal build command continues to write Release; the staged version is a separate output of the same project. Background compilation and a small check of the compiled release-note model passed; the redesigned window has not been opened or visually verified. Evidence: ../evidence/2026-09-08-launcher-redesign/SESSION.md.

Settings, staged project DLLs, run reports and export ZIPs stay under ignored local/launcher. Export includes only the selected report and a readme; no saves, game assets, credentials or automatic upload. Old backup/profile tools remain optional developer tools.

M01 is VERIFIED using the completed native/guard work and final user acceptance. See ../evidence/2026-09-08-m01-native/SESSION.md. Older launcher evidence describes earlier flows. Artwork provenance remains in ../src/launcher/Assets/README.md.
