# Native capability investigations

## R13 — M05 active ability, social UI and ordinary load boundary (2026-09-14)

Bridge **0.0.29** completes M05's four original acceptance clauses in the recorded living Creature fixture. [Acceptance decision](../evidence/2026-09-14-m05-ability/acceptance.md) retains all unqualified adapters and abilities; full-scene/global H2 coverage remains unfinished. The M00–M20 work and acceptance clauses are unchanged.

The inspected C1E8B0 charge loop applies target impulse despite a denied strike. .25 adds C1EBDB → C1EA58 before collision-query lifetime, preserving original preceding self movement and later cleanup. Two no-attack timer slices retain the consumed-event byte. Actual B bite in native-02 remains active after arming and later cleans up; the fixture has no charge/spit, so no charge callback or projectile parity is claimed. The natural foreign Payer selection reaches the original social UI-output tail with unchanged awards and inspected introduction/mission/card UI. Temporary charm/no-attack probes remain marked adversarial fields, not casts.

Ordinary saved-world Play exposed a separate presentation problem. B28020 filename denial (.26) happened after loading UI appeared; DEC139 countdown denial (.27) happened after the panel was hidden; the DF2391 alternative schedule cut (.28) was never called by this menu. The actual event/control branch at DF7A26 calls UI hiding and scheduling first. .29 guards it before those calls and resumes its original return. Actual Play now leaves the panel usable, and actual Cancel restores the full galaxy menu. All intermediate failures remain recorded.

Six native processes exit 0 with healthy traces, zero foreign callbacks, unchanged sealed Games hashes and all 29 personal files unchanged. Native-02 supplies 787 matching vitals/DNA samples through 197.0564326 s; final .29 native-06 supplies two passed denial/load challenges and actual menu/scene-exit coverage. Retained .24 stability/Save and .18–.23 reward/Off/disconnect evidence was reused without an unchanged soak. Release .29 build and affected HOST target pass; full .28 CTest passes 8/8. [Session](../evidence/2026-09-14-m05-ability/SESSION.md), [closed analysis](../evidence/2026-09-14-m05-ability/native-summary-02.json), [eight static audits / 19 exports](../evidence/2026-09-14-m05-ability/research-provenance-02.json).

## R12 — M05 native save bypass and presentation boundaries (2026-09-13)

Historical status at bridge 0.0.24: **IN_PROGRESS**, superseded by R13. Original .21 menu Save bypassed the guarded B29600 entry and changed six disposable Games files. Static instructions identify the primary persistence listener B29960, which inlines serialization. The .22–.24 listener guard is tested through actual Save clicks and unchanged closed Games hashes. Failed artifacts and the sealed-fixture restoration are retained; personal saves stayed unchanged.

The Animal Update charm slice now skips only expiry/decrement/refcount changes while preserving native movement/animation/effects. Native temporary timer probes are explicitly adversarial field fixtures, not social casts. An interior x86 ABI shim preserves live register/FP state and is HOST-tested. The social-result cut retains both original UI output IDs but has zero natural armed callbacks. Target selection had been overblocked; .24 permits the original live-avatar selection view under census/idle checks, producing the native name/HP/relationship display without enabling gameplay authority.

The .24 original run passes three challenges, retains 3,826 later vitals/DNA samples for 958.609 seconds, exits its scene and closes cleanly. Off .23 retains original starvation/death/respawn. Actual WGC frames and descriptive D3D9 samples are reviewed; process-audio integrity is measured, listening acceptance pending. The next bounded blocker is C1E8B0's mixed charge-impact path, where target impulse can follow a denied strike. Natural social/consumption, other progression and replica load/reuse/death remain open. Exact ABI/provenance, commands, failed runs and limits: [session](../evidence/2026-09-13-m05-presentation/SESSION.md), [binding audit](m05-mutation-audit.md).

## R11 — M04 native acceptance completed (2026-09-13)

Bridge 0.0.14 original load/save and existing-noun adoption now have native evidence. One closed sealed checkpoint restores the same A/B fingerprints in two fresh worker generations without creating nouns; post-restore native actions succeed. An actual normal-account player exits while authority continues performing original actions. Two distinct OS-profile workers execute overlapping original saves, produce own-profile Games writes and exit cleanly before independent sealing. Full-lifetime ETW records zero lost events and no resolved peer/personal access or mutation, retaining unknowns. The earlier deliberate crash test remains valid. This closes M04 for the recorded rendered Creature fixture; full transactional recovery and independent campaign state remain later milestones.

Launcher 0.1.8 repairs inherited startup output, polling/selection and submitted-operation lifetime. Normal Play also exposed the pinned SDK's documented `SporebinEP1/spore_log.txt`: the guard now treats only that exact safe regular file as separately fingerprinted runtime output. A later approximately 47-second native AI plateau is recorded as an unexplained operating limitation; current-desktop/unminimized rendering remains required. Normal Play's observed personal-file changes were preserved, and the final worker-only phase retains all 29 personal hashes. Exact native/visual/source evidence and remaining limits: [acceptance session](../evidence/2026-09-13-m04-acceptance/SESSION.md).

## R10 — M04 native filename loader and checkpoint identity (2026-09-13)

Original saved-game UI uses filename loader `0xB28020` after an immediate message and native reset. This avoids SDK `GameLoadParameters`, whose `0xB4` declaration is shorter than the original `0xBC` construction. Original full save `0xB29600` returns a bool after serialization/marker handling; it is not a durable acknowledgement. At this implementation-only point, bridge 0.0.14 added guarded engine-thread candidates and separated native return, closed-file hashing and ownership adoption. Native execution was NOT RUN then; R11 above records the subsequent original-game acceptance.

Recovery validates existing original noun IDs, species/archetype and live herd membership before binding A/B; it creates no nouns. B player/reward persistence remains future work. Supervisor shutdown escalation and launcher generation/peer handling now have focused HOST regressions. Exact static evidence and acceptance limits: [current session](../evidence/2026-09-13-m04-completion/SESSION.md), [binding audit](../evidence/2026-09-13-m04-completion/native-persistence-audit.md).

## R09 — M04 current-desktop actions, concurrency and checkpoint boundary (2026-09-13)

Historical 0.0.13 evidence; R11 resolves its unattended load/save and basic identity-restoration gap.

The user made the desktop available for native testing. Bridge 0.0.13 initialized in four original processes under two owned OS profiles. Eight unattended A/B commands produced eight original jump returns with later landings. Two concurrent workers advanced native AI; worker 01 completed actions while worker 02 was in front. Terminating worker 01 yielded a real crash report (game -1, supervisor 31); worker 02 stayed ready and completed another action pair. Three other native exits were clean with same-thread bridge disposal and detach 0.

Minimization pauses app/AI updates and produces an unresponsive status after three seconds; restoring before the implemented 30-second timeout resumes the worker. Original Options/Save dialogs pause AI while app updates continue. Concurrent ETW contains no resolved personal/peer-profile access or mutations, but unresolved correlations and shared NVIDIA driver writes remain. All 29 personal files retain their hashes. This is bounded current-desktop evidence, not headless/session/capacity qualification.

Original UI Save reported success; ETW recorded the worker's own temporary checkpoint writes/renames. Its closed snapshot survived a new-process manual reload into the native Creature scene. Actor ownership did not restore: A/B IDs were both zero, with old-generation, stale-epoch and unbound-actor commands rejected. No replacement actors were created. The missing capability is checkpoint-bound mod identity restoration plus unattended load/save. Static helper `0xBADAD0` consumes selected-star/species parameters and dereferences the selected star; guessed zero-filled load parameters are unsafe. Save paths `0xB29000`, `0xB28890` and their callers are static leads for a verified completion observer, not established callable APIs. See [native session](../evidence/2026-09-13-m04-native/SESSION.md) and [minimal reproduction](m04-workers.md).

## R08 — M04 supervision and native renderer/load gates (2026-09-13)

Historical first attempt; the successful current-desktop observations above supersede its pending desktop request.

The private-desktop worker reproduces original renderer error [1001] before bridge initialization. Read-only window enumeration records the dialog without switching desktops or sending input. Actual supervisor/game PIDs 21628/31940 reach the 120-second initialization deadline, producing game exit 35/wrapper 31; the personal postcheck retains all 29 files. Current-desktop rendering is the next native experiment, pending desktop availability. No color-depth repair is established by the generic dialog.

Static original-code inspection identifies EAMain's `multipleInstances` switch and its `-/` prefix parser, enabling the exact worker-only option after OS-profile guards. The Canvas message pump establishes the intended WM_QUIT route, but native worker shutdown is untested. The persistence manager load slot is 0xB271D0; SDK parameter size/player prerequisites and its return contract remain unqualified. Load/save operations explicitly return unavailable.

Bridge 0.0.13 and launcher 0.1.4 implement local supervision/IPC and two prepared account fixtures. The 173-assertion HOST rerun passes after fixing a wait on a nonpending pipe operation. Host process and access-control results are not native authority or checkpoint evidence. Exact commands/provenance: [session](../evidence/2026-09-12-m04-workers/SESSION.md); [binding and process audit](m04-workers.md); [native gates](../tests/engine/M04.md).

M02 original-action observation is verified for its recorded Creature fixture; the multiplayer architecture hypotheses below remain unverified. Each investigation must produce a trace, executable probe, failed reproduction or code change; headers alone do not satisfy acceptance.

M01 resolution (2026-09-08): R01–R03 below describe the initial investigations. They were subsequently resolved through the pinned built injector, a guarded native host, three clean bridge lifecycles and recorded native paths. The user later rejected profile/backup gates for players and confirmed the normal-account launcher works. See ../evidence/2026-09-08-m01-native/SESSION.md. These initial NOT RUN statements are historical, not current blockers.

## R01 — SDK lifecycle and build identity (M01)

Pinned SDK: `cbf9206b9a823f0911cd9be0217104a49d72380b`. Read `Spore/ModAPI.h`, `SourceCode/DLL/DllModAPI.cpp`, `Application.cpp`, `dllmain.cpp` and the actual vcxproj. The local executable size 24,895,536 appears in the combined selector as March2017. Current projects use v143; the older installation tutorial says v142. Implemented a Win32 v143 build using the actual project and generated import library. No guessed exports or import stubs are used.

The SDK core installs detours at load. Post-init callbacks run after `AppInit_detour` calls its original function; disposal callbacks run inside `AppShutdown_detour`. The bridge only registers these two callbacks and installs no gameplay hooks. Callback execution is NOT RUN. Next: pin/qualify loader/core artifacts inside an isolated native test environment and collect initialize/dispose traces over three clean starts/exits.

Initial CMake generation failed because MinSizeRel/RelWithDebInfo were generated without corresponding imported libraries. Resolved by limiting configurations to Debug/Release. Initial isolation probe stopped on the first child HRESULT and lost detail; resolved by recording per-folder errors and both environment variants. See session evidence for commands and results.

## R02 — Environment variables do not establish profile isolation (M01/M04)

Minimal reproduction:

```powershell
python tools/diagnostics/sporemp_diag.py new-profile --root local/profiles --name isolation-repro
python tools/diagnostics/sporemp_diag.py probe-isolation --profile local/profiles/isolation-repro/profile.json
```

Expected gate result: exit 21, `isolation_verified=false`, original-game test NOT RUN. Observed on this machine: APPDATA/LOCALAPPDATA overrides alone retain personal shell folders. Adding USERPROFILE produces `SHGetFolderPathW` errors `HRESULT -2147024894 (0x80070002)` for AppData and LocalAppData, while Documents changes. A directory workspace is not a Windows profile.

Missing capability: a test process launch arrangement with demonstrated game/launcher save, creation, cache, config and registry isolation. This is an OS/path-isolation prerequisite, not evidence that original gameplay cannot be reused.

Next smallest experiment: establish a disposable Windows user/VM with its own shell folders and game access, then capture an unmodified native startup's file/registry paths. Alternatively investigate the source-documented early `cAppSystem::SetUserDirNames` path with a read-only binary call-site audit before a confined runtime probe. Do not call `IAppSystem::Get()` before its documented initialization boundary. Enumerate the resulting Resource path IDs and compare to access traces; test two processes later in M04.

## R03 — Loader setup (M01)

No installation found in the targeted registry, standard directories or `%APPDATA%\Spore ModAPI Launcher` lookup. Source inspected at `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. The launcher starts a suspended original process, injects `ModAPI.DLLInjector.dll`, and requests the current core, legacy core and mod DLLs. Its source also copies files to mLibs and writes user metadata. A source clone is not an installed/qualified loader. No installer was executed.

Next: provision a pinned release or build in the isolated test environment, inventory all injected files and turn the read-only preflight into the guarded launch adapter. It must reject an executable/content/loader mismatch before injector/core loading. The current bridge/core build is legitimate compilation evidence, not a load test.

## Architecture hypotheses and executable gates

| Hypothesis | Next bounded investigation | Required observable result | Gate |
|---|---|---|---|
| H1 multiple owned actors preserve native player context | Trace one native action in M02, then issue distinct actions to two native actors across AI ticks and a delayed completion | Correct actor/faction resource, target, damage/reward and death attribution; no temporary-avatar-swap leakage | M03; block dependent gameplay if it fails |
| H2 authority can separate from presentation | Apply one native result to a replica while tracing local decisions/rewards and render/UI activity | One authoritative outcome; no re-emission, duplicate AI or reward, retained presentation | M05 VERIFIED in the recorded living Creature fixture: nonzero native reward, local denial, disconnect fences and presentation controls tested. Whole-scene/ability/campaign coverage remains unqualified; see R13. |
| H3 unattended workers can be isolated and supervised | R02/R03, then clean lifecycle, focus-loss, two-workspace and single-worker-kill probes | Paths confined, native tick continues where required, one failure does not kill the other | M01/M04 |
| H4 sufficient native state can be checkpointed/restored | Controlled encounter save/reload plus ID map; kill after action/event/metadata/artifact boundaries | Valid committed identities and native state, explicit rollback window, no duplicate durable reward | M04/M09 |
| H5 workers share one canonical universe | Trace off-screen galaxy mutations on two workers, then fenced location transfer with crash injection | Exactly one global-system outcome and one actor authority after restart/transfer | M10/M15 |
| H6 mixed stages share native consequences | Lower-stage player and Space visitor, canonical terrain/population owner, observed native tool effect | Matching supported consequence after reload without forced advancement | M16 |

Stage-specific bindings remain unknown until separately audited. In particular Cell does not inherit Creature interfaces. `native-behavior-baseline.md` and its coverage CSV carry every initial mechanic family forward.

## R04 — M02 original-action observation and lifecycle (2026-09-08)

Implemented the source-backed candidate `cCreatureBase::DoJump(bool return, int argument)` at the SDK's March2017 preferred address `0xC18CA0`, with animal landing `0xC04CB0`. The actual declaration is `bool DoJump(int energyConsumed)`. Original arguments and return are forwarded unchanged, with no post-call object dereference. Noun factory/destruction/avatar hooks supply diagnostic identity observations. Scene messages invalidate all scene tokens. NPC/avatar AI entries are counted without changing AI. Full ABI/source/context inventory: `m02-binding-registry.md`.

Do not interpret campaign `mEnergy` as proven resource spending: the SDK documents this field for adventurer creatures. Raw health/dead samples also do not qualify damage/death event coverage. `TakeDamage` is deliberately not hooked: the SDK assigns the same native address (`0xBFCF10`) to combatant, creature base, animal and citizen declarations, and this project has not established the runtime receiver/vtable path. Next bounded combat investigation is read-only call-site/disassembly inspection followed by one observed native damage invocation in the disposable scene.

Native minimal fixture: `tests/engine/M02.md` gives exact reference/observe commands, Creature campaign input sequence, closed-profile fixture hash comparison, jump/landing, actual entity destruction and scene exit. Expected: matched native entry/result and visible outcome, engine/mod source separation, fresh IDs after recreation, no duplicate native effects and measured overhead. Actual: **NOT RUN**; previous no-desktop-control restriction remains pending. This is an unavailable acceptance run, not a reproduced engine failure or proof of impossible multiplayer. Continue only with actual native evidence before promoting bindings/M02.

Executed host reproduction: `sporemp_observation_tests.exe` attaches the real pinned Detours library to compiled fixture methods, preserves this/int/float arguments and bool results, then detaches. The first Release fixture check failed because its tail-recursive source call did not produce a separate intercepted function entry. The fixture was corrected to use a volatile member-function pointer, forcing an actual second entry; all 22 host assertions then passed in Release and Debug. This correction concerns a synthetic test's compiled call path, not a discovered SPORE behavior.

The retained HOST_FIXTURE trace is parsed by the real analyzer, reports NOT_VERIFIED and fails `--require-native` with exit 2. Source-only synthetic coverage fixtures never count as NATIVE evidence even when their schema is well-formed. A new-mode host denial test executes `--observe` under an unauthorized account and observes exit 20 with `launched_processes=0`. Exact commands/results and artifacts: `../evidence/2026-09-08-m02-observation/SESSION.md`.

R04 native update (2026-09-08): the user authorized launch, operated the original Creature campaign and confirmed doing jumps. Closed run m02-user-observe-01 / PID 35020 passes the actual trace and provenance analysis: nine accepted avatar DoJump calls and nine later same-avatar landing callbacks before the next jump (about 0.992–1.068 seconds). The additional avatar landing before the first jump and 626 other-creature landing callbacks are not counted as user jumps. The trace records native AI, 513 entity invalidations, a menu-to-Creature scene message pair, zero lost/foreign callbacks and clean disposal/game exit. All 29 personal files remain unchanged. Detailed commands, source/loaded-artifact identities, raw trace hash and event sequences: `../evidence/2026-09-08-m02-user-probe-01/SESSION.md` and `jump-evidence.json`.

The minimal callback-capture hypothesis now has real execution evidence; M02 is still IN_PROGRESS. The analyzer's coverage-ready result only establishes its stated record requirements. It does not prove Creature scene exit/reload, a console invocation, action causality from video, complete destruction paths, unchanged baseline behavior or overhead. Next experiment is the paired user-operated reference/observed sequence from one hashed closed Creature fixture. No additional jump repetitions are required to salvage this capture; damage/death/progression/save/load qualification remains independent outstanding work.

R04 pair 01 update (2026-09-08): both user-operated runs exited normally with the same payloads and all 29 personal files unchanged. The reference save was accepted, preserved, and the developer roots were restored to the hash-verified starting fixture before Observe. The observed trace passes strict validation for 20,275 records: five accepted avatar jumps before reload and one after, each with a later same-avatar landing; two diagnostic commands; real Creature exit/reload; fresh sampled avatar identity after reload; zero loss/foreign callbacks. Both resulting saves are preserved. The 14 pair correlation/provenance checks pass; commands, artifacts and exact limits are in `../evidence/2026-09-08-m02-pair-01/SESSION.md` and `paired-native-analysis.json`.

The reference video is usable but observed GDI capture failed: its valid 2560x1440 container/normal recorder exit concealed initial non-game pixels and 163.5 seconds of black frames. Minimal reproduction is the title-selected GDI command retained in the pair capture request. Expected continuous game frames; observed unusable media. No paired visual equivalence or rendered diagnostic text is accepted, and the precise capture failure mechanism is unproven. PresentMon separately still exits 1 without frame data; overhead remains NOT MEASURED.

The next capture experiment is `tools/native/capture-game.ps1`, using the installed FFmpeg Windows Graphics Capture filter with exact executable/title selection and no monitor fallback. Final missing-game and direct missing-window HOST rejection checks passed; actual native WGC capture is IMPLEMENTED_NOT_RUN. Verify short real-game media before requesting further comparison actions. This repairs the evidence workflow without changing native gameplay, expanding binding qualification or repeating M01. M02 remains IN_PROGRESS; its missing visual/performance evidence and remaining ABI/lifetime/gameplay families are explicit.

## R05 — M02 capture/timing repair and native acceptance (2026-09-09)

The user authorized assistant computer manipulation and completion of M02. A five-second, exact-window WGC probe captured the actual original game and was inspected before the gameplay comparison. The 240-second reference, 120-second reference-reload and 420-second observed clips all finalized normally; representative frames, six three-second jump sequences and both rendered console outputs were inspected. Full-frame black scans found no qualifying black interval. This resolves the prior evidence gap without claiming a cause for the rejected GDI clip. The old media remains rejected.

The bundled NVIDIA PresentMon invocation had exited 1 without frame data. The replacement uses the official standalone PresentMon 2.5.1 executable, downloaded only from its versioned GitHub release and verified against the release asset SHA-256. `tools/build/fetch-presentmon.ps1` pins both. The actual five-second PID-specific D3D9 probe exits 0 and emits rows; two subsequent 60-second samples also exit 0 with only the intended game PID/runtime. Medians are 16.55235 ms Off and 16.54640 ms Observe; p99 17.9772/18.0072 ms. Process CPU use differs by +0.01196 effective cores (+3.87% relative). This is a measured capped sample with uncontrolled NPC/background timing, not isolated hook cost or a universal limit. Capture FPS is not used as native timing.

The reference and observed runs started from the same closed 27-file fixture. Both perform two normal jumps, native movement, discard unsaved movement, exit/reload, then a third jump; Observe also invokes the read-only console before and after reload. The original saved scene returns. Native Games/ and creation files retain their start hashes; both end states and original developer directories are preserved. All 29 personal files remain unchanged.

The accepted Observe trace contains 28,614 records with three original avatar calls, one later same-avatar landing each in 0.9910–0.9948 seconds, two console commands, real destroy/reload observations, fresh diagnostic avatar IDs and clean disposal. All 21 provenance/correlation checks pass. The paired videos show unchanged original action behavior and no duplicated visible effect. M02's original acceptance is therefore VERIFIED for these paths, while the analyzer's standalone result correctly remains NOT_VERIFIED until external review.

The next smallest investigation is the M03 actor-context gate: source-audit actor creation and the shared `TakeDamage` address/receiver path, then observe native attribution in a disposable two-actor fixture. Damage/death, inventory/progression, save/load hooks, complete lifetime paths and other stages remain unknown; sampled scalars do not substitute for events. No later scope or original M00–M20 acceptance clause was changed. Exact commands, tool/artifact hashes, OS/hardware, input timestamps, trace sequences and raw-media locations: `../evidence/2026-09-09-m02-completion/SESSION.md`.

## R06 — M03 native actors, held input and remaining player context (2026-09-09)

The guarded Creature harness binds distinct owners, creates B through the original factory, dispatches native WalkTo/Jump/Bite and fences ownership on destruction/reload. Two early crashes identify required native herd and campaign archetype context; the corrected factory copies metadata from a live template and requires resolved archetype data. Failed traces remain excluded from acceptance. Probe04 qualifies independent movement, delayed actions, A/NPC reciprocal damage and an assisted global DNA award. Probe05 qualifies reload fencing but rejects B's already-airborne jump.

Target-call observation in probe06 establishes that native NPC evaluation temporarily assigns/clears targets; one sampled target does not prove an avatar-only rule. Its unthrottled trace reaches the32MiB cap and is incomplete. Explicit one-second NPC target sampling fixes recording volume without dropping original native calls. Owner input and damage/ability observations remain unthrottled.

The bounded held-intention adapter preserves native AI, pathfinding, cooldowns and hit/damage/reward processing. Probe07 exposes an approach threshold that demands overlapping bodies and correctly expires after30.1162s. Build09 uses native ability range plus creature footprints only to select input; it does not replace native hit checks. Healthy probe08 completes two B kills through six original Bite hits each. Native NPC3 attempts six abilities against B without a hit; neither kill emits DNA. Fresh B6 after same-process reload completes an accepted jump after1.0027685s and70 AI ticks. Both kills end held input on target death. Active Stop and active-intention scene cancellation remain NOT RUN.

Build09 and CTest pass5/5 HOST/FIXTURE targets. Probe08 ends normally with40,075 valid contiguous records, clean detach, matching payload hashes, zero foreign callbacks and unchanged29 personal files. All eight disposable Games/creation snapshots preserve starting content. Native video contact sheets and live screenshots are reviewed with explicit coverage limits. Full M03 stays BLOCKED on NPC hit eligibility and independent player/reward context for B. Next: audit the native ability-to-hit eligibility and kill-to-DNA branches before a scoped context adapter. Exact reproductions, argv, exits, native sequences, failures, provenance and media review: `../evidence/2026-09-09-m03-actors/SESSION.md`; binding details and unsupported behavior: `m03-native-context-audit.md`.

## R07 — M03 attack deactivation and focused reverse engineering (2026-09-12)

Bridge 0.0.6 adds five guarded combat observers and native AI boundary correlation. Three healthy native runs record two further B six-hit kills without DNA. In probe-03, NPC3 selects Bite against B seven times and each attack clears 15.4362–16.6991 ms later at the next native AI return, with no NPC effect dispatch. The actual target-clear caller and raw native behavior table identify the deactivation routine at `0xD672A0`. The exact predicate/decider causing that transition remains unknown; no reciprocal-combat fix or independent B player context is implemented.

The user questioned the repeated tests and proposed reverse engineering. Subsequent work uses a portable, pinned Ghidra/JDK workflow and the installed executable's machine code. The first import's automatic analysis reached its explicit limit; its initial export failed despite process exit 0. That failure is preserved and corrected follow-up exports complete. The database remains partially analyzed. Selected exports and native callsite disassembly establish attack-stop conditions, distinguish target combatant state from a wrongly inferred game-mode getter, and show that the native spatial constructor enables the creature independently of the herd flag. The shared DNA path also updates cPlayer goals, display and synchronous/deferred action state. A second DNA float does not supply independent native progression.

The next work is behavior decider/order and player-context reverse engineering, followed by the smallest runtime observation that distinguishes why the first accepted NPC attack is cancelled. Repeating a full fight/reload sequence is unnecessary for that question. Host build06 and CTest04 pass 6/6 targets, including ten ABI assertions and 67 Python tests; these do not execute SPORE. All three native lifecycles are healthy with unchanged 29 personal hashes and unchanged disposable Games/creation hashes. Active Stop/scene cancellation, nonzero resource costs and full M03 acceptance remain unqualified. Exact findings: [focused RE audit](m03-reverse-engineering.md); commands, failures, provenance and capture limits: [session](../evidence/2026-09-12-m03-context/SESSION.md).

## R08 — M05 bounded native replica application (2026-09-13)

H2 remains open beyond the living Creature avatar vitals fixture. M05 now has immutable roles, source/entity/baseline fencing, scoped absolute-state application, selected native mutation guards, lifecycle invalidation and per-domain audits. It uses the original game and existing private M04 IPC. No custom simulation, network session or player-facing replica mode was introduced.

Six focused static audits inspect the original update paths, DNA scalar getter/setter, Save, ability failure handling, hunger/healing, death/target/cooldown leads and effective max HP. The outer Animal Update mixes authoritative mutation with presentation; skipping it wholesale would not preserve native movement/animation. The hunger/healing virtual branch `C02E10` is separately byte/vtable checked and guarded. `D2E480` stores the scalar that `D2E350` reads, without AddEvolutionPoints reward dispatch. `C05D50` obtains effective HP from base/species/stage modifiers through the Combatant receiver. SDK names and raw field comments alone did not establish these semantics.

Two failures have concrete reproductions. Bridge .15 attached the M05 Save detour before M04 validated the original Save bytes: `load` returned `unavailable`, exit 5, with `persistence_binding_rejected`. Moving M04 initialization before M05 fixes that order in .16. The .16 replica then loaded/adopted correctly but rejected a 10-HP sample because its raw max-health field is 1. No state was written; the baseline was quarantined. The .17 receiver checks the actual Combatant max-HP entry and receives 10 from the native call. It reuses the retained .16 authority sample instead of replaying the authority session.

The .17 native result is one application, health 7.62724638 → 10, energy 1000, hunger 25.6914024, DNA 0 → 0; duplicate `stale`, disconnected apply `invalid`, forbidden publication `unavailable`. Eight connected audits match, and 344 later actor samples preserve those vitals through 86.4512188 seconds. AI/hunger attempted-call denials each reach 147,808. All five original traces close healthily on their recorded engine thread with clean game/supervisor exit and matching loaded bridge/SDK hashes. The actual receiver scene and 10-HP HUD are inspected; the concurrent authority's splash-plus-HUD capture is explicitly not 3D gameplay evidence. Personal 29-file hashes and both sealed disposable Games trees are unchanged.

Missing capability: suppress and project irreversible pickup/death/spawn/progression paths while retaining the presentation portions of the same update. Current native reproduction establishes stable vitals under AI/hunger suppression but does not exercise those unhooked paths or a nonzero DNA grant. Expected full H2 behavior is one native worker outcome with no client re-grant, full scene consistency, and usable original camera/movement/animation/audio. The next smallest experiment is a scoped observer of the terminal pickup grant/consume branch and its object lifetime, followed by a causally recorded native worker reward and a deliberate conflicting client action. Zero counters on unhooked domains must not be presented as a pass. Details: [M05 binding/coverage map](m05-replicas.md), [session](../evidence/2026-09-13-m05-replicas/SESSION.md), [remaining acceptance protocol](../tests/engine/M05.md).
