# M03 native actor context audit — updated 2026-09-12

## M03 completion: native actor and reward ownership, bridge 0.0.12

**M03 is VERIFIED for the recorded original Creature fixture.** The unchanged milestone clauses are mapped in [acceptance](../evidence/2026-09-12-m03-awards/acceptance.json). The retained A/B movement, delayed jump, wrong-owner rejection and reload tests combine with the new native reward run. Full independent Creature progression remains M11; this harness is not networked co-op or an unattended worker.

In `award-01`, B deals six original native hits and receives seven NPC hits. Its NPC dies with B as last attacker; B's original DNA, goal and combat progress become 8.75 while A remains zero. A then attacks another NPC; five further hits from untracked native allies complete the kill. The original assisted award gives A 8.75 and one native part unlock while B's balances/items remain unchanged. The native B display queue receives and processes exactly one award and returns to idle/empty. All 18,733 records are contiguous, with zero foreign callbacks, original award/player cleanup and clean exit/detach. See [native summary](../evidence/2026-09-12-m03-awards/native-summary.json) and [session](../evidence/2026-09-12-m03-awards/SESSION.md).

All bindings below are for the pinned executable SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`, preferred base `400000`, SDK `cbf9206b9a823f0911cd9be0217104a49d72380b`. Concrete types are in `native_award_abi.h`; actual x86 instructions, callers and relocation-aware prefixes are recorded in [binding-audit-02.json](../evidence/2026-09-12-m03-awards/binding-audit-02.json). The static exports under ignored `local/m03-static/award-abi`, `award-queue-consumer`, `award-queue-path` and earlier `reward-*` folders are the binary source, not inferred SDK prototypes. Every new path executes on the verified engine thread 32704 with live owner/actor/epoch checks.

| Binding, preferred VA | Inspected Win32 ABI and exact context | Runtime evidence / limit |
|---|---|---|
| Player-owned predicate `C02FF0` | thiscall bool `(Spatial*)`; extend B only inside its actual native damage scope and return RVAs `807AC9`/`807B87` | Twelve original classifications in six B hits; original predicate still runs once. No general avatar flag is written. |
| Reward amount `C042A0` | thiscall float `(Animal*, bool)`, ret4 and x87; B stage scope only at return RVA `807BE4` | Original amount 8.75; no replacement reward formula. |
| DNA application `D2E8A0` | cdecl void `(float)`; dispatch B only at damage return RVA `807BF1` with live actual B attacker | Original DNA/goal update, display enqueue and action dispatcher execute once; A scope restored afterward. |
| Player getter `F67D40`, avatar getter `B1FD00` | thiscall pointer `(Manager*)`; four-byte `mov eax,[ecx+74/+54]; ret` | Original once; return B only within audited stage scope, plus player getter's exact combat-trait caller `807C0A`. Persistent manager pointers remain A's. |
| Display enqueue `D2E2E0` | thiscall void `(display*, float)`; original routine touches only counter at receiver+128 | Adjusted receiver selects B's originally constructed counter. One native enqueue/dequeue recorded. |
| Action dispatcher `D39360` | thiscall void `(Strategy*, uint32_t, void*)`; qualified reward action `045AB96E` | Original five handlers and outer native flags execute. B pending world flag retained separately; A shared tutorial/quest presentation is still observed. |
| Creature update `D45740`, goal getter `D2E360` | thiscall void `(Strategy*, float, float)` and cdecl float `()`; goal selection only at return RVA `945AA9` | Entire original update executes once. Same-species B pending goal is selected on 4,711 passes. No completed native herd evolution observed. |
| Native deque init/destroy `D2C490`/`D2C940` | thiscall void `(void*, uint32_t)` / `(void*)`; counter+04 is the native 40-byte deque | Native construction, enqueue, pop and destruction; no SDK container substitute. |
| Native timer init/destroy `B639D0`/`B639F0`, counter tick `D2D0B0` | thiscall void `(void*)`, counter tick `(void*, uint32_t)` ret4; timer at counter+30 | Original timed queue processing reaches idle/empty; native teardown precedes player retirement. |
| Creature stage reset `D2E980` | cdecl void `()` on native 50-byte stage singleton | Original reset and SDK-pinned normalization initialize retained B stage. Native configured goal-total sum equals A's 1,000. This is the tested first brain segment, not the entire global mode initializer. |

The actor damage hook supplies a nested owner scope from the actual resolved attacker. The stage scope saves/restores the native 0x50-byte stage singleton only around B reward calculation/application and B's deferred counter. B's native player stores its own goal/combat/items. World AI/physics does not run with B's stage singleton installed. The manager's persistent avatar/player pointers are never swapped. Native pointers and containers stay inside the bridge.

Detours 3 from the SDK rejects the four-byte getters. CMake now builds Detours 4.0.1 from the existing pinned injector commit `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`, validating that revision at configuration. It recognizes the inspected padding after RET. Release build-05 and the two affected HOST targets pass in 0.18s (26 actor/award ABI assertions and 22 observation/Detours assertions). Native installation validates all new 17 prefixes, including six exact rebased relocations, and attaches all 27 hooks. Host fixtures never load SPORE.

**Remaining behavior is explicit.** A/B share a species profile in this fixture. The first-victory tutorial and hunt progress are still campaign-global. B part unlocks, independent quests/species, completed herd evolution, brain advancement, full mode initialization and B persistence are not qualified. At the native first-brain threshold the B counter retains queued work until the later progression adapter exists. Nonzero costs and active Stop/scene cancellation remain NOT RUN. No server-only avatar is introduced; M04 must qualify its conditional target/reward/area behavior if such a worker architecture is adopted. These limits retain the M04/M11/M17 requirements and do not constitute complete Creature gameplay.

B's recovery to health 12.5 preceded A's encounter; it must not be reported as an A reward changing B health. The precise native recovery/maximum-health rule remains unqualified. All 29 personal hashes match. The disposable `Games/Game0/GGEUserData.dat` changes despite Don't Save; both snapshots are preserved and the unchanged-fixture check remains a recorded failure. Other disposable Games files and all creations match. The capture covers 300 seconds and ends before A's later award and exit; the complete trace and live observations cover those results.

The sections below preserve the earlier version-specific findings. Their former missing-reward/M03-blocker conclusions are superseded by the 0.0.12 result above; their failed or unrun tests remain failed or unrun.

## Previous increment: native player lifetime, bridge 0.0.11

**Native B player creation, explicit retirement, recreation and bridge-disposal cleanup are VERIFIED in player-02. Full M03 remains BLOCKED on stage/reward ownership.** Bridge **0.0.11** creates B's native cPlayer through the original noun factory and owner initialization, while A's manager player/avatar pointers remain unchanged. Fresh `player-02` creates player 7426, retires it, creates 7427, and cleans up at bridge disposal. Both have distinct native game IDs, independent item storage and ten selection groups. Native RemoveOwner runs once each, clears items/groups and leaves zero listeners requiring extra cleanup. The 2,141-record trace is contiguous with zero foreign callbacks, clean detach and game/wrapper exit 0. All 29 personal hashes and the disposable Games/creation contents remain unchanged.

A's goal total stays 1,000 with 53 unlocked items; both new B players have the native constructor defaults of goal total 1 and zero unlocked items. Player lifetime is verified; complete Creature stage initialization and reward routing remain unimplemented. Three configuration notifications execute B's original callback; two more are rejected during retirement. Campaign-global cheat/UI/Space callbacks remain assigned to A. Scene-exit retirement and multi-stage callback ownership are NOT RUN. The 60-second capture covers setup and first creation; later retirement/recreation/exit are established by the complete trace and live observations.

| New binding | Pinned binary ABI / context | Evidence and limit |
|---|---|---|
| Native player factory | Manager thiscall `CreateInstance(uint32_t)` at `B20BF0`, noun ID `02C21781`; constructor `C7B930`, size `12D8`. | Original factory/owner lifecycle preserves manager+74 player and manager+54 avatar. Native cast ID `02C216ED` differs from SDK TYPE. |
| Player owner initialization/removal | Primary vtable `1472CC8`; slot28 `C7C870`, slot44 `C7C3C0`. | Engine thread only, retained cGameData reference. Manager `B22560` reaches synchronous destroy helper `B20CC0`; explicit release follows native cleanup. |
| Player message callback | IMessageListener subobject+34, vtable `1472CA4`, slot4 `C77ED0`; thiscall bool `(listener*, uint32_t, void*)`, ret8. | B's live configuration callback uses the original; global campaign actions remain A-owned. Slot0 is a destructor thunk. |

The current harness installs 19 hooks after the existing 12 prefixes/3 slots and five additional prefixes/six slots pass. `B20BF0` contains one ASLR-relocated absolute immediate; the guard compares the exact relocated value identified by IMAGE_REL_BASED_HIGHLOW at RVA720BF5. Initial 0.0.10 player-01 rejected that prefix before actor-hook installation and is preserved separately. Release build and focused host ABI evidence pass: 18 assertions/ten signatures, CTest 1/1 in 0.07s. See [session](../evidence/2026-09-12-m03-rewards/SESSION.md), [native lifecycle](../evidence/2026-09-12-m03-rewards/native-player-summary.json), [static guards](../evidence/2026-09-12-m03-rewards/player-binding-relocated-audit.json), and [visual limits](../evidence/2026-09-12-m03-rewards/visual-review.json).

## Previous verified combat increment: native nest-context correction, bridge 0.0.9

**Reciprocal B/NPC combat is VERIFIED in the bounded original Creature fixture; full M03 remains BLOCKED on independent native player/progression context.** The pinned nest-avoidance decider `0x04A1A0F9` at preferred VA `0x1587C40` uses `Decide 0xD7D900`. It normally exempts remembered avatar targets (general flag `0x200`). B has a bridge owner but no avatar flag, so this branch repeatedly displaced native attack behavior near the avatar's home herd. Diagnostic `decision-02` records this selected decider at all seven observed attack deactivations.

Bridge 0.0.9 extends that existing exemption to a live target with explicit bridge ownership. The original decider and original memory lookup each execute once; their arguments, scratch output and returned target pointer are preserved. Only a positive nest-decision score for that exact remembered owned target becomes zero. The adapter never changes actor/avatar flags, the manager avatar, target pointers, health, cooldowns, native AI ticks or reward balances.

Fresh `ownership-01` (game PID6160, engine thread11960) executes `setup` and `duel 2` only. Original AI continues after that one initial B attack. The complete 3,134-record native trace records six effect/Animal/base-damage paths in each direction: **B health 10→4, NPC health 6→0/dead, A health 10 unchanged**. No active-attack deactivation is recorded for NPC3, and the exact nest exemption is observed. Game and wrapper exit 0, one same-thread initialize/dispose, clean detach, zero foreign callbacks, all 29 personal hashes unchanged and disposable Games/creation hashes unchanged. This qualifies the affected combat behavior, not general player input arbitration or multiplayer.

The Release build passes. The focused `native_actor_abi_host` test passes 15 assertions across eight actual Win32/Detours signatures in 0.07s. The three inspected contact sheets contain 27 samples. The ownership recording ends shortly after combat starts; complete reciprocal damage is trace evidence, not a claim of continuous recorded visual coverage. See [session](../evidence/2026-09-12-m03-decisions/SESSION.md), [native correlation](../evidence/2026-09-12-m03-decisions/native-summary.json), [acceptance](../evidence/2026-09-12-m03-decisions/acceptance.json), and [visual review](../evidence/2026-09-12-m03-decisions/visual-review.json).

| New binding | Pinned binary ABI / context | Evidence and limit |
|---|---|---|
| Attack-stop predicate, `0xD65980` | cdecl bool with five words: opaque behavior state, creature, flags, combatant target, animal target. | Observer added in0.0.7. Native records distinguish original stop results from tree preemption; original result retained. |
| Nest decider, `0xD7D900` | cdecl float; creature, double clock, flags, scratch, dirty byte pointer, decider, context. Eight stack words/0x20, x87 result. | Original callsite `0xBC8B27..44`, guarded prefix and native ownership-01. Preserves scratch/dirty outputs; null cleanup slot at decider+0xC is statically checked. |
| Remembered creature lookup, `0xD99930` | cdecl creature pointer with one opaque memory argument; only exact return RVA `0x97D9AA` is attributed to the active nest-decision scope. | Original pointer/null returned unchanged. Engine-thread-only identity capture; no extra lookup or retained native pointer. |

The 0.0.9 harness installs 17 hooks after 12 code-prefix and 3 virtual-slot guards. [Nest binding audit](../evidence/2026-09-12-m03-decisions/nest-binding-audit.json) pins bytes, ABI and executable identity. The diagnostic tree stack read is gated by exact callback/cleanup caller RVAs, bounds the selected index to the native child count, and never writes the native frame. These addresses apply only to the executable identified below.

The NPC's death still emits **no DNA callback**. The [reward audit](m03-reverse-engineering.md#native-reward-context) now identifies native player lifecycle, an SDK cast-ID mismatch, five synchronous action handlers, campaign brain input to the award amount, player goal/combat-trait updates and deferred display/progression dependencies. These were static findings for 0.0.9. The 0.0.11 increment above qualifies the native player lifetime subset; reward routing remains unimplemented.

## Previous increment: bridge 0.0.6 observations, preserved history

**M03 remains BLOCKED.** Bridge 0.0.6 adds five guarded observers to the existing nine-hook harness: ability selection, animal effect dispatch, animal damage, attack-index assignment and attack-animation completion. It correlates original AI entry/return with accepted attack state. Original arguments, return values and function execution are preserved. The current three native runs have healthy traces, clean disposal and unchanged personal hashes; they do not qualify reciprocal combat or independent B progression.

Probe-03 shows seven NPC attacks accepted against B and cleared 15.4362–16.6991 ms later during the next native AI tick. The observed callsite and pinned behavior table identify deactivation at preferred VA `0xD672A0`; the decision that causes deactivation remains unresolved. B completes two further native six-hit kills in probes-01/02, without a DNA callback. Native reward analysis identifies shared player-goal/display/action state beyond the campaign balance.

The user asked to reduce repeated testing and prioritize reverse engineering. A reusable local Ghidra project and selected-function exports are now available. The [focused RE findings](m03-reverse-engineering.md) record the actual stop predicate, constructor defaults, decompiler corrections and native reward dependencies. Static analysis does not support blindly enabling the herd, changing Guard personality, forcing player flags or suppressing AI. The next runtime question is the exact branch/decider transition, with one bounded observation after its ABI is established.

Current evidence: [session and commands](../evidence/2026-09-12-m03-context/SESSION.md), [native correlation](../evidence/2026-09-12-m03-context/native-summary.json), [acceptance](../evidence/2026-09-12-m03-context/acceptance.json), [visual review](../evidence/2026-09-12-m03-context/visual-review.json). The release build and six HOST/FIXTURE targets pass, including ten real Win32/Detours ABI assertions and seven new analyzer fixtures. These host checks do not execute SPORE.

| Additional binding | Pinned source / ABI / preferred VA | Native execution and limit |
|---|---|---|
| Ability selector | Static callsites in `binding-audit.json`; thiscall `int(Creature*, int, bool, bool)` at `0xC19800` | Original index/negative rejection preserved and observed; accepted selection is not hit acceptance. |
| Animal effect dispatch | SDK `cCreatureBase.h` primary slot `0xB8`; thiscall `bool(Animal*, Combatant*, int, Vector3*)` at `0xC02B40` | Guarded actual vtable slot and code prefix; tracks dispatch/return without adding an effect. |
| Animal damage | SDK `cCombatant.h` slot `0x18`; thiscall `int(Combatant*, float, uint32_t, int, const Vector3&, Combatant*)` at `0xC075E0` | Correct `+0x5A8` subobject receiver; original nested base damage/award processing observed. |
| Attack index setter | Binary instruction audit; thiscall `void(Creature*, int)` at `0xC07500` | Setter observation does not cover inline stores, including the located deactivation reset. |
| Attack animation completion | Binary callers and implementation; thiscall `bool(Creature*, uint32_t)` at `0xC0E2D0` | Original result preserved. Logging filters do not make absence of a record proof that the function was never called. |
| Native player-owned predicate (no hook) | SDK `cSpatialObject.h` slot `0x58`; actual Animal target `0xC02FF0` | Guarded slot before invocation; body tests native general flags `0x300`. A is true; B/NPC are false at binding. |

All new hooks run on the already verified engine thread with lifetime/identity resolution in the bridge. Code guards cover six prefixes and three virtual slots in 0.0.6. See `src/bridge/native_actor_abi.h` for concrete Win32 types and the evidence's binding-audit reports for bytes/hashes. No new hook or callable binding to the RE-only behavior/decider functions is installed.

## Earlier native qualification — 2026-09-09

**M03 acceptance: BLOCKED.** The implemented harness creates a second native Creature actor, enforces distinct local owners, dispatches movement/jumps/held attack intentions, and fences identities on destruction and scene exit. Healthy probe 08 completes two B kills through native Bite, with six B-attributed hits each, and completes B's jump after reload. The remaining gate is native NPC hit eligibility and player/reward context for B: NPC3 attempts six native attacks against B without a recorded hit, and neither B kill emits campaign DNA. Independent B progression is not implemented. This is a native research increment, not playable co-op. M04–M19 multiplayer gameplay acceptance remains blocked; independent infrastructure and binding research may continue. The full requirements are retained.

Evidence: [session](../evidence/2026-09-09-m03-actors/SESSION.md), [eight-probe native analysis](../evidence/2026-09-09-m03-actors/native-analysis-02.json), [acceptance record](../evidence/2026-09-09-m03-actors/acceptance.json), [visual review](../evidence/2026-09-09-m03-actors/visual-review.json), [test procedure](../tests/engine/M03.md). Native captures, snapshots and crash dumps remain local and excluded from Git. No game binary, save or third-party asset is a source deliverable.

## Qualified configuration and ABI

All executed paths use the recorded **original Creature campaign, Satiria**, on the installed GOG GA 3.1.0.29 Win32 executable at `C:\Games\SPORE\SporebinEP1\SporeApp.exe`. Its SHA-256 is `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`. This is not a Galactic Adventures adventure test, a separate base-game executable qualification, or qualification of other content configurations. The guard validates the pinned 108-file content/configuration inventory before injection.

Pinned SDK: `cbf9206b9a823f0911cd9be0217104a49d72380b`; pinned injector source: `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. Source headers below are relative to `external/Spore-ModAPI/Spore ModAPI/Spore/Simulator/` at that commit. Addresses are **preferred virtual addresses for this executable** (image base `0x400000`), not ASLR addresses or transferable offsets. SDK `GetAddress` resolves ordinary bindings. The additional virtual-slot guard compares the actual function RVA.

| Binding / pinned source | ABI and preferred address | Executed evidence / limit |
|---|---|---|
| `cCreatureAnimal::Create`, `cCreatureAnimal.h:41` | cdecl, `Animal*(const Vector3&, cSpeciesProfile*, int, cHerd*, bool, bool)`; `0xC09B40` | Native model, physics, abilities and herd membership created in probes 03–05. Null herd crashes later campaign AI in probe 01. |
| `cGameNounManager::CreateHerd`, `SubSystem/GameNounManager.h:124` | thiscall; position, species, herd size, bool, personality, bool; `0xB23920` | Returned a valid empty disabled herd. It did not synchronously create the requested animal in probe 02. The animal factory must receive the new herd. |
| `WalkTo`, `cCreatureBase.h:119` | thiscall, `(int, const Vector3&, const Vector3&, float, float)`; `0xC1C720` | Standard speed 2; six-unit local movement, stop distances 0.5/1.0. A/B reach separate goals in probe 04. Approach is a single native walk request to the target's sampled position, not continuous pursuit. |
| `DoJump`, `cCreatureBase.h:137` | thiscall, `bool(int)`; `0xC18CA0` | A/B accepted jumps in probe 04. Probe 05 B request returns false while B is already airborne; that animation is not a successful commanded jump. |
| Animal `OnJumpLand`, `cCreatureAnimal.h:92` | thiscall, `void()`; `0xC04CB0` | Matching delayed A/B owner/command completion in probe 04; A completion after reload in probe 05. Native callbacks with `jump_command=0` are ordinary game activity. |
| `SetCreatureTarget`, `cCreatureBase.h:236` | virtual slot `0x84`; thiscall `void(cCombatant*, bool, int)`; actual Animal RVA `0x803DF0` | Native caller `0xD3599B..0xD359B0` uses `(target,false,0)`. Runtime RVA guard precedes the call. The ninth observer records target changes and native caller RVAs in probes 06–08. Native implementation retains/releases `mpCombatantTarget` at `+0xE7C`, and stores the other arguments at `+0xB5C/+0xB74`; their broader enum semantics remain unknown. |
| `PlayAbility`, `cCreatureBase.h:132` | thiscall, `void(int, Anim::AnimIndex*)`; `0xC1E5C0` | Original default Bite, index 5, causes delayed damage under A and B identities. A returned void call does not establish a hit. Range, cooldown and native state still apply. |
| `cCombatant::TakeDamage`, `cCombatant.h:84` | thiscall, `int(float, uint32_t, int, const Vector3&, cCombatant*)`; `0xBFCF10` | Receiver is the combatant subobject at full creature `+0x5A8`. Animal virtual damage RVA `0x8075E0` delegates to this observed implementation. All recorded entry/return call IDs pair; the raw int result is not interpreted as a bool. |
| `ConsumeEnergy`, `cCreatureBase.h:142` | thiscall, `void(float)`; `0xC15F20` | Campaign Bite calls amount 0. `mEnergy=1000` and declared ability costs do not demonstrate a nonzero resource debit. Acting creature's native Bite cooldown changes independently. |
| `NPCTickAI` / `AvatarTickAI`, `cCreatureAnimal.h:43–44` | thiscall `void(float)`; `0xC09570` / `0xC04020` | Original implementations remain active and execute once per intercepted call. B uses NPC AI, A uses avatar AI. Intervening ticks are observed during accepted delayed actions. |
| `DestroyInstance`, `SubSystem/GameNounManager.h:86` | thiscall `void(cGameData*)`; `0xB22560` | Invalidates local ownership before original destruction. B retirement/recreation in probe 04 has a fresh actor/native ID. No dereference follows original destruction in this hook. |
| `GetEvolutionPoints` / `AddEvolutionPoints`, `cCreatureGameData.h:22–26` | static cdecl `float()` / `void(float)`; `0xD2E350` / `0xD2E8A0` | A-associated, allied-assisted native reward changes the single campaign balance 0→8.75. There is no per-B reward ledger or qualified reward redirection. |

All commands resolve through a fresh manager noun enumeration on the verified initialization/engine thread. The stored local key alone cannot authorize a call: binding must be live, epoch current, noun type correct, not destroyed/marked, and native noun ID equal to the bound ID. Commands carry integer IDs/owners/values only. Raw pointers and native containers never cross a process boundary. The console owner number is a developer test identity, **not network authentication**.

Nine observational hooks are installed through the checked Detours transaction (eight before build 07). Original functions retain their arguments and execute once. AppUpdate dispatches at most one queued command, then at most one due step per held actor intention. OnModeExit invalidates bindings and drops pending/held work. Hook installation/removal and game calls occur in SDK callbacks, not DllMain. The loader-lock audit remains [M01 compatibility documentation](compatibility.md). The lifecycle record's legacy `gameplay_hooks:0` predates M03 initialization; `actors-*.jsonl` `harness_ready.hook_count=9` is the current installation record.

Probe 06 logged every temporary NPC target assignment and exhausted the 32 MiB trace cap. It exited normally but has no `trace_stop`, so it is incomplete evidence. Build 08 samples owner-0 target calls once per second per tracked NPC (one global timer for unbound actors), while owned actors and explicit dispatch remain unthrottled. Every sampled-out callback still calls the original. Probe 08 records 87 paired target calls out of 4,187 relevant callbacks, with 4,100 explicitly counted omissions; ability/damage observations are not sampled. The final 40,075-record trace is healthy and complete. Recorded target pairs do not constitute a complete NPC decision history.

## Continuing owner input and range correction

`engage OWNER ACTOR TARGET` holds one value-only intention for at most 30 seconds, reauthorizes and resolves live identities each step, and submits native WalkTo/PlayAbility at a 250 ms maximum cadence. It waits for the acting creature's native active/recharge bits to clear. A new valid command supersedes it; expiration, invalid authority/context, unavailable actor/target and scene exit cancel it. Native AI, hit checks, damage and rewards remain unchanged. This bounded developer input adapter is not a network control protocol or complete player-input arbitration.

Probe 07's initial fixed 1.5-unit centre-distance threshold repeatedly asked the actor to walk into the target's body; separation settled near 2.2 units and no held attack was issued. It expired after 30.1161689 seconds. Build 09 instead chooses when to submit attack input from the pinned `cCreatureAbility::mRange` (`+0xA8`) plus both `cSpatialObject::mFootprintRadius` fields (`+0x60`), read through the existing ability catalog (`GetAbilitiesCount`/`GetAbility`, virtual slots `0xB0/0xB4`). Invalid range/footprint values cancel the intention. This threshold is an input heuristic, **not a claimed reconstruction of the native hit formula**; PlayAbility still decides the outcome.

Probe 08 observes Bite range 1, B footprint 0.968847275 and opponent footprint 0.585624814: approach threshold 2.55447197. B kills NPC3 and NPC4 with six native one-point hits each (health 6→0). The held requests end on target death after 8.5410549 and 11.7943048 seconds. NPC3's six native Bite attempts target B but cause no observed reciprocal damage. The second encounter was intended to test Stop; Stop arrived 13.6912861 seconds after death cancellation, so **active Stop cancellation remains NOT RUN**. Active-intention scene-exit cancellation also remains NOT RUN; ordinary scene fencing is verified.

## Native creation failures and correction

Probe 01 created B through the SDK's optional null-herd argument. Subsequent native AI dereferenced the null herd at preferred `0xD41AB7`, reading `mCreaturePersonality` at `+0x15C`. The getter `0xC046F0` returns Animal's `mHerd` (`+0x1674`). The access violation was `0xC0000005`; Windows ended the callback/process with `0xC000041D`; guarded wrapper exit 31. The original failed evidence is retained.

Probe 02 established that `CreateHerd` returns an empty herd in this campaign. Probe 03 supplied that herd to `Animal::Create` and obtained working native A/B actions and reciprocal B/NPC damage. A later target UI update crashed because the foreign NPCs' campaign archetype data was null. Fault `0xB6CA11` follows getter `0xC0C300` of full creature `+0xE84` and dereferences `+0x32C`.

Installed factory inspection at `0xC09C26..0xC09C43` identifies campaign archetype resolution from herd archetype (`cHerd.h`, `+0x88`) and generation (`+0xF0`). The corrected factory uses a live species template and copies its native herd archetype/group/generation/owner-species metadata before native creation. It requires non-null resolved archetype data, and attack dispatch rejects an incomplete target. Probes 04 and 05 have healthy native exits and valid context for every bound actor. The correction preserves native AI/UI/combat/resource calculations.

## Player context and unsupported behavior

| Context | Observed behavior | Remaining boundary |
|---|---|---|
| Local avatar | A is the one manager avatar; B is a separately owned native NPC representation. | A second native player lifetime is qualified, with no complete stage context, local-input arbitration or server-only avatar qualification. |
| Native AI versus command intent | Separate movement/jumps and bounded held attacks work. Bridge 0.0.9 ownership-01 now completes six native hits each B/NPC direction after one initial B attack, with original AI still active. | The located nest-context assumption is corrected in the fixture; complete input arbitration, other AI rules and distant activation remain unqualified. |
| Species / campaign archetype | A and B use the same native species key; foreign NPCs use a live foreign template. | Independent species progression and divergent campaign contexts are not implemented. Native respawn/global campaign state can change derived creature health. `raw_max_health_field` is not the final campaign maximum. |
| Faction / political ID | Bound creatures share `UINT32_MAX` political ID in the fixture. Native damage records still carry distinct attacker objects. | Political ID is not a unique actor/player ID. Faction changes and cross-owner social rules are unqualified. |
| Resources | Synchronous Bite changes the acting creature's cooldown bit 32 while the other owner's cooldown/energy/inventory count stays unchanged. | This verifies the tested cooldown only. Nonzero currency/energy debits, item contents and independent inventory progression were not tested. |
| Rewards | Healthy probe04: A deals one hit; allies finish the NPC; global DNA adds8.75. Probe08 and September 12 ownership-01 B kills emit no AddEvolutionPoints call; campaign DNA remains0. | A separate native B player now exists, but independent stage/reward progression is not implemented. Static native brain, goal, trait, display, action and lifecycle dependencies are documented. A receives no DNA from these kills; reward theft is not demonstrated. |
| Death / recreation | Probe 04 B is destroyed and recreated with a fresh identity; A starves and natively respawns using the same live noun. Probe 05 reload reuses native IDs but assigns fresh actor IDs/epoch. | Dead B cannot currently be retired by the live-only command dispatcher. Other destruction/reuse paths and durable identity restoration remain unqualified. |
| Delayed actions | Probe 04 three jumps complete with the same owner/command in 1.0031–1.0108 s. Probe 05 A completes after reload. Probe 08 fresh B6/owner 2/epoch6 completes command6 after 1.0027685 s and 70 intervening AI ticks. | Probe 05 B's rejected jump remains a rejection. Probe 08's post-reload video ended before the accepted jump; live screenshots and paired native events cover that action. Rapid/reentrant jumps remain unqualified. |
| Other gameplay | Only original Creature movement, Bite and Jump paths have native evidence. | Cell, Tribal, Civilization, Space, editors, independent locations, worker authority, replicas, networking, reconnects and persistence remain mandatory later scope. No dummy/server avatar area-activation claim is supported. |

## Current blocker and next experiment

**Missing capability:** a native player/species reward context that supports B progression without modifying A's native campaign state. The NPC hit blocker is resolved by bridge 0.0.9 in the fixture above. The second native player now has qualified lifetime and distinct native item storage. Native stage initialization, campaign brain-level context, owned reward processing and delayed progression remain unresolved.

Current minimal reproduction: `sporemp_actors setup`, then `sporemp_actors duel 2` (read actual IDs; A1/B2/NPC3 apply to this fresh ordering). Expected full acceptance: native reciprocal combat and a correctly attributed native progression outcome. Actual ownership-01: six native hits each way, B survives at 4, NPC dies at 0, A remains10, campaign DNA remains0 with no award callback. Original AI supplies subsequent attacks without an `engage` command. The complete trace and call IDs are preserved.

Probe 06 identifies a native B target-clear caller RVA `0x9A8097` (preferred `0xDA8097`), inside the behavior routine beginning `0xDA8080`. Native NPC evaluation also temporarily assigns/clears targets from other callers; sampled persistent targets cannot establish the cause. The original target-change investigation and grounded B reload retry are now completed.

The next implementation must give native reward processing the correct owner through calculation, player goal/combat progress and deferred progression, with native creation/destruction and listener routing accounted for. Five action-handler definitions can now be inspected offline; the earlier crash dumps lack their heap pages and do not supply a live handler-vector snapshot. Current next steps are in [focused RE findings](m03-reverse-engineering.md). Active Stop/scene cancellation and nonzero resource costs remain explicit follow-up coverage. Another fight without a changed reward adapter or a precise unresolved runtime question would add no necessary evidence.
