* text=auto eol=lf
# Preserve the user-supplied brief byte-for-byte across Git exports/checkouts.
docs/implementation-brief.md -text
# Preserve recorded evidence and artifact hashes byte-for-byte on every checkout.
evidence/** -text
*.png -text
*.dll -text
*.exe -text
*.zip -text
