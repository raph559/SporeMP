# M04 native worker acceptance

Current status: **VERIFIED for the recorded bounded Creature worker fixture**, bridge **0.0.14**, launcher **0.1.8**. The current session's final evidence assembly and source freeze record the decision and its limits. These gates require original SPORE; C++ two-process fixtures, account ACL probes and launcher fixture tests remain separate HOST evidence. No multiplayer networking, unlimited worker capacity or full B campaign persistence is implied.

Prerequisites: pinned game/content/SDK/loader and fresh source/artifact provenance; two owned OS profiles with hash-checked closed fixtures; verified personal backup; actual mutual write denial; a desktop available for rendered native testing. First repair and validate capture before any manual scene setup. Do not repeat accepted M03 combat/reward tests that this increment does not change.

| Gate | Expected observation | Current result |
|---|---|---|
| Worker initialization | Original game plus bridge start; native app/AI progress becomes live without simulated counters. | VERIFIED on the current desktop, including .14 IPC load into the controlled Creature scene. Private desktop remains FAILED at renderer [1001]. |
| Unattended action | In the controlled original scene, IPC queues distinct A/B actions, rejects wrong owner/stale epoch and matches original action return to delayed landing, with no person playing those actions. | VERIFIED for initial, restored, post-launcher-close, post-player-close and concurrent .14 probes. Original return/landing pairs and wrong-owner/stale-epoch refusals pass. Retained .13 crash-condition action evidence remains separate. |
| Controller/player lifetime | Close the launcher/operator, then a real player process; worker native AI/action progress continues and no authority dies with the client. | VERIFIED for actual launcher closure after completed .1.7 startup and normal-account original player exit. Two valid worker jumps occur 179 ms / 1.330 seconds after player exit and complete natively. Exact closure during a pending mutation has HOST coverage only; network-client disconnect/reconnect remains future work. |
| Two native workers | Two original processes occupy distinct OS profiles simultaneously, both preserve native AI/actions, and observed file/config access shows no save/config cross-write. | VERIFIED within the captured access boundary: games 31528/39716 execute original saves with at least 414.7126 ms overlap; actual `stars.db.tmp` writes overlap 20.1228 ms, each writes its own Games tree, and both closed checkpoints verify. ETW reports no resolved personal/peer-profile access or mutation and zero event loss. Unknown schemas, unresolved write/registry correlations and shared NVIDIA writes remain visible; exhaustive OS isolation is not claimed. |
| Crash containment | Kill exactly one verified native game PID; its supervisor reports crash, the other keeps native progress and accepts an action. | VERIFIED: game 30140 exit -1, supervisor 29916 exit 31, state crashed; game 21944 remains ready and completes both native jumps. |
| Native checkpoint | Native save reaches a proven completion boundary; close/restart/reload its preserved controlled checkpoint; restore/fence identities and reject stale actions. | VERIFIED for the fixed actors-only checkpoint: original full-save return, clean closure, hashed backup, two fresh-generation loads and exact existing A/B noun/herd/species/archetype adoption with zero nouns created. Valid actions and wrong-owner/stale-epoch refusals follow with setup forbidden. Newly sealed concurrent checkpoints are not reloaded. Mismatched-sidecar rejection has HOST/FIXTURE coverage only; native negative execution is NOT RUN. Power-loss durability and full B player/rewards remain unqualified. |
| Shutdown | A healthy original engine exits cleanly through its real loop; stalled startup/exit is bounded and marked forced without claiming save success. | VERIFIED for initial/recovered .14 workers and both concurrent .1.8 WPF Stops: game/host exit 0, healthy same-thread trace retirement and detach 0. Retained .13 exits, deliberate crash and initialization timeout remain distinct. |
| Desktop requirements | Record focus loss, minimization, modal dialogs, lock/disconnect/session behavior, renderer and resource envelope from real observations. | Current desktop and unfocused actions work. Minimization stops app/AI updates; restoration before timeout resumes them. Latest worker 02 has an unexplained approximately 47-second AI plateau after save with continuing app heartbeats; `simulation_stalled` and clean Stop are observed. Short CPU/RAM sample is retained; lock/disconnect/service/RDP/VM, GPU and native frame timing NOT RUN. |
| Launcher | Actual selection/start/stop/status reflects the real supervisor, handles crash/restart and leaves normal Play prominent. | VERIFIED within version-specific evidence: .1.7 WPF Start and closure after startup, normal Play, and .1.8 dropdown held open 15.661 seconds across polls, selection 02/01 and both clean real Stops. Native .1.8 Start is NOT RUN. Poll/mutation/lifetime regression suite passes 39 HOST assertions. Prepared workers require an elevated developer operator; normal Play has no new elevation/account flow. |

For each original process record exact PID, account SID, worker generation, epoch, payload/game hashes, save/creation before/after manifests, command records, native trace, CPU/memory/GPU and capture/file-access provenance. `accepted` means queued; observe the native result independently. Never replay an action after an unknown reply to create cleaner evidence.

For a future changed-path regression, use fresh evidence names and the recovered action probe with `--require-existing-actors`. Record both workers from before startup through native save/clean exit and retain unknown ETW correlations. Keep original scene recovery, basic actor adoption, native-file integrity and crash/power-loss durability separate. Do not repeat the completed native gameplay sequence without a relevant change or unresolved concern. Later M09/M11 work must establish full B campaign/reward persistence; this fixture excludes that state.

The independent [concurrent checkpoint cross-check (archived)](../../docs/public-evidence.md#historical-artifacts "Original path: evidence/2026-09-13-m04-acceptance/concurrent-checkpoint-crosscheck.json") verifies both closed trace hashes, exact save requests 9/7 in epoch 3, actor fingerprints, backup manifest/payload hashes and current closed Games trees. The [full-lifetime concurrent file audit (archived)](../../docs/public-evidence.md#historical-artifacts "Original path: evidence/2026-09-13-m04-acceptance/concurrent-isolation.json") attributes 488 successful own-Games writes to each engine thread and retains 103/102 unresolved file writes, 3,263/3,132 registry SetInformation events and 1,109/1,093 undecoded events. The original personal baseline failure after normal Play is retained: ten files changed, with bounded native player attribution; it is not reported as an unchanged personal profile. The later closed 29-file baseline remains unchanged across the concurrent workers. Source/evidence freeze belongs to the current acceptance session.

Implementation/reproduction: [worker document](../../docs/m04-workers.md). Current native results and exact limits: [acceptance session](../../evidence/2026-09-13-m04-acceptance/SESSION.md), [recovery (archived)](../../docs/public-evidence.md#historical-artifacts "Original path: evidence/2026-09-13-m04-acceptance/restore-01.json"), [concurrent native correlation (archived)](../../docs/public-evidence.md#historical-artifacts "Original path: evidence/2026-09-13-m04-acceptance/native-correlation-concurrent-01.json"), [player lifetime (archived)](../../docs/public-evidence.md#historical-artifacts "Original path: evidence/2026-09-13-m04-acceptance/player-lifetime-after-close.json"). Earlier crash/desktop and implementation evidence: [native session](../../evidence/2026-09-13-m04-native/SESSION.md), [M04 implementation](../../evidence/2026-09-12-m04-workers/SESSION.md). The original M04 acceptance text in MILESTONES.md is unchanged.
