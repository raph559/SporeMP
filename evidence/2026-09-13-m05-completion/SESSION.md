# M05 native reward replay, mutation guards and retained crash

Date: 2026-09-13. Requested task: finish M05. **M05 remains IN_PROGRESS; full acceptance is NOT VERIFIED.** The original M00–M20 work, deliverable and acceptance clauses are retained. This session closes real nonzero reward replay and several native denial tests, fixes an observed native crash, and identifies the remaining presentation/mixed-mutation gate. It does not implement M06 networking or claim playable co-op.

Final source version: bridge/NativeHost **0.0.21**; launcher **0.1.8** is unchanged. This is an explicitly selected developer worker probe. No player-facing launcher change or release-note entry is appropriate. Prior M02–M04 working-tree changes remain preserved. `source-before.json` / `initial-working-tree.txt` record the starting state; source snapshots and payload identities are in `source-state-01.json` (.18), `source-state-02.json` (.19), `source-state-03.json` (.20), and `source-state-04.json` (.21).

## Native results and qualification

Final **0.0.21** native result: A health **3.01911187 → 10**, DNA **0 → 8.75**, five successful native mutation challenges, and **580.2236249 seconds disconnected** before the third challenge. Fresh-baseline replay at **589.0983451 seconds** retains DNA 8.75 with no second reward. **20** connected audits and **2,371** later health/energy/hunger samples match. The original game and supervisor exit **0** at **597.5137985 seconds** after first apply; the healthy trace contains **12,650 records / 6,402,162 bytes**, below the unchanged 32 MiB bound. All 29 personal-file hashes and both sealed worker Games trees remain unchanged. The session retains four clean native runs and two failed runs, with their causes and corrections recorded. [Final closed analysis](native-summary-02.json).

| Run | Role / build | Original game | Result |
|---|---|---|---|
| `native-authority-01` | Authority .18 | PID 20304, engine thread 11628 | Native combat produces global A DNA 0 → 8.75; source captured, clean game/supervisor exit 0. |
| `native-replica-01` | Replica .18 | PID 5276, thread 12948 | Short replay and four challenges pass; later natural population call dereferences denied factory null. Original game crashes; failure retained. |
| `native-off-01` | M05 disabled, .18 | PID 11072, thread 13844 | Original combat earns 8.75 DNA, zero M05 policy events, clean exit 0. No current visual/audio/timing parity claim. |
| `native-replica-02` | Replica .19 | PID 12804, thread 11756 | Five challenges, 437.1948113 seconds disconnected before third challenge, fresh baseline at 450.2652277 seconds; clean exit 461.9581021 seconds after first apply. |
| `native-replica-03` | Replica .20 | PID 18796, thread 10932 | All five expanded challenges pass; trace-budget exhaustion produces a failed progress report, supervisor job exit 35. |
| `native-replica-04` | Replica .21 | PID 22664, thread 16008 | Five challenges; 580.224s disconnected before recheck, 597.514s apply-to-clean-exit, 6,402,162-byte trace. |

The authority's actual damage/award sequence is 6177–6185. A native strike enters Animal damage (call 156), base damage reduces diagnostic actor 3 / native noun 9598 from health 1 to 0, and the original award routine enters at sequence 6181 (call 158, enclosing strike 155, damage 156, caller RVA `807BF1`). Its return at sequence 6182 observes global A DNA **8.75**, previously 0. The last attacking creature is not bound to a diagnostic ID and appears as actor 0. This is a real original combat reward to A's global balance, not proof that A personally made the final attack.

`source-reward-01.json` captures native sequence **10894**, source epoch **3**, A entity **1**, generation **1**, sample **1**. Its exact prefix is **4,108,815 bytes**, SHA-256 `fba33253080c613654dc5ba57c2ff408228d83e400d0f52718d1291e1b81d373`. Source vitals: HP 10, energy 1000, hunger 36.6837425, DNA 8.75. The source report carries scalar bit patterns, worker generation, native PID/thread/clock and prefix hash. It contains no synthetic reward result. The .19/.20/.21 receivers reuse this same .18 source; the capture ABI is unchanged, and re-running native combat just for a version-label match is unnecessary.

The corrected .19 replica first copies health **2.12447119 → 10** and DNA **0 → 8.75**. The .20 replica first copies health **1.38651609 → 10** and DNA **0 → 8.75**. Original AddEvolutionPoints, damage and strike bodies do not run after apply. The pure native scalar setter is used only under the apply guard. A second baseline copies the same absolute balance; it does not add 8.75 again. Duplicate applies return `stale` (control exit 5), post-disconnect applies `invalid` (exit 5), and replica source publication `unavailable` (exit 5). These nonzero command exits are expected rejections, not failed acceptance checks.

The challenge calls actual guarded entry points, with live native A/B, player item storage, herd and persistence manager. It attempts NPC/avatar AI, targeting, selector, PlayAbility, strike, damage, energy, hunger/cooldown, DNA add/set, death/revival/growth, brain change, mark/delete, part unlock/lock, factory creation, herd update and Save. The .20 challenge also invokes the real Strategy dispatcher with the known deferred reward and seven part-grant actions, plus the separate ability-use routine. It checks selected vitals, target/current-attack state, ability/recharge bits, pending-world flag, native item state, lifecycle flags, identities and noun count. It passes through the actual detoured native entry, not through `Policy::allow()` as a substitute for execution. The inventory comparison is a noncryptographic fingerprint plus counts/points, not an assertion of full object-memory equivalence.

The first four closed runs are independently checked by `native-summary.json`. The final six-run analysis is `native-summary-02.json`; it checks contiguous native sequences, one engine thread, monotonic QPC, zero foreign callbacks, exact trace prefixes, native exits/detach, loaded module hashes, checkpoint adoption and command results. A failed run is not forced through healthy-exit assertions. Full milestone acceptance remains a separate human-readable decision.

## Observed crash and correction

Minimal reproduction: with .18, load/adopt worker-02's sealed Creature checkpoint, begin the replica baseline, apply the real source, disconnect, and let the original update loop continue. Expected: local spawning remains denied and the process remains usable. Observed: at 14:58:29 UTC the original executable faults with `C0000005` at image RVA **94F86D**, followed by `C000041D`. The supervisor observes game exit **3221226525**, without a requested shutdown, and records lost engine IPC. The last flushed trace has **45,644** contiguous records but no healthy trace-stop or hook-detach event. Its earlier four successful challenges do not qualify long-run stability.

The saved dump has EAX=0 at the original `MOV EAX,[EAX+B4C]` immediately after `CALL C09B40`. Static whole-function analysis identifies `D4F290` as the parent population routine. The original factory may normally return null, but this caller assumes success. Guarding only the factory exposed that caller assumption. The initial export at interior address `D4F86D` is a fragment; the corrected guard is based on the full `D4F290` body, its prologue and no-argument thiscall convention. The raw stack scan in the existing dump reader is not an unwound call stack.

The correction denies `D4F290`, `D4EDD0`, herd update `C6D960`, and scripted scene-event timer `D52270` before native allocations. Scene-event clock ABI is two 32-bit words, RET 8; HOST detours test a full 64-bit value with the high bit set. The .19 run passes beyond the prior failure interval and exits cleanly. It records **254,875** herd-update and **25,487** scene-event denials; the two direct population guards remain at zero because upstream calls are suppressed. Do not convert zero counters into native invocation evidence. Other factory callers remain a coverage boundary.

Windows Application 1000/1001 records are in `replica-crash-01-events.json`; register extraction is in `replica-crash-registers-01.log`. The two actual dumps are copied under ignored `local/m05-completion/crash-replica-01`; their hashes and original paths are in `research-provenance.json`. No raw dump, game binary or save is added to the repository.

## Retained diagnostic-limit failure and correction

The .20 replica passes the expanded challenges and fresh-baseline replay, then the supervisor ends it at **15:41:34.855 UTC**, game exit **35**, `requested:false`. Its final trace is **33,552,875 bytes** / **90,459 records**, just below the 32 MiB file cap with buffered records still pending. The trace's existing size guard disables observation, so the old status accessor returns a failed phase with zero AI progress. The supervisor reports `regressed_native_progress`. The following shutdown invocation returns control exit 1 / `win32=2` because the supervisor is already closed. Both failure and command are retained; this run is not a clean exit.

The .21 correction emits complete cumulative counter summaries every **5 seconds** and on explicit commands/disposal while still counting every intercepted call and retaining every projection check. It does not increase the 32 MiB file limit. A reserved terminal `trace_limit` record reports `diagnostic_budget_exhausted`; failed status preserves its prior native progress, and the supervisor checks explicit engine failure before checking counter regression. The native terminal-limit event is NOT RUN independently on .21; the final soak checks log growth and clean shutdown below the existing bound. No indefinite logging support is claimed.

A real child-process/private-pipe HOST test reproduces failed status after live progress and verifies `engine_reported_failure`, bounded owned-job termination, and no clean-exit claim. Its first test execution passed the new assertions but failed because the new fixture directory was absent from the explicit cleanup list. That list was corrected. `ctest-06` passes **189 worker assertions**. `ctest-05` separately passes 313 policy / 18 replica ABI checks, and `python-04` passes the five affected tool tests. The diagnostic file-cadence change does not reduce native simulation or presentation quality.

## Presentation evidence and next blocker

Two exact-PID/HWND WGC attempts against the original authority produced zero frames and zero-byte media, despite encoder initialization. Frame extraction consequently failed. Their capture reports/logs remain under ignored local paths referenced by the exact command records. A read-only selected-game screenshot showed the occluding Codex application, not the game. Neither is accepted visual evidence. PresentMon 2.5.1 returned exit 0 for the M05-disabled sample but produced no `frames.csv`; the later replica attempt refused a target that had already exited. No frame-time or audio claim follows from those outputs.

No keyboard/mouse input was sent. Current desktop availability was requested before foreground testing, in accordance with the user's saved preference, and remained pending. The earlier .17 scene/HUD footage remains valid only for its recorded build and fields; it does not qualify current camera, movement, animation, audio or frame-time parity.

The remaining native acceptance gate is the split between presentation and native mutation in live interactions. `C1E8B0` contains effects/projectiles, movement and animation alongside hit/timer work. `C0AE30` contains inline charm expiry. `C2EE80` updates social-result bookkeeping and awards DNA but also chooses UI text IDs through two output pointers and emits effects. Its caller consumes those outputs. A blind no-op or invented UI values would violate presentation requirements. The separate `C19900` ability-use mutation and `D47FC0` part-action dispatch could be isolated safely and are guarded in .20.

Next smallest native experiment: with a visible current-build replica, exercise camera, ground movement, jump/ability animation and audio, then trace a real pickup or social-result interaction through those mixed routines. Preserve the original presentation callbacks while stopping only identified authority writes. Natural consumption, remaining progression/effect state, native binding invalidation/reuse and death/respawn presentation are still unqualified. Do not describe desktop availability as the only missing test or label this M05 VERIFIED.

## Commands, builds and HOST checks

Every recorded executable invocation is a fresh `NAME.command.json` with exact argv, expected/observed exit and start/end UTC; corresponding complete stdout/stderr is `NAME.log`. `run-command.ps1` supplies that record. Native start, load, adoption, duel, capture, replay/challenge, shutdown, archive and personal-hash checks are separate explicit actions. Build and unit-test commands do not launch SPORE or install a DLL into the personal game.

Main build/test commands:

```powershell
cmake -S . -B build/win32 -G 'Visual Studio 17 2022' -A Win32 -T 'v143,version=14.44.35207' -DCMAKE_SYSTEM_VERSION=10.0.26100.0
cmake --build build/win32 --config Release --parallel 4
ctest --test-dir build/win32 -C Release -R 'replica_host|worker_host|native_actor_abi_host|actor_commands_host' --output-on-failure -V
python -m unittest discover -s tests/unit -p 'test_*.py' -v
ctest --test-dir build/win32 -C Release -R 'replica_host|native_actor_abi_host' --output-on-failure -V
python -m unittest discover -s tests/unit -p test_replica_probe.py -v
```

The first .18 build failed on an incorrect raw-pointer accessor, an intrusive-pointer argument and an assumed SDK Save method name. The implementation was corrected to the actual pointer forms and already qualified persistence ABI before any native run. A later HOST fixture build failed on a shadowed elapsed-time parameter and was corrected. Final `build-07` and the affected-test rebuild `build-08` exit 0. `ctest-04` passes 2/2 targets: **313** policy assertions, **18** replica ABI checks and **34** existing actor/award/persistence ABI checks. Replica ABI tests attach twelve real HOST detours, exercise originals and denial results, preserve receiver/argument/AL/x87/clock conventions as applicable, repeat denied calls 4,096 times with ESP checks, and detach successfully. The earlier four-target run also passed **185** worker and **15** actor queue checks. The **103-test** Python suite passed; the five affected replica-tool tests passed again for .21. These are explicitly HOST/FIXTURE tests, not original-game execution.

Static invocations use pinned tooling through `tools/native/invoke-static-audit.ps1 -ReuseDatabase -RunKey ... -Addresses ...`. Seven runs export mutations, grant callers, population/crash context and mixed interactions. `static-interactions-01.command.json` records the last addresses `D47FC0,C19900,C155D0,C2FBB0,C2EE80,C0FE30`. `preserve-research-01` hashes 83 exports and archives the actual manifests/indexes/logs without committing decompiled bodies. A missing optional Python capstone import was abandoned in favor of the installed pinned Ghidra instruction export; no dependency was installed for that failed attempt.

## Provenance and save isolation

Original executable: `C:\\Games\\SPORE\\SporebinEP1\\SporeApp.exe`, GOG GA **3.1.0.29**, SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`. SDK commit `cbf9206b9a823f0911cd9be0217104a49d72380b`; injector commit `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. Ghidra **12.1.3**, JDK **21.0.12.1+1**, VS 2022 v143 **14.44.35207**, Windows SDK **10.0.26100.0**, Win32 Release C++17 /MD. Exact content/loader/modules and their hashes are in each archived native-host trace and frozen identity manifest.

`environment.json` records Windows 11 Pro **10.0.26200**, Ryzen 7 **9700X** (8 cores/16 logical), RTX **4080 SUPER** driver **32.0.15.9649**, plus the reported AMD/Parsec adapters. The developer operator is elevated. The two enabled disposable accounts are `SporeMP-M04-01` / `-02`; their verified distinct OS profiles are reused from M04, with one live original process per account. This session does not requalify arbitrary headless, minimized, locked, RDP or private-desktop support.

Every native start verifies the personal backup and reuses the sealed profile fixture. Backup: ignored `local/backups/automatic-5bc48ae4a6b147fe63b32945423523e7`, **29 files**. Per-run `*-personal-after-result-*.json` checks all personal hashes after process closure. The closed worker Games hashes remain **dd23c69a1a35ad1c9a06f4d103014511f49f547fb17ce303dc405ffcd85d2940** (01) and **f361377b1bc4fa4a41b3ab621e0fdea85ce787c48cea375afdfa563b2925a332** (02). Each `checkpoint_restore` reports `valid:true`, exact existing A/B adoption and `created_nouns:0`. No live profile was reseeded and no shared live profile was used.

Source/payload snapshots freeze 140 files for .18–.20 and 141 files for .21, plus six artifact identities, before each native run. The final bridge/host/control, SDK, injector and executable hashes are in `source-state-04.json`; loaded module hashes are compared independently with those expected identities. Status and evidence documentation added afterward is not misrepresented as code executed in the game. All owned native probes are closed. Final cleanup and evidence-file hashes are recorded in `cleanup.json` and `artifact-manifest.json`. The terminal trace-budget event remains explicitly NOT RUN on .21.
