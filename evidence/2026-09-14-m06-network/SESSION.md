# M06 real network session and authoritative scene replication

Status: **VERIFIED for the bounded living Creature fixture**, by the root's acceptance decision on 2026-09-15. Native acceptance, archive identity, save/process cleanup and exact ACL restoration checks are complete. User explicitly authorized implementation through M06 and PC use for native acceptance. Existing dirty and untracked M01-M05/launcher work is preserved. Acceptance uses actual original-game execution and inspected evidence; no acceptance follows from a build or simulated client alone.

## Acceptance scope

Preserve MILESTONES.md M06 unchanged: two original client game processes join one original authority worker, consistent fixture and distinct controlled actors; stale motion cannot resurrect entities or change ownership; incompatible versions and malformed/oversized messages fail; reconnect gets a new baseline without a duplicate player. Join/rejoin belongs in the launcher. Full shared combat/reward/death belongs to M07 and stage coverage remains M11-M17.

## Current implementation

Network transport: Windows TCP with Schannel TLS and certificate pinning, per-player invite credentials, bounded scalar protocol and engine-thread handoff. Native bindings extend the recorded living Creature fixture only when their actual native tests pass. Network code executes no native gameplay. All native probes retain disposable OS profiles, personal-file backup/hash checks and pinned executable/content/payload gates.

The protocol/coordinator, native adapter, launcher and repeatable harness are implemented. The final integrated CTest run passes all **9 HOST/FIXTURE targets in 10.08 seconds**, exit 0; [exact command](ctest-m06-final.command.json), [output](ctest-m06-final.log). Corrected native05 analysis preserves its action/reconnect evidence and material terminal failure (expected exit 1); native06 analysis passes 400/400 replica samples, all four native action chains and its clean three-process living fixture (exit 0), with inspected WGC presentation. Native09's actual normal-UI Join/Rejoin passes for the same player/entity and fresh baseline 3 → 5. Final personal files, disposable save trees, session-config removal, process cleanup, archive identity and exact ACL restoration pass. [Acceptance decision and limits](acceptance.md).

## Native failures retained

- `native01`: authority PID 18280 loaded Satiria and authenticated, then rejected native noun 2 as `unsupported_living_entity`. Clients 45340 and 8560 exited during their load before authentication/baseline. All three original games recorded exit 0 at 18:28:27 UTC during the interrupted session; no successful shared scene is claimed. Server03's buffered event log was empty after process termination. Personal postcheck `local/m06-network/native01-personal-after.json` verifies all 29 protected files unchanged.
- `native04-diagnostic`: authority PID 11820, worker generation `fe2ae10919aa47a6b2e9032b746e4ac2`, pinned bridge `399947d29fb569f7ffb7e3adb3b986b08e4df3c1abd0ef77be471458a6d9155e`, supervisor `114d89618cf4becc6819624e3ed7a2da9b928d35cd338d2504a52c2822b74fcb`. Exact field diagnostics found 71 unsupported objects; the first 32 recorded objects (IDs 2–33) have no species/profile/herd/archetype data/model/model world/animated creature, spatial disabled, default position and vitals. This establishes unused native allocations were incorrectly admitted to the live-creature census. The subsequent fix distinguishes that observed empty-allocation signature while retaining rejection of partially initialized active creatures. The worker shutdown was requested through its authenticated IPC; coordinator stop used its exact private `stop.request`. The archived traces and host exit events are under `native04-diagnostic`.

Commands, timestamps, observed exit codes and failures are retained in adjacent `.command.json`/`.log` files. Each `source-nativeNN.json` distinguishes that run's actual source/payload hashes despite the common bridge version 0.0.30. Transport rebase handling and periodic low-volume log flushing were built and HOST-tested before the subsequent native05/native06 runs; [transport evidence](host-transport/SESSION.md).

## Native05 — original actions and reconnect, followed by terminal death divergence

Authority PID 41472, client A 5752 and client B 3716 join with distinct owners and actual native baselines. The [action harness](native05-actions/report.json) completes all four requested actions: owner 1/2 move 1.095381/1.092296 native units, and the corresponding accepted original jumps have later matching landings after 0.7351630/0.7535129 seconds within the authority trace. The source/global entity pairs are owner 1 `12884902120`/generation 30 and owner 2 `12884911487`/generation 33. These are observed native actions, not queued acknowledgements alone.

Original client B exits and a fresh process PID 4024 rejoins as the same player 2 with baseline 2 → 3 and one new native baseline application, without adding another controlled player. [Before reconnect archive](native05-before-rejoin/report.json), [after reconnect snapshot](native05-after-rejoin/report.json), [closed archive](native05-closed/report.json). [Corrected analysis](native05-corrected.json) matches 607/607 native replica samples (346 + 222 + 39), validates the original action chains and reconnect, and deliberately returns **expected exit 1** for the material terminal death/removal failures. [Command/exits](native05-corrected.command.json). Actor QPC is per-process relative; no cross-process latency/overlap duration or absolute QPC origin is inferred. Native within-process action intervals above remain valid. The earlier analyzer's cross-process numbers are superseded.

The eventual owner-1 death remains a failed terminal boundary: authority event 35640 despawns entity `12884902120`, later native states record health/hunger 0 and dead true, but client A remains alive at approximately 0.00364595 health and reports `Native entity removal failed.` The avatar-destruction guard prevents that projection; death/respawn is not implemented or accepted. This does not erase the earlier living-Creature action/reconnect evidence, and the run is not represented as clean through death. It is an explicit M07 prerequisite. All four selected native generations close with healthy traces and game exit 0. The [personal postcheck](C:/Users/Developer/Documents/ChatGPT/SporeMP/local/m06-network/native05-personal-after.json) records 29 protected files unchanged and zero recognized running workers.

## Native06 — inspected living-scene presentation and clean original closure

The exact source/payload freeze is [source-native06.json](source-native06.json): bridge SHA-256 `147ee5736e1fe361170ca3985b8b00dd602c0256f708000c256dba6ad582397c`, NativeHost `17e9b403e187a2d2ff1a9f71636345ae44eeef058baa279e943cfa28174b778a`, coordinator `e97309caf2b72901573b266fa7dc923ce23824def87445a6055f4a6fd6ff3881`. This source fixes the earlier title/overlay readiness signal interference. Native06 uses authority 24228, client A 45400 and client B 27264. Both clients apply a 33-entity native baseline; client A controls owner 1 and client B owner 2.

The exact command `python tools/native/m06-harness.py actions --output evidence/2026-09-14-m06-network/native06-actions --coordinator-run local/m06-network/server06 --seconds 8` exits **0** and completes all four checks. Owner 1/2 move **3.481028 / 5.705179** units. Original jump-to-landing intervals are **0.9390574 / 1.0089260** seconds within the authority trace; no scene lifecycle change occurs during these commands. Owner 2's native06 entity is `12884911489`/generation 33, distinct from the earlier session's native05 ID. [Command](native06-actions.command.json), [report](native06-actions/report.json).

Three concurrent exact-process WGC captures each request 20 seconds and exit 0 without timeout. The root operator inspected all contact sheets and observed the same original nest/terrain and blue creatures, distinct movement across the scene and airborne poses. The authority is unoccluded. [Visual review and complete source hashes](native06-visual-review.json) links the local original media/contact/request/completion files. Review is of contact sheets, not a claim of continuous frame-by-frame analysis, audio or native frame-time measurement; captures explicitly use `-an`.

The exact command `python tools/native/m06-harness.py archive --output evidence/2026-09-14-m06-network/native06-closed --coordinator-run local/m06-network/server06` exits **0**. [Closed archive](native06-closed/report.json) contains 36,046 authority, 3,593 client-A and 1,476 client-B actor records, all with healthy final traces. No `network_status:error` appears in these selected runs. Original game/worker exits are 0: client B at 22:09:46.874 UTC, client A 22:09:47.126 UTC, authority 22:09:47.405/406 UTC. All three recorders completed and the native06 coordinator stopped cleanly.

[Corrected native06 analysis](native06-analysis.json) returns **exit 0**, matching 400/400 actual replica samples: 292 client A and 108 client B. Exact source ticks/generations, local-avatar ownership, interpolated native pose/velocity and all four hash-bound original action chains pass. A recorded generation-bound live IPC/status witness, corroborated by NativeHost UTC lifetimes, establishes the three current original processes. There are no material network errors or controlled-actor terminal deaths in native06. Its reconnect gate is NOT_RUN; that evidence remains the separate native05 reconnect. [Exact analyzer command/exits](native06-analysis.command.json). No cross-process QPC subtraction, absolute QPC origin or derived latency distribution is claimed.

The [native06 save check](native06-save-check.json) verifies all 16 Games files in each of profiles 01/02/03 against the fixture manifest and their empty Creations trees, all unchanged. The [native06 personal postcheck](C:/Users/Developer/Documents/ChatGPT/SporeMP/local/m06-network/native06-personal-after.json) verifies all 29 protected files unchanged with zero recognized running workers. These checks apply to the completed native06 interval; later staged launcher runs need separate final checks.

## Native07 — actual first launcher Join, with later source death retained

The [first staged actual WPF Join archive](launcher-native07-closed-02/report.json) records original game PID 18176, authenticated player 2, baseline 3 and one owned native avatar. Actual baseline application precedes Connected, the backend confirms the completed Join, the native lifecycle closes healthy and temporary session configs are removed. Its material-network-error gate passes. This validates an actual launcher/native Join and is not relabeled HOST fixture evidence. Rejoin remains explicitly NOT_RUN in that archive; native09 supplies the completed pair.

The source worker from the longer native07 interval still reaches an unsupported owner-1 death: PID 25564, actor-state sequence 50676, native ID 232, health/hunger 0, dead true. [Closed source archive](native07-source-closed/report.json). Keep that terminal limit separate from the first successful launcher Join; death/respawn is not accepted by M06.

## Native08 — failed Join after source death/respawn

The original authority PID **32020** died and respawned before the attempted pause, advancing the affected source generation to **34**. The [actual failed launcher Join](launcher-native08-failed/report.json), game **26684** / host **10768**, records `Shared scene could not be applied: native_failure`. It has no connected baseline, owned-avatar adoption or replica samples; the backend correctly does not report a completed Join. Healthy closure and removed temporary configs do not convert the failed admission into a pass. [Closed native08 source](native08-source-closed/report.json). This death/respawn limit remains separate from the later living-fixture test.

## Native09 — actual normal-UI Join/Rejoin and clean closure

The operator starts a new living source and pauses it using the original Options menu at approximately **22:32:20 UTC**. Authority **39776** / supervisor **13384**, generation `6bac482ecc774efd9163e7bfe1bdb59e`, and client A **32396** / supervisor **15116**, generation `ebebd0c87caf471390d4f3a898007563`, supply the session. This pause bounds the launcher lifecycle test before the unsupported death boundary. It does not qualify moving gameplay while paused or death/respawn; native06 supplies the separate movement evidence.

The [actual WPF Join/Rejoin archive](launcher-native09-closed/report.json) verifies both ordinary Home UI runs using the same invitation:

| Run | Original game / host | Authenticated and controlled identity | Baseline | Closed native result |
|---|---|---|---|---|
| Join `join-cb970b5f45ca4935` | **22428 / 43908** | player 2; entity **12884911489**, generation **33**, owner 2; one local avatar **232** | **3** | 102 replica samples; no material network error; healthy lifecycle; game exit **0** at **22:34:09.377 UTC** |
| Rejoin `join-7de52971596a42d4` | **42088 / 21524** | same player/entity/generation/owner; one local avatar **232** | **5** | 60 replica samples; no material network error; healthy lifecycle; game exit **0** at **22:35:36.471 UTC** |

All per-run admission/ownership/lifecycle gates and `rejoin_identity_and_fresh_baseline` pass. In each run native baseline application precedes Connected and the backend reports the completed Join. Temporary session-config removal passes with **zero entries** remaining at **22:36:32.849112 UTC**. The report's `native_game_started:false`/`desktop_input:false` fields describe the read-only archiver; the operator performed the actual UI launches documented by its per-run traces.

The root operator inspected all seven `launcher-native09*.jpg` screenshots, including the original Options pause, Connected states, both actual original-game windows showing the same fixture, Rejoin-ready and final closure. The archive contains their exact names, JPEG formats, byte counts and SHA-256 values. Its generic `MANUAL_REVIEW_REQUIRED` image tags are supplemented by the [completed operator UI review](launcher-native09-ui-review.json). Test-account staging protects the developer fixture; normal Play/Join continues to use the current Windows account without a worker-account setup flow.

[Native09's closed source archive](native09-source-closed/report.json) contains **15,832** authority and **5,311** client-A actor records with healthy closed traces, matching the source-native06 payload hashes. The worker-03 slot retained in that archive is historical native06 data, not a new third native09 source process. The actual native09 player-2 lifecycles are the separate launcher runs above. The post-close process witness observes no games. The root operator confirms WPF, all owned games/supervisors and recorders closed and server09 clean exit **0**; [final process check](final-process-check.json).

## Final protection, host checks and acceptance boundary

[Final personal comparison](final-personal-check.log): all **29** protected files unchanged. [Final save comparison](final-save-check.json) at **22:36:02.283515 UTC**: all **six** trees unchanged, with **16 Games files** and an empty Creations tree for each of profiles 01/02/03. Fixture manifest SHA-256 remains `fcc48e03c54bc71f93f259f46b36a2c2708acc344fb81b0417578bd84083c284`. These checks cover the final launcher interval as well as the preceding native tests.

The final `ctest --test-dir build/win32 -C Release --output-on-failure` run exits **0**, **9/9** passing in **10.08 seconds**, including `network_host` in **3.62 seconds**. Its `.command.json` and `.log` above retain exact invocation, expected and observed exit codes. These remain HOST/FIXTURE evidence; original-game acceptance is supplied by the native traces, inspected captures and actual WPF lifecycles.

[Executed ACL restoration](launcher-acl-restored.json) at **22:37:02.4197928 UTC** confirms zero native/staged launcher processes, all **six** restoration commands exit **0**, and all **20** checked paths match their exact original SDDL. The archive's pinned trace, player/entity ownership and fresh-baseline identity gates pass. No native acceptance or cleanup gate remains open.

The [final native payload consistency check](final-native-payload-consistency.json) at **22:39:52.9353257 UTC** finds no runtime source or payload changes from `source-native06.json`; all seven frozen payload artifacts remain unchanged.

The root records **M06 VERIFIED for the bounded living Creature fixture**. The [acceptance decision](acceptance.md) preserves every earlier native05/native07/native08 death failure and separates native observations, real TLS/HOST negatives and visual inspection. Individual harness reports keep `milestone_acceptance:NOT_VERIFIED` because they report evidence rather than decide milestone status. Death/respawn and shared encounter state remain M07; arbitrary content remains M08; coordinator-restart persistence remains M09. No cross-process QPC latency or absolute-origin timing claim is used, and no additional native rerun is required for M06 closure.
