# M03 native actor commands — 2026-09-09

Status: BLOCKED at full M03 acceptance; implemented native research increment with partial verified paths. User authorized implementation and computer control. M02 is closed; its evidence is preserved. Eight M03 native attempts are recorded below. Probe08 now completes two native B kills and an accepted B jump after reload. Native NPC hit eligibility and independent B player/reward context remain unresolved. Failed/incomplete attempts do not grant acceptance. Current results: native-analysis-02.json, acceptance.json, visual-review.json and ../../docs/m03-native-context-audit.md. The probe05 checkpoint below is historical and superseded by the final probe08 section.

Implement an opt-in developer harness in the bridge, with native creature creation, per-actor movement/actions, explicit ownership, queued value-only commands, destruction/scene fencing, and original damage/action/resource observations. Normal Play must clear the harness switch. All native attempts use the previously measured disposable OS account and guarded wrapper, with personal-file backup/hash checks. Builds and tests do not launch a game.

Pinned inputs: SDK cbf9206b9a823f0911cd9be0217104a49d72380b; injector 26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9; installed executable SHA-256 dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37. Working HEAD d106404da0b7c4531f63f98d0df2377bb897a0f7 includes preserved uncommitted M02/launcher changes. The original M00–M20 acceptance text remains authoritative.

Initial inspection: no SporeApp.exe process running. Pinned headers and source wrappers provide cCreatureAnimal::Create (cdecl), cCreatureBase::WalkTo/PlayAbility/DoJump (thiscall), cCombatant virtual targets, and per-creature ability bits. Installed binary disassembly at 0xBFCF10 reads receiver+0x28 and calls attacker vtable+0x60/+0x64, matching cCombatant::mpLastAttacker/AddRef/Release and the five-argument SDK TakeDamage declaration. The cCreatureBase combatant subobject offset is 0x5A8. This is an ABI audit, not runtime acceptance. The native DNA accessor is static/global and must not be treated as per-actor storage.

Initial read-only command errors: one unquoted SDK path caused rg exit 1; corrected with quoted paths. A guessed Math/Vector3.h and Program Files Visual Studio location did not exist; rg found MathUtils.h and installed objdump at C:/msys64/ucrt64/bin/objdump.exe. No files or game processes were changed by these failures.

Commands, builds, native results, source/artifact hashes and the smallest remaining step will be recorded here and in fresh adjacent evidence files as work proceeds.

First build: configure-01 exit 0; build-01 exit 1 because native_actors.cpp omitted generated build_identity.h; corrected include, build-02 exit 0; ctest-01 5/5 HOST/FIXTURE targets pass, exit 0. Initial run-command.ps1 invocation using -File did not bind unknown CMake flags; call the script with an explicit -Arguments array. These errors launched no game.

Native probe-01: host 4912, game 32280, engine thread 33752; guarded payload/identity in provenance-01.json and probe-01/native. Native setup bound A=1/native232 and B=2/native7424 with distinct owners. The second actor was created with the SDK factory's default null herd. The game crashed in subsequent Creature AI at preferred address 0xD41AB7, reading null herd+0x15C (mCreaturePersonality); preceding call 0xC046F0 returns cCreatureAnimal::mHerd (+0x1674). Original exception is access violation 0xC0000005; Windows terminated the callback/process with 0xC000041D. Wrapper exit 31, personal-file postcheck passes. M03 is not accepted from this run. Crash dumps remain local under the disposable profile; dump-01.json contains exception metadata, and recovered-buffer-01.json preserves complete M03 JSON records recovered from the dump (not a complete healthy trace).

Corrective implementation: use native GameNounManager::CreateHerd with one creature, Guard personality, no nest, and normal native member setup. This supplies the campaign AI context instead of suppressing AI or inventing behavior. Flush after binding so a later native fault cannot hide already-completed setup diagnostics. Reproduction remains the fixed sporemp_actors setup command. The next attempt tests this correction before movement/actions.

Probe-02 (host33096/game26256/engine7548) returned a valid but empty native herd; setup recorded create_failed and bound only A. No crash. Native starvation damage observed on A while idle; that is environmental damage, not a successful attack test. Quit without saving through native UI; wrapper/game exit0 and 29 personal hashes unchanged. Fresh closed 27-file snapshot fixture-probe-02-end.json. The recording visibly contains the loaded game (scene.png at30s); first-scene.png at8s is the loading screen. No two-actor acceptance from this attempt.

Build-04 supplies the native herd to Animal::Create when the new herd is empty. Installed factory 0xC09BD4..0xC09D76 retains the herd and adds the member. Target adapter now uses the actual SDK virtual SetCreatureTarget(target,false,0), not the general cCombatant target. Installed animal vtable (file offset0x1069480, slot0x84) points to preferred0xC03DF0; function retains/releases mpCombatantTarget at0xE7C, stores its other two arguments at0xB5C/0xB74 and returns12stackbytes. Native caller0xD3599B..0xD359B0 uses target,false,0. Guard the exact runtime RVA0x803DF0 before calling this virtual. No direct field assignment or avatar swap added. Host build-04 exit0.

Probe-03 (host11712/game30932/engine33340) created distinct A/native232 and B/native7425 with continuing native AI. Both native jumps completed asynchronously with the matching actor/owner/command; distinct WalkTo requests and wrong-owner rejection were recorded. B attacked native NPC4: ten native damage callbacks reduced NPC4 from10 to0; nine reciprocal NPC attacks reduced B from10 to1. Native last-attacker identities match. Native Bite cooldown changed on the acting creature. Campaign ConsumeEnergy received amount0, so this run does not demonstrate a nonzero energy debit. No global DNA award was observed. A's subsequent attack on NPC3 returned from the native ability call, then crashed during the target UI update. Wrapper31, exceptionC0000005 at preferred0xB6CA11 (RVA0x76CA11), null EAX. Both foreign NPCs had a null archetype record, which the UI reads through getter0xC0C300 (full creature+0xE84), then dereferences+0x32C. Native factory0xC09C26..43 resolves that record from herd archetype+0x88 and generation+0xF0 through0x4E00C0. The new herd's default archetype0 was incomplete campaign context. Failed run and dumps metadata are preserved; no healthy M03 acceptance is claimed. Personal postcheck passes29 unchanged; disposable snapshot fixture-probe-03-end.json verifies27 files. Video is local/m03-captures/0909-probe-03/capture.mkv.

Build-05 exit0: create the herd using a live same-species template, copy its native archetype/group/generation/owner-species metadata before Animal::Create, require the factory to resolve a non-null archetype record, and reject attacks on an incomplete target. This fixes factory context without changing native UI, AI, combat or resource formulas. Log campaign_context for each binding. Next experiment: fresh guarded probe-04, confirm both opponents have native archetype context, then execute A/B combat, asynchronous actions and lifetime fencing.

Probe-04 (host23648/game30984/engine18212), provenance04/build05: all four bindings have native archetype data. A attack produces native damage on NPC3, NPC3 retaliates three times, and original allied NPCs assist and kill NPC3. Global campaign DNA changes0->8.75 and the native tutorial appears. Attribution of this assisted reward is still only the original campaign account, not independent B progression. The tutorial interrupted the first attempt to submit B's command; the retained console text was submitted after dismissal, once, command4. B was already far from NPC4 and its ability attempt caused no damage. Setting NPC4's native target to B subsequently returns to A under its original AI. Preserve that limitation rather than treating a PlayAbility call as a successful distant attack.

Probe-04 A/B distinct six-unit WalkTo commands reached within0.4892 and0.9579 units of their respective requested destinations within5s. Native AI remained active. B/native7425 was destroyed through the manager (command7), replacement B received actor5/native7435; old actor2 jump rejected unknown_actor and actor5 jump completed natively. Both initial jumps and the replacement jump were visible. A also starved, died and respawned through the original game with the same native noun232; no artificial health setter was used. Quit without saving: host/game/wrapper0, trace_stop healthy true, detach0, foreign callbacks0; personal postcheck29 unchanged; fixture-probe-04-end.json verifies27 files. Video records600s (capture duration limit), so the final menu exit is documented by GUI audit and host trace rather than claiming video coverage beyond that limit.

Build-06 exit0 adds explicit owner-checked approach via native WalkTo and a duel OWNER fixture command that creates one fully initialized nearby NPC and queues one owned native attack in the same engine update. It does not force subsequent damage or NPC attacks. This avoids test setup latency moving B out of reach. Fresh native resolution additionally matches the recorded native noun ID against the live address, and raw mMaxHealthPoints is labeled raw_max_health_field because it is not the final campaign maximum. Next: fresh probe05, B-first close-range combat, compare actual rewards/resources, then scene exit/reload fencing.

Probe-05 (host32296/game33012/engine30016), provenance05/build06: B/native7425 attack command1 yields original damage on fully initialized NPC3/native7427 at sequence670, health6->5. NPC3 subsequently attempts native abilities targeting B without a recorded reciprocal hit. B approach command2 uses native WalkTo, coming within1.20894 units of the sampled target destination in the next5s. Attack command3 produces a second native damage callback at sequence1433, health5->4. B subsequently has no target and NPC3 selects A under continuing original AI. This records actual selection, not proof of its cause or a hardcoded-avatar defect. No controlled B encounter completion or reward is qualified; global DNA remains0.

Probe-05 exits to the native galaxy menu without saving and reloads Satiria in the same process. Old actor2 is rejected unknown_actor. Native IDs232/7425 are reused by the loaded fixture/factory, while fresh bindings are A4/B5 in epoch6, preventing stale ownership reuse. B jump command4 is queued but native DoJump returnsfalse at sequence3546: B was already airborne in native AI activity. The visible jump is not credited to this request and there is no matching commanded landing. An initial visual-only commentary overstated this; native analysis corrected it. A jump command5 returns true and lands at sequence3948, 1.0106677s after command begin, under owner1/actor4. Earlier probe04 already has three accepted owner-matched commanded jumps, including B's recreated identity.

Probe-05 final native Quit/Don't Save succeeds: game/host/wrapper0, same-thread bridge disposal, trace_stop sequence4561 healthytrue/detach0/foreign0. No malformed or noncontiguous records; original ability/damage entry-return IDs pair. Personal postcheck retains29 files. Closed fixture-probe-05-end.json verifies27 files. Across all five snapshots, Games/creations retain starting hashes; GraphicsCache, Pollination, cacheDB and the native event file have normal recorded changes. No fixture restoration was performed between these attempts. All personal postchecks are unchanged, including the failed probes.

## Historical checkpoint through probe05 (superseded below)

Checkpoint executable validation: `& ./evidence/2026-09-09-m03-actors/run-command.ps1 -Name ctest-02 -Program ctest -Arguments @('--test-dir','build/win32','-C','Release','--output-on-failure','-V')`, expected/observed exit0, five HOST/FIXTURE targets pass (13 actor assertions; 60 Python tests; other targets in ctest-02.log). This validates implementation/tooling only. Build06's loaded bridge SHA-256 is36c62273a5084170565e657dfd552fb0ea33789fa4fbe9befab17dacc8136f9d; source native_actors.cpp SHA-2569e234e7afb30f694f25e582e48ba85f27fd2a858f15e59111ff3559c533cb78a. These hashes apply to probe05, not the subsequent builds.

Analysis command: `& ./evidence/2026-09-09-m03-actors/run-command.ps1 -Name analyze-01 -Program python -Arguments @('evidence/2026-09-09-m03-actors/analyze.py')`, expected/observed0. Native-analysis.json keeps failed runs separate and grants no acceptance. Capture command for probe05: `pwsh -NoProfile -File tools/native/capture-game.ps1 -GamePid 33012 -OutputDirectory local/m03-captures/0909-probe-05 -Seconds 600`, observed0, no wall timeout; actual source closes with game. Captured media and action keyframes were inspected. `python evidence/2026-09-09-m03-actors/review-frames.py`, expected/observed0; exact FFmpeg extraction commands/hashes in review-frame-extraction.json. Input audit records one native UI action per fresh screenshot, with UTC/window/command. WGC frame cadence is not native timing and is not used as such.

Full M03 acceptance remains BLOCKED: the local mapping and bounded native actions do not yet supply persistent owned intent, a completed healthy B/NPC encounter and identified native reward beneficiary. Native political IDs are not player IDs; DNA is global; nonzero costs and independent progression are unqualified. No server-only avatar or remote area activation is implemented/tested. M04–M19 gameplay acceptance is marked BLOCKED without removing scope; independent infrastructure/research remains allowed. The next experiment is the controlled, visibly framed B encounter with native target-change caller tracing, then kill/reward attribution and a grounded post-reload B jump. There is no user-permission blocker. No game remains running after this session.

## Probe06 — native target callers and a diagnostic-cap failure

Build07/provenance06 adds the ninth observer, Animal SetCreatureTarget at preferred0xC03DF0 / RVA0x803DF0, with the original thiscall arguments unchanged. It records full actor/native identity, prior/new target, flag/intention and caller RVA. The existing global DNA observer also records caller RVA. This is observation; no native targeting or reward rule was replaced.

Probe06: host25404/game22608/engine29304, game creation15:17:54.105Z, normal game exit15:31:26.467Z, wrapper exit0. B attacks fully initialized NPC3 once (sequence646, health6→5); subsequent approach/attack commands do not complete combat. Native B target clear at sequence3566/qpc1802219254 comes from RVA0x9A8097 (preferred0xDA8097), inside the behavior routine starting0xDA8080. Native NPC evaluation repeatedly assigns/clears temporary targets. These calls prevent treating one sampled target as a complete persistent AI decision or proof of a hardcoded-avatar bug. The exact disassembly commands for0xC03DF0,0xDA8080 and the NPC callers are in target-binding-01.json, target-clear-01.json and target-ai-01.json (all expected/observed0). Only aligned function portions support interpretation.

Logging every temporary target change exhausts the32MiB cap:33,529,488 bytes /86,335 complete prefix records, with no trace_stop. The normal OS exit does not make this a healthy complete trace. The incomplete file is retained. Build08 corrects diagnostic volume by sampling owner-0 targets once per second per tracked NPC, plus one unbound timer, while owned actors and explicit dispatch remain unthrottled. Every original target call still executes. Omitted counts are explicit; damage/ability observers are not sampled.

A first closed-fixture snapshot attempt raised `RuntimeError: Close the game normally first` (exit1): a menu-animation mismatch had selected Exit to Main Menu, so the game was still at the galaxy. After actual native Quit, the guarded snapshot succeeds and verifies27 files. All29 personal hashes remain unchanged. The600-second capture ends before final quit. This failure changed no saves or source. Later read-only capture metadata lookup initially guessed capture-request.json; the actual file is request.json.

## Probe07 — bounded owner intention and an approach failure

Build08/provenance07 introduces `engage OWNER ACTOR TARGET`: one value-only held intention per tracked actor, maximum30s, a due step no more often than250ms, fresh authorization/native resolution on every step, and native active/recharge checks. Native WalkTo/PlayAbility do all actual movement/combat. Native NPC AI remains active. Superseding input, expiration, invalid identity/context, actor/target unavailability and scene exit cancel the intention. No native pointer persists in the queued command.

Probe07: host31968/game11444/engine24744, creation15:32:10.387Z, exit15:42:47.051Z, game/wrapper0. The initial duel attack reduces NPC3 health6→5 at sequence4521. Held command2 starts sequence7285/qpc4499249461 and repeatedly submits native approach. A fixed1.5-unit centre threshold cannot be reached without overlapping creature footprints; actual centre separation settles around2.2 units. No held attack is issued. Expiration cancels at sequence13393/qpc4800411150 after30.1161689s. This qualifies the bounded expiry branch, not successful held combat. A's visible starvation/death is ordinary campaign behavior, not a NPC hit on B. Native respawn and normal Quit follow.

The trace ends healthy at sequence15948, detach0, foreign callbacks0. Target observations contain119 paired calls;10,079 of10,198 relevant callbacks are explicitly sampled out. Personal29 and the closed disposable27-file snapshot pass. Build09 corrects input selection to use native Bite range plus both native footprint radii. The threshold is an input heuristic; it does not claim to replace or reproduce native hit checks. Invalid range/footprint values cancel the intention. Build09 and ctest04 both exit0; all five HOST/FIXTURE targets pass, including15 actor-authority assertions,60 Python tests and22 launcher assertions.

## Probe08 — two native B kills and successful B reload action

Final candidate: build09 / provenance08. Host19852/game33168/engine32304; creation15:44:09.098Z, bridge initialization15:44:13.786Z, same-thread disposal16:00:32.458Z, normal game exit16:00:33.399Z and wrapper0. Original Creature campaign Satiria, pinned GOG GA executable,108 content fingerprints, disposable SID S-1-5-21-1000000000-2000000000-3000000000-1007. Windows11Pro10.0.26200/build26200, Ryzen79700X8cores/16threads. Toolchain: VisualStudio17.14, MSVC14.44.35207, WindowsSDK10.0.26100.0, Win32 Release. Native Debug is NOT RUN.

A1/native232 and B2/native7425 have complete campaign archetype context. First duel creates NPC3/native7427 and submits B attack command1. Native damage sequence560 reduces health6→5. Held command2 starts sequence912; five additional native B hits at sequences940,1015,1067,1113,1167 reduce health5→0. Every post-damage last-attacker is B2. Native dead state becomes true at sequence1172/qpc2018041091 (the immediate damage-return sample still has dead=false, so death is not represented as synchronous with the call). Held intention cancels at sequence1174/qpc2020024975,8.5410549s after start. Native attack input uses range1 plus footprints0.968847275/0.585624814, total2.55447197; native hit delays/cooldowns remain effective.

NPC3 executes six native Bite calls targeting B at sequences577,957,1018,1070,1116,1118, but none causes a recorded B damage event. A issues no attack and stays near the centre. No DNA call occurs. Repeating duel/engage against NPC4/native7429 yields another six B hits, from sequence2726 through8651, health6→0. Native dead state is sequence8663/qpc3099964119. Held command4 cancels after11.7943048s. There are no allied damage callbacks against either target and no NPC-to-A/B damage callbacks in this run; A's health decline is recorded environmental starvation. The native global DNA balance remains0 throughout.

The second encounter was intended as an active Stop test, but UI action latency allowed B to kill the NPC first. Stop command5 at sequence11450/qpc3237530353 arrives13.6912861s after target-death cancellation. It cannot qualify active Stop. Both held intentions were already finished when exiting the scene, so active-intention scene-exit cancellation also remains NOT RUN. These cases remain separate from verified ordinary scene fencing.

Exit to Main Menu without saving and reload Satiria in the same process: old B2 is rejected unknown_actor. Fresh A5/B6 bind at sequences32622/32639 in epoch6; original native IDs232/7425 are reused. B6 command6 begins sequence35881, native return35883 is accepted=true, matching owner2/command6 landing36083 follows after1.0027685s with70 intervening AI ticks. The final native Quit uses Don't Save twice. The scene's idle A starves and respawns before the quit menu settles; early Escape inputs during that cinematic did not open the menu. Input labels describe intended actions, and fresh observed state controls the next action.

Probe08 trace:40,075 contiguous valid records, one healthy trace_stop, detach0 and foreign0. All18 recorded ability pairs,10,086 damage pairs and87 target pairs match;4,100 of4,187 relevant target callbacks are explicitly sampled out. Loaded payloads match provenance08. Across all eight attempts the29 personal files and disposable Games/creation content hashes remain unchanged; only recorded native cache/event files differ. No fixture restoration was performed between attempts. The closed probe08 snapshot verifies27 files. No game remains running.

## Final artifacts, commands and remaining native issue

Latest bridge SHA-256: `01cf9279cde15a4a9e920eed19290fd01d5da1ab669a366329f5eb4eff83c523`; native host: `289a2cf6d866b61351f801217bad466c2f8e9590b5426515a726ab77e41cddee`; native_actors.cpp: `5bc78823a9a5c99945b7697c7d95d837ca9fba66f3db489e0f73d6928b70a15f`. No executable source change follows build09. provenance08 pins all contributing source/tool/payload hashes; artifact-manifest.json verifies the final source still matches and records final document/evidence hashes. Earlier provenance01–07 remains attached to each corresponding native run.

Exact build/test argv, expected/observed exits and UTC are in configure-01.json, build-01..09.json and ctest-01..04.json; adjacent logs retain output. Each probe/completion.json retains the actual isolated launch arguments and exit, and each native/native-host.jsonl retains guard, module hashes and game exit. Input-actions.jsonl records actual GUI actions with UTC/window and completion status. New capture command examples, both observed0:

```powershell
pwsh -NoProfile -File tools/native/capture-game.ps1 -GamePid 33168 -OutputDirectory local/m03-captures/0909-probe-08 -Seconds 420
pwsh -NoProfile -File tools/native/capture-game.ps1 -GamePid 33168 -OutputDirectory local/m03-captures/0909-probe-08-reload -Seconds 300
& ./evidence/2026-09-09-m03-actors/run-command.ps1 -Name fixture-08-end -Program python -Arguments @('evidence/2026-09-09-m03-actors/fixture.py','snapshot','probe-08-end')
& ./evidence/2026-09-09-m03-actors/run-command.ps1 -Name analyze-02 -Program python -Arguments @('evidence/2026-09-09-m03-actors/analyze.py','--runs','8','--output','native-analysis-02.json')
& ./evidence/2026-09-09-m03-actors/run-command.ps1 -Name review-02 -Program python -Arguments @('evidence/2026-09-09-m03-actors/review-frames-02.py')
& ./evidence/2026-09-09-m03-actors/run-command.ps1 -Name close-evidence-01 -Program python -Arguments @('evidence/2026-09-09-m03-actors/close-evidence.py')
```

The first eight and five follow-up native contact sheets were inspected; visual-review.json records what each actually shows, including offscreen/ambiguous views. Probe08's second kill visibly ends with the green NPC collapsing. capture-manifest.json hashes raw local recordings and retains exact FFmpeg argv/request/completion metadata. WGC cadence is not native timing. The300-second reload recording finishes15:57:10Z, before B6's15:58:49Z jump. That accepted delayed action has paired native events and live screenshots, but no continuous recorded video. No game binaries, saves, recordings or dumps are added as source artifacts.

Full M03 remains BLOCKED by native NPC hit eligibility against B and missing independent player/species reward context. B combat completion, held input, target-caller observation and the grounded reload retry are now demonstrated; these are no longer the next experiments. Minimal reproduction is setup → duel2 → engage2/2/3 with actual fresh IDs. Expected: native reciprocal interaction and correctly owned progression. Actual: B kills the NPC, the NPC attempts attacks without a B hit, and DNA stays0. Next: audit the original ability-to-hit eligibility branch, then the native kill-to-DNA context. SDK funcB8h is an unverified research lead, not permission to call an invented binding. Active cancellation branches, nonzero costs, independent inventory/species progression and server-avatar/remote-area behavior remain explicitly unqualified. M04–M19 dependent gameplay remains blocked, with original scope intact. There is no user-permission blocker.
