# M00/M01 implementation session — 2026-09-08

## Outcome and evidence boundary

**M00 VERIFIED (project contract); M01 IN_PROGRESS.** The full global five-stage persistent-universe objective is unchanged. No native game process was launched, no gameplay adapter/network session was implemented, and no native callback/load/restore test is claimed. The experiment is explicitly partial/experimental.

Initial folder was empty and not a Git repository. No parent AGENTS.md was found. Initialized `main` locally without committing or publishing; the normal index remains untouched. The full 514-line brief was read and copied byte-for-byte. All 21 milestone work/deliverable/dependency/acceptance sections remain verbatim in MILESTONES.md. A 75-row feature-family matrix retains all five stages and cross-cutting requirements.

Tested source tree: **`c918d7e09beefb92f275a4eed51c6e9eed56eb17`**. It was recorded with a temporary Git index and exported without external dependencies, build caches, local profiles or evidence. The export fetched SDK **`cbf9206b9a823f0911cd9be0217104a49d72380b`** and rebuilt. `source-and-artifacts.json` records all 33 source-file hashes and six built-artifact identities. Source qualification uses an immutable Git tree because this new repository has no commit yet. Evidence files were added afterwards and are excluded from that source tree.

## Changed modules

M00: AGENTS.md, GOAL.md, complete MILESTONES.md, STATUS.md, README, architecture, authority, protocol, compatibility, policy, native baseline, research, recovery, sources and testing documents; feature coverage matrix; source brief preservation and Git attributes/ignore rules.

M01: pinned dependency lock; PowerShell SDK/bridge build and clean-export build harness; CMake Win32 targets; real ModAPI callback registration; Windows CNG executable hashing and lifecycle diagnostic session; Python PE/content validation, prerequisite gate, disposable directory manager, quiescent file backup and shell-folder probe; C++/Python tests; environment collection and native acceptance procedure.

The bridge links the import library generated by compiling actual SDK source. It does not invent exports, attach gameplay detours or implement native formulas. The upstream SDK core itself does install hooks, which is why the external pre-loader guard is required. The current prerequisite command is read-only and cannot launch a game yet.

## Environment and candidate

Full current inventory: `environment.json`. One local Windows testing machine inspected; no second game-testing machine or qualified disposable OS environment was established.

- Windows 11 Pro 10.0.26200 x64; Ryzen 7 9700X (8 cores / 16 logical); 33,407,430,656 bytes RAM; NVIDIA RTX 4080 SUPER (plus integrated/virtual display adapters recorded in inventory).
- Visual Studio 2022 Build Tools 17.14.37216.2; MSBuild 17.14.40; tool directory 14.44.35207, compiler 19.44.35226.0; Windows SDK 10.0.26100.0; Win32/v143; /MD Release and /MDd Debug. Actual pinned SDK projects use v143 despite older docs describing v142.
- CMake 4.3.2; Git 2.54.0.windows.1; CLI Python 3.11.15; CMake-selected test Python 3.14.3.
- Installed candidate: `C:\Games\SPORE\SporebinEP1\SporeApp.exe`, 3.1.0.29, PE32 x86, 24,895,536 bytes, SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`.
- Local SDK runtime build identifier is 2.5.0 (`SDK_BUILD_VER=0`), deliberately identified by commit rather than borrowing a published build number.
- Launcher source inspected at `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. No configured installation was found in the targeted standard-path/registry/Launcher-AppData searches. No installer executed.

## Exact build/test commands and observed results

Commands below were executed from `C:\Users\Developer\Documents\ChatGPT\SporeMP`. Full expanded compiler/MSBuild/CMake commands appear in build logs. File outputs were created with new paths. Native tests are not included in the pass counts.

| Command | Expected | Observed | Evidence/class |
|---|---|---|---|
| `pwsh -NoProfile -File tools/build/build.ps1 -Configuration Release -SdkOnly` | Actual SDK core/import library builds | Exit 0 | SDK MSBuild output and `build/sdk/Release`; BUILD |
| `pwsh -NoProfile -File tools/build/build.ps1 -Configuration Release` | Win32 core, bridge and host-test executable | Exit 0 after CMake configuration fix | `build-release.log`; BUILD |
| `ctest --test-dir build/win32 -C Release --output-on-failure -V` | Host + fixture tests pass | Exit 0; 2/2 CTest targets, 16 Python tests, 11 C++ assertions | `tests-release.log`; HOST/FIXTURE |
| `pwsh -NoProfile -File tools/build/build.ps1 -Configuration Debug` | Separate Debug SDK and bridge, matching /MDd | Exit 0 | `build-debug.log`; BUILD |
| `ctest --test-dir build/win32 -C Debug --output-on-failure -V` | Same host/fixture checks in Debug | Exit 0; 2/2 targets, no skipped Python tests | `tests-debug.log`; HOST/FIXTURE |
| `pwsh -NoProfile -File tools/build/test-clean-build.ps1 -Destination local/clean-room-m01` | Independent pinned fetch/build/test | Exit 0 | `clean-build-driver.log`; BUILD/HOST/FIXTURE |
| `pwsh -NoProfile -File tools/build/test-clean-build.ps1 -Destination local/clean-room-m01-final` | Repeat after export attribute/evidence-exclusion changes | Exit 0; 2/2 targets, 16 Python tests and 11 C++ assertions pass | `clean-build-final-driver.log`, `clean-build-result.json`, `clean-tests.log`; BUILD/HOST/FIXTURE |
| `python tools/diagnostics/sporemp_diag.py inventory --game-root 'C:\Games\SPORE' --output config/compatibility.candidate.json` | Read original file identity/content | Exit 0; candidate captured, native status NOT_RUN | `../../config/compatibility.candidate.json`; HOST |
| `python tools/diagnostics/sporemp_diag.py inventory --game-root 'C:\Games\SPORE' --output build/compatibility-with-directories.json` | Add directory structure to inventory | Exit 0; copied to final candidate | Same candidate; HOST |
| `python tools/diagnostics/sporemp_diag.py validate --game-root 'C:\Games\SPORE' --output evidence/2026-09-08-m00-m01/candidate-validation.json` | Candidate equality, no launch grant | Exit 0; `candidate_match=true`, `launch_allowed=false` | `candidate-validation.json`; HOST |
| `python tools/diagnostics/sporemp_diag.py preflight --game-root 'C:\Games\SPORE' --profile local/profiles/m01-worker-a/profile.json --output evidence/2026-09-08-m00-m01/native-preflight.json` | Block missing native prerequisites | Exit 22; NOT_RUN; zero launched processes | `native-preflight.json`; HOST, native NOT RUN |
| `python tools/diagnostics/sporemp_diag.py preflight --game-root 'C:\Games\SPORE' --candidate local/deliberately-mismatched-candidate.json --profile local/profiles/m01-worker-a/profile.json --output evidence/2026-09-08-m00-m01/mismatch-preflight.json` | Reject deliberately wrong executable SHA | Exit 20; fingerprint mismatch; zero launches | `mismatch-preflight.json`; HOST with altered manifest fixture |
| `pwsh -NoProfile -File tools/diagnostics/collect-environment.ps1 -OutputPath evidence/2026-09-08-m00-m01/environment.json` | Record actual environment without native execution | Exit 0 | `environment.json`; HOST |

To generate the deliberate mismatch fixture, the executed Python snippet loaded `config/compatibility.candidate.json`, assigned `m['executable']['sha256']='0'*64`, and wrote `local/deliberately-mismatched-candidate.json`. It did not change the game executable.

PE/import inspection command (actual executable path):

```powershell
& 'C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x86\dumpbin.exe' /headers /dependents /imports build/win32/Release/SporeMP.Bridge.dll > evidence/2026-09-08-m00-m01/bridge-pe-imports.txt
```

Observed exit 0, x86 DLL/PE32 and actual `SporeModAPI.dll` imports for AddPostInitFunction, AddDisposeFunction, GetGameType and version methods, plus bcrypt and dynamic CRT. These are imports, not evidence they ran.

Main Release bridge SHA-256: `ae36c9f59e2960938b944028e0790b7c5ab8d94994229044ba210d2ce2317af5`. Fresh-export Release bridge SHA-256: `9d5cc169a72aaab65e615f3109186869f5e54aa5c788a4025ad8ac7650fec1dd`. Rebuilds include linker/PDB/path-dependent data; byte-identical reproducibility is not claimed.

## Personal backups and disposable workspace results

Executed separately for `m01-client-a`, `m01-client-b` and `m01-worker-a`:

```powershell
python tools/diagnostics/sporemp_diag.py new-profile --root local/profiles --name m01-client-a
python tools/diagnostics/sporemp_diag.py new-profile --root local/profiles --name m01-client-b
python tools/diagnostics/sporemp_diag.py new-profile --root local/profiles --name m01-worker-a
python tools/diagnostics/sporemp_diag.py backup --source 'C:\Users\Developer\AppData\Roaming\Spore' --source 'C:\Users\Developer\Documents\My Spore Creations' --destination local/backups/m01-before-native
python tools/diagnostics/sporemp_diag.py verify-backup --destination local/backups/m01-before-native
python tools/diagnostics/sporemp_diag.py probe-isolation --profile local/profiles/m01-worker-a/profile.json --output evidence/2026-09-08-m00-m01/isolation-probe.json
```

All profile creation and backup/verification commands exited 0. Saves: 26 files / 23,777,939 bytes; creations: 3 files / 108,163 bytes. `backup-verification.json` confirms file integrity only; native restore remains NOT RUN. A final call to the same `tree_manifest` function compared each personal source against its committed before-backup snapshot and found equality, recorded in `personal-files-after.json`. No original file content or directory set changed. Backups live under ignored `local/backups`; game data is not included in versioned evidence.

Isolation probe exited **21 as expected** after error-reporting refinement. APPDATA/LOCALAPPDATA overrides alone left shell-folder paths unchanged under the personal profile. Adding USERPROFILE produced AppData/LocalAppData `HRESULT -2147024894 (0x80070002)` and changed Documents. Details, child argument vectors and environment overrides are preserved in `isolation-probe.json`. This is a Windows shell API reproduction, not native SPORE path tracing.

## Earlier failures and fixes (observed, not reconstructed runtime logs)

1. First full CMake generation exited 1: `IMPORTED_IMPLIB not set for imported target "modapi" configuration "MinSizeRel"` and the same for `RelWithDebInfo`. Fixed by generating only Debug and Release, corresponding to the built SDK configurations. Subsequent builds and fresh exports passed.
2. First isolation probe exited 2 because a child error was raised before its detail was saved. The direct child reproduction reported `SHGetFolderPathW failed for appdata: HRESULT -2147024894`. Revised the probe to preserve each folder error and run both environment variants. The final report is an expected unverified-isolation result, not an invented successful profile test.
3. The SDK linker prints an informational `/LTCG specified but no code generation required` message. Build exits are 0; no source modifications were made to suppress it.
4. First Git source export reported automatic line-ending conversions. Added repository attributes with explicit binary preservation for the brief and excluded evidence from subsequent source snapshots. Byte equality between Downloads, working copy and final clean-export brief was checked. This did not establish native compatibility.

## Native tests NOT RUN and next step

NOT RUN: loading either diagnostic bridge artifact into original SPORE; SDK callback execution; actual initialize/dispose JSONL creation; three clean game starts/exits; native file/registry write confinement; native save reload; two original worker isolation; gameplay in any stage; network/Internet testing; benchmarks.

Exact missing capability: a qualified loader plus a demonstrated isolated native save/configuration/creation environment. A source-compatible installed game is present, so this is not a claim that the machine lacks SPORE. The generic environment-variable approach is insufficient; the mandatory game-file access trace has not happened.

Next smallest implementation: provision a disposable Windows user/VM or verify an early native path adapter, pin the launcher/injector/legacy/core files, and implement the guarded launch path. Trace confined native startup, then normal callback disposal. Repeat three times, compare file-access/before-after manifests and test mismatches before injection. The source-declared `cAppSystem::SetUserDirNames` / Resource path APIs are investigation candidates only. Complete that M01 gate before M02 native action tracing, while retaining every later required stage and global-universe milestone.
