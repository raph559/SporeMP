# M07 completion work in progress

This session continues the incomplete M07 encounter after explicit user instruction to finish it. M07 remains **IN_PROGRESS**. Raw captures, native traces, account inventories, invitations and exact local command output remain in ignored `local/m07-completion-2026-09-21/` and the corresponding private coordinator/worker runs. This reviewed report does not publish native game assets or personal account paths.

## Native attempts 07–09

Bridge **0.0.36** adds read-only herd diagnostics to the earlier missing-template refusal. It does not enable an inactive herd or change the factory selector. A separate qualified, bounded noun census identifies the exact herd and its profile candidate; profile readability alone is explicitly insufficient ownership evidence.

- **Native07/.36:** no combat request was admitted. The original authority entered a mating sequence and subsequently left the scene before the encounter command. No desktop click or key was issued. The cause of that original behavior is unresolved. All three games and workers closed with exit 0.
- **Native08/.36:** no combat request was submitted. Client A's supervisor rejected `Worker status publication failed`; this closed its owned job before the harness status request. The old error omits the Windows code, so this run does not prove which specific replacement error occurred. The authority and other client closed with exit 0; A has no successful native exit record.
- **Native09/.37:** both authenticated clients submitted an original `engage` intention against NPC79. A produced five native health decreases and B produced one, taking health 6 to 0. The retained analysis observes one original lethal reward. A's client successfully created four newly published animals through its existing living template. B refused the first new animal before calling the factory. This is another incomplete three-machine encounter, with useful native diagnostics rather than a passed M07 gate. All three games and workers closed with exit 0.

Native09's failing client has one exact, live manager-owned herd with native ID **1772**, species key `[108668588,731352134,1080189440]`, raw archetype **4130283391**, and generation **0**. Its enabled byte is **0**; the authority's corresponding herd has enabled byte **1**. Both have a present readable matching profile header. The client census inspected **8,031** nouns and completed below the 16,384 cap. This establishes an inactive matching herd candidate, not the profile's ownership or the safety of its factory use. Those require the additional current species-manager membership and archetype checks.

The separate 4,096-noun animal diagnostic remains truncated. Its zero template count must not be used as exhaustive absence evidence; the production selector's refusal and the complete herd census are distinct observations.

All three closed attempts preserve the **29 protected personal files** and all **six disposable Games/Creations trees**. The same pinned 16-file game fixture remains unchanged in each disposable profile. WGC recordings are process-specific; initial scene frames were inspected for all three native09 processes. This inspection does not qualify corpse presentation. Recorders completed with exit 0.

Capture setup failures are retained: the first native07 capture commands omitted the required concurrent-process flag and were refused. During native09, an incorrect current-state path produced no valid PID; malformed capture invocations failed before recording. Corrected commands used the process identities returned by each successful worker launch and `-AllowConcurrent`. No gameplay acceptance is inferred from those failed invocations or from a media container alone.

## Supervisor publication correction

Bridge/NativeHost **0.0.37** retains .36 gameplay and diagnostic behavior while adding a bounded supervisor retry for atomic status replacement. Actual Windows readers that omit `FILE_SHARE_DELETE` reproduce the prior immediate failure in a HOST fixture. The correction retries only sharing, lock and access-denied errors for at most 500 ms, preserves the previous complete status, and logs the exact Windows error on permanent failure. It does not run on the original engine thread.

HOST checks cover a reader released after 80 ms, a persistent lock with bounded failure and unchanged old bytes, and immediate publication after the lock is released. The original code fails the new regression; the corrected worker target passes **201 assertions**. The subsequent full Release build succeeds; the affected worker/network/native-actor ABI CTest selection passes **3/3** in **5.59 seconds**. These are HOST/BUILD results, separate from original-game acceptance. Native09 does not repeat the observed supervisor termination.

## Work remaining after native09 (historical)

The next bounded increment qualifies original animal creation from the existing inactive herd through the current native species-manager ownership and resolved archetype. A separate original model animation path is being qualified for corpse/revival presentation. Authenticated pickup, original NPC response to both owners, post-encounter reconnect, unauthorized-action rejection during continued gameplay, and a substantive packet-impaired repeat remain required. The full M00–M20 plan is unchanged.

The extended encounter harness can submit B's original intention first, then A's after a bounded lead. It never assigns the NPC's target or manufactures damage. HOST fixtures ensure that an unavailable NPC prevents A's later submission, that B's already submitted result is retained, and that uncertain requests are never retried. All **14** harness tests pass. This is not evidence that an original NPC has targeted B.

The independent packet impairment helper and its actual TCP/Schannel HOST evidence are documented in `docs/m07-packet-impairment.md`. A native impaired encounter has not yet been run.

## Native attempts 10–13

The reviewed [native10 report](native10-combat.md) records both owners damaging NPC79, the NPC's original response to B, one original B reward and matching corpse animation/readback on both clients. A real authenticated wrong-owner challenge is rejected; B subsequently completes an original jump and landing. The inherited 150-second manifest bound versus approximately 305-second trace duration remains an explicit evidence defect. Later runs use a separately recorded generation-bound watchdog.

[Native11/.39](native11-combat.md) exercises the previously missing inactive-herd factory fallback on client A. It then fails publication during the original interval between health reaching zero and the native dead flag becoming true. Pickup and reconnect are not attempted. [Native12/.40](native12-combat.md) correctly transports that intermediate state and later corpse to both clients, but its pickup attribution assumes a positive DNA award. The actual original callback supplies zero under the loaded game property; A still receives real nutrition from the corpse. An unrelated initialized NPC returned to SPORE's retained pool subsequently causes `herd_missing`. These failures are retained with their exact frozen sources and traces.

The `.41` increment preserves valid zero-DNA returns and verifies positive resource benefit separately through original food decrease and hunger/health increase. It adds exact source-tick food telemetry and native readbacks. The original pool-return hook retires the previous scene, queued-command and beneficiary incarnation, while the scene adapter recognizes only the pinned inactive retained-pool signature and fences the original E54 cycle counter. The Release build exits0 and all **10/10 HOST/FIXTURE CTest targets pass** in **13.37 seconds**: 2,662 network assertions and 163 Python tests. The new Win32 pool ABI fixture performs actual Detours attach/detach and 4,096 mixed call paths; this is calling-convention evidence, not SPORE behavior.

Native13/.41 passes the bounded contested-food check. Both original orders precede first feeding. Exactly one returned first-feed DNA call has amount0, while A receives 481 positive original nutrition callbacks and the losing owner receives none. Each client has a food/first-feed/beneficiary readback paired with an identical authoritative source tick, without rounding or freezing food. The authority continues through original pool returns and eventual consumed-corpse removal.

Native13 does not pass the complete fight or reconnect. B's original NPC AI resumes retaliation after a correctly executed Stop and kills the target before A arrives. Later, the native pool reuses noun79 with a new generation, different species/herd and original dead=true/health0/combatant state0. Both client A's spawn and fresh B's baseline reject the corpse-animation helper's stricter dead=>combatant state2 assumption. The original fields remain unchanged; the next correction qualifies animation selection independently of that combatant state.

The native13 watchdog requests shutdown of the exact current generations at 270 seconds, including the replacement B. All original games, hosts and recorders subsequently exit cleanly. A later root shutdown attempt finds those controls already closed; its actual errors remain in the private record and are not presented as successful requests. Closed personal verification reports **29 files unchanged**, no recognized running workers, and unchanged six disposable Games/Creations trees. The four exact-process recordings and inspected scene frames remain private. The replacement B footage shows its initial local world, consistent with the failed baseline; it is not successful Join footage.

The current smallest remaining work is the original corpse-presentation condition, A-first native combat staging, a verified post-result reconnect and the substantive configured packet-loss repeat. The full M00–M20 plan and original M07 acceptance remain intact.

## Native attempts 14–19 and the publication cadence correction

[Native14/.42](native14-lifecycle.md) qualifies the original corpse combination previously refused and projects original A death/revival on both clients. [Native15/.42](native15-combat.md) instead reaches A's death during combat; B's approach expires while the original death prompt pauses simulation. [Native16/.43](native16-capture.md) stops before action because its first live recording cannot yet be decoded. The recorder subsequently flushes packets and opens bounded one-second Matroska clusters, retaining the encoder settings and encoded video content. Native17/.44 obtains inspected first frames but stops before action at its private 60-second startup guard. Native18 extends that guard to 90 seconds while also requiring fresh original health/readiness, without changing native vitals.

[Native18/.44](native18-encounter.md) records the complete normal shared-damage, terminal reward, contested original feeding and result-preserving fresh B Join evidence. Its stricter staged target-switch result remains inconclusive. The original false pickup verdict and late-archive reconnect verdict are retained separately from the corrected, hash-bound evaluations. The paired target-choice design was declared in the native protocol before the impaired attempt: both owners must hit their one shared NPC in each trial, with original retaliation against A in the normal trial and B in the impaired trial. This does not claim a mid-fight switch or waive any other encounter gate.

The .44 role-aware supervisor reports replica readiness from fresh authenticated periodic app/projection status after native baseline application; authority readiness still requires native AI. Native18 directly records all three ready roles, including connected replicas whose deliberately suppressed AI has not advanced. This is separate from its passing BUILD/HOST evidence.

[Native19/.44](native19-startup.md) starts the actual packet-impaired process sequence but stops before gameplay. A slow initial scene capture exceeds the start-based 50 ms publication interval, allowing immediate subsequent equal-tick updates on three entities. The coordinator correctly refuses them. All native processes and the packet filter close cleanly; no native action is retried and protected saves remain unchanged. Zero actual sampled losses occur in this short failed attempt, so it supplies no loss acceptance.

Bridge **0.0.45** schedules the next scene capture 50 ms after successful capture/publication completion. It retains every actual per-entity source timestamp, receiver sequence/generation checks, native AI and original gameplay. A HOST regression reproduces the recorded slow first capture, confirms the completion delay and absence of a catch-up burst, retains duplicate-tick rejection, and accepts the next newer sample without rewriting its timestamp. `cmake --build build/win32 --config Release --parallel 4` exits **0**; `ctest --test-dir build/win32 -C Release --output-on-failure -V` exits **0**, **10/10**, **23.82 seconds**, including **2,670 network assertions**. Exact commands and complete output are retained privately as `build045` and `ctest045` records. Native validation of this increment remains required.

## Native20/21 stopping point after the user's usage concern

Native20/.45 retains stable native publication under the configured 25 ± 5 ms packet delay, 1% sampled loss and seed731. Both owners damage the same NPC: A once, B five times; original NPC retaliation damages B four times. B receives the single original7-DNA kill reward. This complements native18's original retaliation against A under the declared paired shared trials. The initial original reset jump and landing also complete.

Native20's contested feeding is real and qualifies against its original archived after-prefixes: both orders precede the one first-feed result, A receives positive nutrition, and both clients have exact source revisions. The stored live report initially loses B's pair because it reads the authority before the client's newer trace. The unchanged strict evaluator passes the preserved later archive, including original pool retirement and both removals; neither pickup is resubmitted. The separate diagnostic reevaluation consumes the remaining orchestration margin. Old B closes normally; fresh B starts at11:23:19.743UTC and remains in the menu when the unchanged watchdog requests shutdown at11:23:36.821UTC. G05 and later dependent challenges are not passed. The inspected initial/client corpse views and actual successful shared fight are retained separately from that failure.

The native20 filter captures66,079 packets, randomly drops665, reinjects65,414 and records50 matching dropped-data retransmissions. It reports no errors, no pending packets and balanced captured accounting; RTT is not inferred. All owned games/supervisors close, the original three recorders exit0, and the attempted fresh-B recorder is refused after shutdown. The filter wrapper exits0 and verifies its service is absent. All29 personal files and six disposable Games/Creations trees remain unchanged.

The Python-only correction reads pickup evidence in clientA/clientB/authority order. A failed final convergence recheck may continue only to the original observation deadline; exact revision matching, prefix integrity, connected-peer checks and no mutation retry remain mandatory. `python -m unittest discover -s tests/unit -p test_m07_harness.py` passes41 focused HOST tests, exit0,0.041s. Frozen harness SHA-256:`0b685856353f1c57cf64cad8780a7f8d6d4cfbc846965182c92e7ce6d39a782f`; tests:`fa8bd6fde4d9a0710bc98ef7c318e1981bb0f9971f9d4176aea2254f03c9cc35`. The production binaries remain the built .45 artifacts.

After the user explicitly objects to time and usage, extra audits stop and one final automatically sequenced attempt is authorized within the existing request. Native21's three initial original-game views are inspected, but its review marker arrives at source age87.385s, beyond the frozen80s admission bound for its initial reset jump. The pipeline refuses before any gameplay intention, closes all owned processes and does not launch another attempt. **Native22 is NOT RUN. M07 remains IN_PROGRESS.** The remaining acceptance work is impaired post-result reconnect, late-consumed-resource and wrong-owner challenges with valid continuation, and the final selected post-encounter reset.

Native21's closed pipeline report hash is `7e370edc7bc5da8975687773570231b4425bf0b539c1f935ebb5c82128e01ae3`; the closed archive report hash is `f64de1b2276e0a761bf2c60beba754445344c530138cce6cbf6a3da643ffe145`. All three game/supervisor pairs are absent, all three recorders exit0, all29 personal files and six disposable trees are unchanged. The packet wrapper exits0 and verifies its owned service removed; counts are9,227 captured/105 dropped/9,122 reinjected,11 matching dropped-data retransmissions, zero errors/pending packets. These are startup/cleanup observations, not gameplay acceptance. Raw traces, generation identities, commands, captures and packet logs remain private. No further test, audit or native retry follows this stopping point.

## Explicit resume after the stopping point

The user subsequently instructed **"finish that"**, reopening the remaining M07 work. The preceding NOT RUN and stopped statements describe the earlier stopping point. Native22 and native23 then stop before any gameplay intention because initial visual review does not reach the startup gate in time. Both retain the actual initial frames and failure reports; all original processes and recorders close, packet filters are removed, and the 29 personal files and six disposable save trees remain unchanged. Neither attempt supplies combat, pickup or reconnect acceptance.

Before native23, the private orchestration is revised to prepare a single three-window contact sheet before review, allow setup/review to 120 seconds, and admit the initial reset jump before source age110 seconds. Combat still requires fresh unchanged actor identities, B alive and A health at least4; these are read-only admission conditions, not native health changes. The overall390-second shutdown and420-second bound remain unchanged. The 27 impaired-setup HOST cases and five reset-helper cases pass. Native23 still receives no review marker by its deadline because of the cross-agent handoff, so the next attempt assigns frame inspection, its marker and necessary original dialog handling to the same agent that operates the native sequence. This changes orchestration ownership, not original gameplay, frozen outcome checks or production binaries.

Native24 is launched with the existing .45 artifacts and frozen complete sequence. A subsequent native25 reset is conditional on completed encounter/reconnect/challenge evidence, verified native24 closure and at least180 seconds of active packet-filter lifetime. Final native results and acceptance are recorded separately after execution; launching this sequence does not grant any gate.

## Native24–26: retained orchestration failures

Native24 passes the initial inspected views, original reset jump/landing, shared combat with original retaliation against B and the strict live contested-pickup evaluator. The operator observes and confirms the actual original victory dialog through its current window. Reconnect then fails in the private driver: the worker start returns new process metadata before the first worker-status file is published, and an immediate read raises FileNotFoundError. No start or gameplay request is retried. The owned new B is closed once its control pipe becomes available; all games/supervisors close, the29 personal files and six save trees remain unchanged, and filter cleanup is verified. G05 is not passed.

The private rejoin correction waits at most12seconds within the existing overall deadline for a fresh status matching the returned generation/supervisor and a positive game PID. Exit/failure/mismatch remain fatal; the start request is never resubmitted. Exact-process recording starts after authenticated readiness. Six focused HOST fixtures pass. Rejoin helper SHA-256: `65a73b85db4ad5d979f649abff8bb54a77686e42253a698cf59423f561e10650`; test: `5e350b4582fe77349d1a1587c7a4864ef084a70c874dd6f7f3cf81f45ea5169e`; freeze: `6604587cb94ef5c9a2972a1bc2a01815c554de7b49b85894680a6c4f3033db0b`.

Native25 stops before scene readiness. The authority never records original intro-complete message0x066F8F25 (`network_startup_menu_ready`), so it queues no original load request and remains in menu epoch1 with no actors/AI. In native24 the corresponding menu-ready event is sequence40 at25.7166seconds, followed by load queued41/requested43 and complete98 at27.9677seconds. The source network peer starts only after local readiness; its absence therefore follows the missing local load, not a proven transport failure. The underlying cause of the absent intro completion is unproven because an early original-window view was not retained. No native action or initial-review marker is submitted. All owned processes close, protected files remain unchanged and the filter exits0 with384 captured/3 dropped/381 reinjected packets and no errors/pending packets.

Native26 inspects the authority early through its actual current window. The original intro is ordinary and requires no input; menu-ready40, load queued41/requested43 and complete99 occur normally. The private readiness loop then raises PermissionError reading a freshly replaced worker-status file. It stops before captures or gameplay. All owned processes close, protected files remain unchanged and filter cleanup is verified. Neither native25 nor native26 is relabeled as a visual-review timeout or native encounter failure.

The private metadata reader now retries only transient missing/sharing/access errors for at most250ms, using fresh bytes each time. Malformed JSON and identity failures remain fatal; it does not cache status, retry mutations or extend the original action/run deadline. The startup deadline is rechecked before capture admission. Normal/impaired/reset helpers share this reader and retain all generation/PID/freshness conditions. Five focused HOST checks pass in0.001seconds; the reset helper import/hash check passes. Reader SHA-256: `22a85ffeddf01fdcc8dc3ad826549601cf05c9d61a83979a0508627c4d58b573`; impaired helper: `87559bf30e7eed5b92736d0e63e61ca21044b5f4ae4ab1b7c697a5de9fa55b2e`; reset helper: `b3850ba437a092db36a5260aa6d378854e16e1aaeb38c5a318573929299fe3b9`. Production .45 code and its prior build/test results remain unchanged. Native27 is the next full sequence, with native28 reserved for the conditional final reset.

## Native27/28 completion and final decision

**M07 VERIFIED for the pinned Creature fixture.** The [acceptance decision](acceptance.md) evaluates every original requirement through G01–G09 using native18's normal encounter, native27's complete impaired encounter and native28's selected original reset. Native27 returns exit0, `QUALIFIED_ACTION_SEQUENCE_COMPLETE`, with no native mutation retry. A deals one actual hit and B five to their common NPC; the original NPC hits B four times and B receives one original7-DNA kill award. Both pickup orders precede first feeding, A alone gains positive native nutrition, and exact client revisions agree without a second reward. Fresh B advances baseline2→3 and passes all21 G05 checks with one controlled actor, DNA7 preserved and the consumed incarnation absent. An authorized late request is refused as stale, a real wrong-owner action is rejected without forwarding to the authority, and legitimate B completes an original jump and landing afterward.

Native28 starts only after the preserved native27 result and verified closure. Under the same still-live packet filter, all three original processes execute the declared fresh-session load and native baseline adoption. B moves **1.567994597 native coordinate units**, then completes original jump return and landing. Initial worlds, both native27 client corpse views, fresh B after Connected and native28's post-action world are inspected. The scene reset intentionally reloads the initial fixture; it is not restart durability or a rollback facility.

The final filter observes **34,954 captured /357 dropped /34,597 reinjected packets**, with32 matching retransmissions of dropped data. Actual capture-to-reinjection delay is20.1477–46.0254ms for the configured25 ±5ms/1% loss profile, seed731. There are no helper errors or pending packets. Owned service cleanup is verified after endpoints close. All seven endpoint generations have requested game/worker exit0, healthy contiguous trace stops and no remaining games. All seven recordings finalize with exit0. Personal29 files and all six fixture trees remain unchanged in each final run.

The reviewed [native27 scalar export](native27-summary.json) has SHA-256 `3fdc66db0fcea0f4716ab6baf8e0cf9c211a4549741cf39f7f294f42ffac4282`; [native28 reset export](native28-reset-summary.json) has SHA-256 `db966b36e55f1e0c63752c505db890c87e2aff1acde264a80cbdc5a76c41db60`. They retain the separate original source, payload, content, fixture, trace, inspected-frame, recording, packet and closed-report hashes, action/event sequences, commands' outcomes and cleanup. Exact original command arguments and complete raw evidence remain privately hash-bound. Earlier failed attempts and original false diagnostic reports remain unchanged. No further native run or broad test replay is required. M08 is next; the full campaign and remaining M08–M20 acceptance plan remain intact. No publication or history transfer is performed by this completion.
