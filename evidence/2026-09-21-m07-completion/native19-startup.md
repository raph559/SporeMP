# Native19: duplicate source ticks during impaired startup

Evidence: **NATIVE startup / NETWORK-REAL**, bridge and NativeHost **0.0.44**. Outcome: **failed before gameplay; M07 NOT VERIFIED**. The private source manifest is `ef595e7c7abab31390365ac90dbfe86b1f78e98b1f83a54914199feff2eddfd8`; the closed failure summary is `26cd439357024f3535f269a71724438bbd0df8ab269a37621f52ae47fce4a182`. Raw traces, exact commands, process identities and packet metadata remain in the private archive.

The predeclared original-game repeat used three isolated profiles, replicas started before authority, and the signed pinned packet filter on dedicated loopback TCP port 27160. The profile was 25 ms delay with independent uniform ±5 ms jitter, 1% sampled packet loss and seed 731, bounded to 600 seconds. Native shutdown was bounded separately to a request after 390 seconds and a 420-second target. No HOST forced-drop option was enabled.

Both clients briefly completed native baselines 1 and 2. The authority then received `stale_epoch_baseline_or_sequence` and quarantined. Its initial capture assigned late entity ticks of **77931500**; the immediately following capture published those same ticks for entities `12884909313`, `12884910407` and `12884911489`. The ordered TCP stream itself was not replayed. The coordinator's strict per-entity increasing-tick check refused the duplicate source revisions. The first capture began at **77931437** and lasted at least 63 ms, exceeding the publisher's deadline computed as capture-start + 50 ms. That expired deadline admitted another capture immediately. This is a publication scheduling defect, not evidence that packet loss corrupted TLS.

The expected result was three stable Connected endpoints followed by inspected captures and the original reset jump. Instead the kickoff detected the authority error, submitted no gameplay intention and closed the exact three generations. Combat, pickup, reconnect, unauthorized action and post-encounter reset are **NOT RUN** in this attempt. No capture had started and no mutation was retried.

The short filter run captured and reinjected **111 packets**, with **zero actual sampled drops**, zero errors, zero pending packets and balanced captured-packet accounting. Measured reinjection delays were **21.2621–38.5836 ms**; no RTT is inferred. This short failed startup does not qualify M07's loss gate. The filter report hash is `401898f3b8437ef060ce981ed5e73b5e12453b033d689bfeb72999aa1ee90f0a`; setup/cleanup hash is `62093c35a6bc1310ff69a221e3fdacd53ea213a91f40db86f666d7b541aac73b`.

All original game and supervisor exits are 0. The wrapper exits 0 and verifies its owned service is absent; no WinDivert service remains. All 29 protected personal files and six disposable Games/Creations trees are unchanged. The next bounded change schedules capture after the previous successful publication completes, preserves actual native source timestamps, and retains strict receiver rejection. A HOST regression and a newly frozen native repeat are required before acceptance.
