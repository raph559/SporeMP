# Native16: live capture read failure before combat

**NATIVE setup + HOST media investigation. M07 remains IN_PROGRESS.** Bridge **0.0.43**, wire schema **4**, started the two original replica processes before the original authority. The first live video read failed with `EBML header parsing failed`, before the frame-review marker or any combat intention. The kickoff stopped dependent work and requested clean shutdown. This run adds no encounter, pickup, reconnect, death/respawn or impairment acceptance.

The frozen .43 build exited **0**. The full `ctest --test-dir build/win32 -C Release --output-on-failure -V` run passed **10/10**, exit **0**, in **21.34 seconds**; those checks are HOST/FIXTURE, not original-game acceptance. The [reviewed summary](native16-summary.json) records the pinned executable/SDK, original payload and trace hashes, exact process exits, and capture evidence. Original raw logs, recordings, commands and profile inventories remain private. Hashes labeled original refer to those retained bytes.

The explicit kickoff was `python local/m07-completion-2026-09-21/kickoff-native.py --run native16 --version 0.0.43`. Client A started at **10:35:50.282 UTC**, client B followed, and authority started at **10:35:54.174 UTC**. Each exact-process recorder started after its endpoint reported Connected. Recorder progress beyond 1.5 seconds did not establish that the Matroska writer had flushed readable media. Reading the authority clip at offset one second returned Win32 process exit **3199971767** with the EBML error. No inspection success was inferred from progress, dimensions or the media container.

The final authority and both client traces contain **zero** `network_intention` and **zero** `command_begin` events. All three traces are contiguous and end in healthy `trace_stop`; they contain no `network_error` or `trace_limit`. Authority, client A and client B have **1,760 / 855 / 794** events respectively. All original games and workers exit **0**, requested worker shutdown is recorded, and all three recorders complete **0** without wall timeout. The final game/worker exit is **10:36:34.912 UTC**. The earliest-worker watchdog observes completion before its 390-second shutdown threshold. **All 29 protected personal files and all six disposable fixture trees remain unchanged.**

The capture failure was reproduced separately with a bounded six-second **HOST synthetic media** test. At 2.3 seconds, default Matroska output still exposed **0 bytes**, and the one-second frame read failed. Adding only `-flush_packets 1 -cluster_time_limit 1000` exposed **4,478 bytes**, allowed the same frame read to exit **0**, and produced a PNG. Both writers exit **0**. Copying the final H.264 streams from the two containers produces the identical SHA-256 `e1bd2a6bd3bef9b13ad8d81cfbb75b825d6157ec935748a9f15c7a100e9b1062`. This proves unchanged encoded video for this HOST comparison; the two options alter container flushing and cluster boundaries. The isolated `-flush_packets 1` experiment exposed a header but still no decodable cluster, so both options are retained.

The reviewed recorder change adds those two output options before `-n`, preserving the existing capture filter, codec, preset, CRF and frame-rate policy. The private kickoff also requires a flushed EBML header and a complete PNG, permits at most four read-only extraction attempts within eight seconds, and retains every failed read. Intention mutations are never retried. Fifteen HOST/parser checks and a decode of the closed native16 clip pass. The .43 recorder source is preserved before this change. The next original-game run must still prove prompt actual frame inspection with the changed recorder; this report does not convert the HOST result into native acceptance.
