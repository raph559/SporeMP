# M04 checkpoint implementation and remaining native acceptance — 2026-09-13

Request: **"lets finish m04"**. Result: **M04 IN_PROGRESS**. Bridge **0.0.14** and launcher **0.1.6** are built. Guarded original load/save candidates, closed-checkpoint metadata validation and adoption of matching existing A/B nouns are implemented; their original-game execution is **NOT RUN**. No new SPORE process, visible launcher, desktop input or save mutation was performed in this increment. The request for current-desktop availability remains unanswered. Prior native evidence remains under `../2026-09-13-m04-native` and is not attributed to these new binaries.

## Implemented behavior and evidence boundaries

- Native persistence uses original full-save `0xB29600` and the original saved-game UI's message/reset/filename-loader sequence ending at `0xB28020`. It admits only the controlled `Satiria.spo` fixture, guarded executable/manager identities, correct mode/epoch and engine thread. It never constructs the undersized SDK load-parameter struct. Fixed scalar IPC requests are acknowledged before work advances. A native true save return is separate from durability. Load completion requires a new Creature epoch, matching native home name and later original AI progress; completed-load admission expires when the scene changes.
- Immediately before native serialization, the actor snapshot requires exactly the idle A/B fixture, one original native player and no B campaign/reward context or pending bridge actions. Restore enumerates native nouns and verifies IDs, species/archetype, live herd identities/membership, uniqueness and A's original-avatar identity. It binds existing nouns only and creates zero nouns. Original mID and disabled-herd serialization are statically supported; actual survival through load/scene callbacks remains unobserved. Native OnLoad destroys non-primary cPlayer nouns, so full B campaign/reward persistence is not implied by this actor-ownership slice.
- `worker-checkpoint.py` seals only after all games are closed and the matching worker stopped cleanly. It preserves a quiescent native Games backup under ignored `local/worker-checkpoints`, then binds actor values to the final successful save request, source trace hash, PID/thread/epoch, backup manifest and native file hashes. Restore pins one new worker generation, verifies those artifacts, loads once and adopts once. A status acknowledgement alone cannot establish adoption: matching native restore trace and `created_nouns=0` are required. The action probe can refuse setup via `--require-existing-actors`. Unknown mutation outcomes are never retried.
- Supervisor shutdown now escalates when the native acknowledgement never arrives; the previous guard on `stopping_` could bypass both termination paths forever. A real child/private-pipe HOST fixture with an injected supervisor clock verifies owned termination at the existing 15-second deadline, exit 35 and `stopped_forced`.
- Launcher Stop pins the generation/supervisor it selected before querying status. Replacement of `current.json` cannot retarget the shutdown. A peer with malformed metadata gets an unavailable row without hiding a healthy worker or obscuring its acknowledged mutation. Release notes, CHANGELOG and project version match 0.1.6. No WPF layout change occurred. Prepared worker administration currently requires an elevated developer operator; normal Play keeps its existing account/saves and `asInvoker` behavior.

The sidecar itself identifies the source trace/save/backup, executable, SDK revision and bridge version. Separate content/injector/SDK-core/bridge DLL hashes remain in the guarded native-host run evidence; a version string is not a binary hash. No checkpoint sidecar or original native restore was created in this session. Synthetic fixture sidecars exist only inside the unit-test temporary directories.

## Commands and observed results

The evidence runner records literal argument arrays, UTC start/end, expected/observed process exit, and output in adjacent `*.command.json` / `*.log` files. It refuses overwriting evidence. Native commands are never hidden inside build/unit tests.

| Record | Exact program/arguments | Expected / observed |
|---|---|---|
| `build-01` | `cmake --build build/win32 --config Release --parallel 4` | 0 / 0; initial integration compiles. |
| `build-02` | Same build command after adding persistence ABI fixtures | 0 / 0. |
| `launcher-build-01` | `pwsh -NoProfile -File tools/build/build-launcher.ps1 -Configuration Release` | 0 / 0; launcher and launcher tests, zero warnings/errors. |
| `host-01` | `ctest --test-dir build/win32 -C Release -R worker_host\|native_actor_abi_host\|actor_commands_host\|launcher_host --output-on-failure -V` (regex is one literal argument) | 0 / 0; 4/4 targets in 2.54s. |
| `build-03` | Same native build after serialization-boundary snapshot and scene-fencing review fixes | 0 / 0; final bridge/host. Test binaries were unchanged. |
| `python-01` | `python -m unittest discover -s tests/unit -p test_*.py -v` | 0 / 0; **96 tests**, 0.705s. |
| `provenance-01` | `python evidence/2026-09-13-m04-completion/record-session.py` | 0 / 0; 131 source files, 11 artifacts and all 84 original milestone clauses preserved. |

The focused targets contain **184 worker**, **15 actor-command**, **34 ABI**, and **30 launcher** assertions. Eight new ABI checks exercise the exact save/load/reset/getter aliases with compiled HOST receivers, both bool paths, exact UTF-16 pointer/content, alternating receivers and 4,096 per-alias stack-balance repetitions. No original function is called by those fixtures. The Python suite includes **16 checkpoint** and **12 worker-manager** tests, covering tampering, duplicate/missing fingerprints, mixed thread/epoch/provenance, unchanged backup/native trees, generation replacement and unknown outcomes without mutation retries.

Earlier agent-local focused commands also exited 0: worker target build plus `worker_host` (184 assertions, 1.25s), worker Python tests (12, 0.035s), checkpoint Python tests (16, 0.209s). Parent `host-01` and `python-01` provide the retained final test logs. The three native review corrections changed bridge-only code; `build-03` compiles them. Their gameplay execution remains NOT RUN.

One initial attempt invoked the runner through `pwsh -File` with a PowerShell array expression for `-Arguments`; argument binding failed before the runner executed, exit 1, with "A positional parameter cannot be found that accepts argument 'build/win32'." Calling the script directly with `& ./evidence/.../run-command.ps1 ... -Arguments @(...)` corrected transport. This failed attempt has no standalone timestamp/log and ran no build. A documentation patch with a mismatched heading was rejected atomically and then reapplied with the observed heading. A few exploratory reads referenced missing guessed paths and returned no evidence. Static audit partial exports and missing optional disassembler-module attempts are recorded in their specific audit notes; none executed game code.

## Source, dependencies and environment

Existing work was already uncommitted at session start. It was preserved; no reset, native DLL installation, commit or push occurred. `provenance.json` records HEAD, all selected final source/document/config hashes, the ignored source archive, Release artifacts and test binaries, SDK/injector commits, current executable fingerprint and candidate-manifest hash. The full original M00–M20 dependency/work/deliverable/acceptance/critical-gate paragraphs are checked verbatim against HEAD. The complete campaign scope remains unchanged.

Pinned executable: GOG GA 3.1.0.29 Win32 original campaign, `C:/Games/SPORE/SporebinEP1/SporeApp.exe`, SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`. SDK `cbf9206b9a823f0911cd9be0217104a49d72380b`; injector `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. Existing pinned dependencies/toolchain were reused; nothing new was installed. This session hashes the executable and recorded candidate manifest, not a new complete native content/access qualification.

`environment.json` records current Windows 11 Pro 10.0.26200 x64, Ryzen 7 9700X (8 cores/16 logical), 33,407,430,656 bytes RAM, RTX 4080 SUPER driver 32.0.15.9649, AMD integrated graphics and Parsec virtual display. PowerShell 7.6.5. Final original-game process inventory is empty. This is an inventory, not a renderer/desktop-mode qualification. No fresh personal-save comparison was needed to claim a native run because no native run occurred; earlier 29-file preservation evidence remains attached to its earlier sessions.

Static sources, exact commands, ABI context and identities are in `native-persistence-audit.md`, `native-identity-retention-audit.md` and their provenance JSONs. Project restore/tool details are in `identity-restore-audit.md` and `checkpoint-tool-review.md`. Original-game decompilation/disassembly, source ZIP and all raw game/save assets remain ignored/local; versioned evidence contains project code, diagnostic metadata and hashes only.

## Remaining M04 acceptance and next action

1. When the current desktop is available, take fresh personal/worker backups and source/artifact checks. Start file tracing before the original processes, verify capture with the actual game PID, and launch one prepared worker through the actual WPF Settings controls under its supported developer token.
2. From its original galaxy menu issue fixed-name `load` once. Observe scene/AI completion and actual rendered original Creature state. Run the bounded A/B native actions, wait for landings, save once, observe the native return, then close normally and seal the closed checkpoint.
3. Start a fresh worker generation. Verify and load the exact checkpoint using `worker-checkpoint.py load-restore`, without setup. Confirm existing noun census/adoption, no duplicates, wrong-owner/stale/mismatched-sidecar refusals, and native actions using `test-worker-actions.py --require-existing-actors`.
4. Complete actual launcher closure and original player-process closure while worker identity/AI/actions continue. The earlier short-lived operator process test does not substitute for either. Networking remains later scope.
5. Exercise both workers through checkpoint saves while the file trace runs, then inspect resolved/unknown access correlations and exact personal/peer manifests. Retain prior current-desktop/unminimized requirements; lock/disconnect/service/RDP/VM and larger operating envelope remain NOT RUN. Keep native save return, closed file integrity and restored ownership as independent observations.

The minimal prior failure is unchanged: manual original reload returned the Creature scene with A/B bridge ownership zero; it did not census native B survival. The new code supplies a concrete next experiment, not proof of its outcome. Do not mark M04 VERIFIED or begin M05 as though this gate has passed.
