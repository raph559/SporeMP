# M05 completion — original Creature fixture, 2026-09-14

Status: **VERIFIED for the recorded living Creature fixture**, final bridge **0.0.29**. The [acceptance decision](acceptance.md) maps all four original M05 clauses to native evidence and retains unsupported abilities, full-scene adapters and the mandatory campaign work. The full M00–M20 work/acceptance plan is unchanged. M06 is TODO with its dependencies satisfied; no network session or player-facing join flow was implemented here. Launcher remains 0.1.8.

The user requested completion and had made the desktop available. Native input used fresh observed controls through Sky. Builds, HOST tests and analysis did not start gameplay. Existing dirty/untracked M02–M04 and launcher work was preserved. No personal game DLL installation or save edits occurred.

## Current identity and preserved files

[Environment](environment.json): Windows 11 Pro 10.0.26200, Ryzen 7 9700X, RTX 4080 SUPER driver 32.0.15.9649; other enumerated display adapters are retained. Native qualification remains the signed-in rendered desktop and unminimized original game. No headless, locked-session, RDP, service or general VM support is claimed. Build: MSVC 14.44.35207, Windows SDK 10.0.26100.0, Win32 Release.

All six original runs used measured disposable worker-02 sequentially. Sealed Games SHA-256 `f361377b1bc4fa4a41b3ab621e0fdea85ce787c48cea375afdfa563b2925a332` remains unchanged after every actual close. Each `personal-after-01` through `06` retains all **29** baseline files from `local/backups/automatic-5bc48ae4a6b147fe63b32945423523e7`. No two game processes shared a live profile.

Source freezes `source-025-0914.json` through `source-029-0914.json` each retain 145 source records and six payload identities. Current runtime snapshot: ignored `local/m05-presentation/source-029-0914`. Final NativeHost SHA-256 `f1dedfad505bdcc21be78786d7a536e604556a752dd6ec38f20c306fc07af6f1`; exact loaded identities remain in native-host traces. The final consistency check compares current runtime/tool/test/config source to the pre-launch freeze.

The real .18 authority source is reused unchanged from `../2026-09-13-m05-completion/source-reward-01.json`. Original native combat earns **8.75 DNA**, then the capture records living HP 10 / energy 1000 / hunger 36.6837425 / DNA 8.75. Its 4,108,815-byte native prefix has SHA-256 `fba33253080c613654dc5ba57c2ff408228d83e400d0f52718d1291e1b81d373`. Last-attacker diagnostic ID 0 does not prove A landed the killing blow. Same scalar capture ABI permits .16–.28 sources on the exact .29 receiver.

## Changes and evidence

The initial .25 charge/no-attack slices below were implemented. Charge has **zero actual native callbacks** because this fixture has bite, not charge/spit. Temporary charm/no-attack probes execute and restore during original Animal Update; records label them adversarial field fixtures. Native-02 separately proves a real bite remains active after arming and later becomes idle. Original target HP stays 6 and no original damage/strike/award body executes after application. Natural foreign Payer selection reaches the guarded social-result UI tail with unchanged award state. Introduction, mission description and creature card are inspected; the advertised 20-DNA mission reward is not an actual grant.

Ordinary saved-world Play exposed successive UI boundaries. `.26` B28020 filename denial happened after loading UI started; `.27` DEC139 countdown denial happened after controls were hidden; `.28` DF2391 guards an alternative scheduling route that this menu never called. Final `.29` **DF7A26 → DF7A8C** denies the actual saved-world Play event before UI hiding/controller scheduling. Play leaves the panel usable and actual Cancel restores the full galaxy menu. These failures are retained, not relabeled as successful presentation.

Filename Load uses M04's qualified live-manager/wide-filename ABI. Two native challenges on .26–.29 retain unchanged mode, actors and lifecycle. A new real HOST Detours test forwards Authority's filename pointer and denies 4,096 Replica calls with balanced ESP. The Ghidra exporter adds clearly labelled ±512-byte windows of already decoded instructions when the partial database lacks a containing function; no containing function signature is invented. [Research provenance](research-provenance-02.json): eight audits, 19 exports, exact tool identities, indexes and partial data-xref exits. Raw game instructions stay ignored.

## Six closed original runs

| Run | Build / game PID | Generation | Healthy records | Matching A/DNA samples / seconds after application | Actual result |
|---|---|---|---:|---|---|
| native-01 | .25 / 19280 | f72e34de0f0148a7918ac84deb27fa34 | 14,849 | 848 / 213.3039775 | Two challenges pass; startup splash/ESRB overlay persists over actual 3D. Presentation FAIL. |
| native-02 | .25 / 36040 | ef38be71820e47ba9b8d44896c6219ca | 7,269 | 787 / 197.0564326 | Two challenges; active bite/cleanup, foreign-species UI; post-arm duel unavailable. PASS for these cases. |
| native-03 | .26 / 13244 | c5d2d9eaa7f143c89c89c202d0d711c1 | 1,633 | 160 / 39.8244542 | Two challenges; filename Load denied, loading UI stuck. Presentation FAIL. |
| native-04 | .27 / 25688 | 57191f4424974d72b9890981589aaf06 | 1,774 | 157 / 39.2248276 | Two challenges after load completion; countdown denied, controls hidden. Presentation FAIL. |
| native-05 | .28 / 39404 | 54e4e3bc0b284f11bc820367c64ad8bd | 1,666 | 144 / 36.0375678 | Two challenges; alternate menu hook not reached, controls hidden. Presentation FAIL. |
| native-06 | .29 / 19224 | f9d6ea343adb4fda9fbb81af3dbfa6b9 | 1,622 | 120 / 29.9643827 | Two challenges; scene exit; actual Play denied before UI hiding; Cancel works. PASS for these cases. |

All six game exits are **0**, with healthy detach, contiguous sequences, monotonic QPC, zero foreign callbacks and unchanged closed Games/personal hashes. Each has two passed temporary timer probes. [Closed analysis](native-summary-02.json) retains exact events and media hashes and does not itself infer milestone acceptance. Time in a menu is not Creature-state stability. Native-06 starts at 10:10:32.9711468 UTC and actually exits at 10:18:51.951 UTC.

Final Play: 10:13:53.137 UTC, native `menu_click` sequence 1579 / QPC 1928832784 / epoch 4. Cancel: 10:14:04.320 UTC. [UI review](ui-review.md) records every relevant action, bite timing, misleading earlier clicks and inspected coverage. Capture-06 completes at 10:14:31.311810 UTC, recorder exit 0. All owned recordings finish; the fresh process check finds no game, supervisor or owned recorder remaining.

## Commands, failures and tests

Each named command has paired `NAME.command.json` (exact argument array, UTC start/end, expected/observed exit) and `NAME.log`. Native arm/replay reports additionally preserve inner WorkerControl requests and expected rejection exits. Their profiles/generations/PIDs are historical, not reusable commands for a new live process. `final-consistency-02.json` indexes completed command records and hashes the final documents; the earlier consistency report predates the overview-table status update.

- Builds `build-01`, `build-02`, `build-026` through `build-029` exit 0; final Release build has zero warnings. Affected `ctest-01`, `ctest-026`, `ctest-027`, `ctest-029` pass. Current replica coverage is 313 policy, 25 Win32/Detours ABI and 9 interior-branch assertions; retained actor/award/persistence ABI is 34 checks.
- `ctest-final-028` passes full **8/8** HOST/FIXTURE targets in **6.05 s**. `python-01`, `python-026`, `python-027` each pass five focused tests. These commands execute no original gameplay.
- `native-start-01`, `launch-02` through `launch-06`, and load/adopt/replay/arm records establish explicit isolated native execution. Queued `accepted` is not load/action completion; original trace/load/restore events establish completion.
- `adopt-04` / `replay-04` were issued before native load completed and correctly refused, without application. `adopt-04-loaded` / `replay-04-loaded` later pass after actual readiness. An early archive-02 attempt refuses a running supervisor; `archive-02-closed` later succeeds. These failed command attempts remain preserved.
- Shutdown submission, actual exit, archive and personal-after records are separate. All six closed archives/hash checks pass.
- Capture records target exact PIDs/HWNDs and exit 0. `review-bite-02`, `review-social-02`, `review-menu-06` extract original contact sheets, all inspected. Recorder/extractor success alone never passes presentation.
- Final `analyze-closed-02`, `index-research-02`, `environment-01`, `verify-close-02` and `diff-check-02` preserve analysis, provenance, hardware, frozen-source/original-plan and whitespace results in their paired records. The repeated consistency/whitespace check follows only the overview-table metadata correction, not a runtime change.

Retained .18 original reward/Off, .21 long disconnect/fresh baseline and .24 **958.6087522 s** stability/Save/movement/selection evidence were reused without another unchanged soak. .23 Off starvation/death/respawn and descriptive D3D9/audio captures retain their original versions. Perceptual listening and general overhead are not claimed. Earlier population crash, trace-limit termination and Save bypass remain in their historical sessions.

## Next bounded work

Begin **M06**: real transport/session handshake, value-only object/motion schemas, scene/incarnation fences, reliable lifecycle, authenticated reconnect and launcher join flow. Before admitting death/pickup/charge/projectile or broader campaign behavior, implement and qualify its native adapter as specified in [acceptance.md](acceptance.md). No new unchanged M05 scalar/Save/selection soak is needed.

## Initial plan retained as historical context

The following was recorded before the .25–.29 work above; its open-status language is superseded by the qualified acceptance decision. It preserves the original narrow experiment and pinned source rather than rewriting prior expectations.

Pinned original: GOG GA 3.1.0.29 PE32, `C:\Games\SPORE\SporebinEP1\SporeApp.exe`, SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`. SDK `cbf9206b9a823f0911cd9be0217104a49d72380b`, injector `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. Current rendered desktop/disposable profile qualification remains required. No personal game installation or save edits are part of this increment.

## Implementation under test

Bridge .25 uses three interior cuts in the original C1E8B0 frame. `C1EBDB → C1EA58` skips the charge collision query before its allocation, hit-list writes, strike and target impulse. Earlier native self movement and later animation completion remain original. The original strike's return is ignored before the target impulse at C1EF47, making a terminal strike guard insufficient. `C1E8BA → C1E8DC` freezes no-attack timer decay; `C1F107 → C1F10F` freezes its reset while retaining the hit-event consumed byte, preventing repeated visual effects. Checked complete instruction prefixes and preserved machine state use the existing HOST-tested interior shim.

Static provenance: retained `local/m03-static/m05-mixed-presentation-01/C1E8B0.asm.txt`, hashed in the prior session's `research-provenance.json`. A source label or static branch is not a successful native test. The existing adversarial charm-timer probe also checks/restores a temporary no-attack timer during the same original Animal Update; it is not an actual ability.

The isolated developer bootstrap now admits the existing M03 `duel` IPC operation before its first baseline, allowing an actual native encounter/ability to be in flight when denial is armed. It uses the validated original creature/herd construction and native attack, not invented damage or animation. The fixture is never saved or published. Arming, disconnect and scene exit permanently close this gameplay admission. This is test preparation, not a player command or network feature.

Exact commands/exits are paired `*.command.json`/logs. No build or HOST test launches SPORE. Original M00–M20 work and acceptance clauses remain unchanged. Remaining M05 acceptance includes a natural ability/social-result check, other progression/consumption and replica lifecycle boundaries; no milestone completion is inferred from this patch alone.
