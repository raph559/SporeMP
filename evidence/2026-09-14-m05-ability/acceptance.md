# M05 acceptance decision — 2026-09-14

**VERIFIED for the recorded original-game living Creature fixture, bridge 0.0.29.** M05's four original acceptance clauses are covered below. This is the authority/replica foundation, using captured native outcomes over the existing private developer IPC. It does not qualify a network session, a complete scene replica, all Creature abilities or the five-stage campaign. The full M00–M20 work and acceptance clauses are unchanged.

## Original acceptance clauses

| Original clause | Native evidence and result | Qualification |
|---|---|---|
| Applying a worker result updates the client without triggering a second authoritative action or reward. | Retained .18 original combat produces DNA 0 → 8.75. Its hashed native capture is applied absolutely to existing A; .19/.21 fresh-baseline replay retains 8.75 without another award. Current .25–.29 runs each apply the same real source and pass both guarded native challenges. No original damage/strike/award body executes after application. | Living A health, energy, hunger and DNA only. This is not replay of the combat command or a full inventory/death snapshot. |
| Deliberately divergent local NPC decisions cannot permanently override the worker. | Actual live-receiver AI, targeting, ability, strike, damage, resource, timer, grant and lifecycle challenges leave the checked state unchanged. Retained .24 has 3,826 matching post-application vitals/DNA samples through 958.6087522 s. In current native-02, B's original bite is still active after arming, then cleans up; target NPC 3 stays at HP 6, while 787 A/DNA samples match through 197.0564326 s. The natural foreign-species social callback retains original UI outputs with the award state unchanged. | Tests cover the listed native paths and fields, not every possible native memory write. No claim of charge/spit/projectile parity or complete campaign suppression. |
| Disabling the multiplayer connection does not allow a client to upload locally invented progress later. | Immutable replica role rejects source publication. Duplicate update returns stale; post-disconnect update returns invalid; gameplay/load IPC remains unavailable after arming. Retained .21 challenge after 580.2236249 s disconnected and a later absolute fresh baseline do not create another reward. Actual .22–.24 menu Save and .29 saved-world Play are denied; scene exit invalidates the binding and never reopens bootstrap. Closed Games hashes match. | The connection here is the developer replica link. Internet authentication, hostile-process resistance and authenticated reconnect are M06/M18. A client is never accepted as an authority source. |
| Single-player runs with multiplayer disabled retain the observational baseline. | Retained .18 Off original combat earns its native reward with zero replica events. .23 Off original jump, starvation, death and nest respawn remain native. Normal Play clears the role; `initialize_native_replica` returns before allocating a policy or installing hooks when it is absent. The current changes do not alter that path. | Those native Off runs are tied to .18/.23, not relabeled as new .29 runs. Descriptive frame/audio capture exists; new charge/projectile comparison and perceptual listening acceptance are NOT RUN. |

Sources: [current six-run analysis](native-summary-02.json), [original reward and disconnected/fresh-baseline results](../2026-09-13-m05-completion/SESSION.md), [retained Save, movement, selection, Off and longer stability evidence](../2026-09-13-m05-presentation/SESSION.md), [current inspected UI](ui-review.md). Each original run remains associated with its actual frozen source and loaded payload hashes.

## Deliverables

- [Authority matrix](../../docs/authority-matrix.md): native producer, coordinator responsibility and replica policy by domain, with the bounded implemented rows separate from global design.
- [Native hook policies and binding provenance](../../docs/m05-mutation-audit.md): pinned executable/SDK, ABI, checked instruction prefixes, engine-thread/lifecycle rules and actual versus unexercised paths.
- [Replica lifecycle and application guard](../../docs/m05-replicas.md): immutable roles, existing-object adoption, source/entity/incarnation/baseline/sequence fences, tombstones, reentrancy quarantine, disconnect/scene invalidation and value-only application.
- Native mutation, path, projection and publication counters; HOST tests for the registry, real Win32 Detours signatures and preserved interior-branch machine state.

## Boundaries that remain mandatory

The earlier working protocol grouped broader capability expansion and optional listening feedback with M05's unfinished checks. This closeout evaluates the four unchanged original clauses in the same explicit Creature-fixture qualification used for M02–M04. It does **not** convert the unrun cases below into passes or make them supported. Their feature admission still requires native evidence.

| Unfinished capability | Current status | Required before |
|---|---|---|
| Charge collision/target impulse | Precise C1EBDB → C1EA58 cut implemented and machine-state shim HOST-tested; zero actual charge callbacks in this fixture. | Admission of charge-capable fixtures in M07/M11. |
| Spit/projectiles and other persistent effects | NOT RUN; C0FE30 factory callers are not proven safe to deny by returning null. No projectile suppression/parity claim. | Admission of these abilities in M07/M11. |
| Natural food consumption, contested pickups, complete inventory/progression/relationship state | Selected terminal grants and fields are challenged; full adapters and native encounter coverage remain incomplete. | Contested native encounter M07, full Creature M11 and corresponding M12–M15 domains. |
| Native replica death/respawn, noun reuse, positions and reliable spawn/despawn | Existing-noun adoption and actual scene-exit invalidation verified; dead-state application and full object/motion adapters absent. | M06 lifecycle transport and M07 shared encounter/death/reconnect acceptance. |
| Full ability audiovisual parity and perceptual listening | Recorded original movement, bite cleanup, target/social/menu UI; selected-process audio capture integrity only. Listening feedback NOT RUN. | Qualification of the corresponding playable encounter/stage; do not claim audio acceptance. |
| Replica health indicator and player-facing join flow | Authority AI-progress metric can report a deliberate replica stall; developer-only mode. | M06 launcher/session integration. |
| Global domain coverage, five original stages, transitions and persistent universe | Unimplemented. | Unchanged M08–M19 acceptance plan. |

M06 is now **TODO with its dependencies satisfied**. No network implementation was started as part of this closeout. M07–M19 remain blocked by their unfinished dependencies. None of these results establishes playable co-op.
