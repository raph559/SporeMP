# M03 native reward ownership continuation — 2026-09-12

Status: **native player lifetime VERIFIED in the bounded Creature fixture; full M03 BLOCKED on native stage/reward ownership.** This continues the sealed `2026-09-12-m03-decisions` increment. Its bridge 0.0.9 native reciprocal-combat result remains valid. No independent B reward has yet been demonstrated. Static exports are private ignored artifacts, not recovered original source or runtime evidence.

## Newly established static findings

- `D45740`, the Creature IGameMode Update, consumes strategy+0x48 for deferred native herd evolution. It compares cPlayer current goal progress with herd thresholds, excludes the current player species, selects native archetype generations and updates native herd/member species. It clears the flag only after collecting at least one qualifying herd. `D2E360` reads native player+0x10F0. Selected call returns in Update are RVAs945AA0 (player species) and945AA9 (goal progress).
- The previous session's display interpretation was incorrect: `D2E2E0` uses `add ecx,0x128` to select an embedded counter, not to add DNA to a scalar. `D2D560` rounds/clamps the display amount and appends eight-byte entries to its deque; `D2CA90` grows it with0x100-byte blocks. This correction supersedes the sealed narrative without rewriting the sealed evidence. No new display object or queue has been constructed.
- Player IMessageListener is a subobject at+0x34. Native vtable1472CA4 slot0/C79FB0 is a destructor thunk. Slot4/C77ED0 is HandleMessage. Its global Space actions and Creature item reload need ownership-aware lifetime handling for a second player.
- C7A0A0 is a native flag-map write, not a goal initializer. cPlayer+0x10FC is the Cell consequence trait; +0x10F0/+0x10F4 are current/total goal progress. No binding was installed from either discarded guess.

Commands and exit codes for selected exports are recorded in `player-callback-01`, `player-callback-02` and `display-counter-01` command JSON/log files. Each successful static invocation exits0 and verifies an export index independently of Ghidra's process status. No game was launched for these exports and no full test suite was repeated.

## Native player lifecycle implementation

Bridge0.0.10 implements a developer `sporemp_actors players` command, after `setup`, that creates a second native cPlayer through the verified noun factory and SetGameDataOwner lifecycle. It preserves the manager's campaign player/avatar pointers, retains one explicit native reference, checks the actual cast/noun/vtable identity and records the two players' native goal/trait/item state. `players off`, scene exit and disposal retire B through the native manager and verify listener cleanup. Pointer values are never serialized.

The native initializer's global registrations remain intact. The bridge routes resource/configuration notification01A0219E through B's original callback; the remaining registered campaign-global cheat/UI/Space callbacks belong to A. C77E30 inspection confirms a global cheat/achievement side effect, not an independently owned B gameplay action. This is the Creature fixture's explicit callback ownership policy. Reward routing is still disabled; this increment does not claim B earnings or a complete campaign context.

`player-build-01` exits1 on ambiguous cPlayer AddRef/Release inherited from both native interfaces. The correction explicitly dispatches through cGameData. `player-build-02` exits0. `player-abi-01` exits0:18 assertions, ten Detours signatures,1/1 focused CTest target in0.07s. `player-binding-01` exits0:five new code prefixes and six native virtual slots match the exact installed PE. These are build/host/static evidence, not native lifecycle acceptance.

The additional `player-lifetime-01` export exits0. Requested B20CD0 belongs to function B20CC0, the native synchronous destroy helper reached by B22560. It calls RemoveOwner, marks the noun destroyed and unlinks it before the manager releases its deferred references. The index records this actual entry; no entry at B20CD0 was bound. Closed fixture backup `local/m03-fixtures/0912-post-ownership01` is reused; the guarded launch independently backs and hashes personal sources again.

`player-01` (game15476, host13064, engine21780) rejected the new CreateInstance prefix before installing actor hooks. Read-only inspection found Windows ASLR changed only the absolute immediate in `MOV ECX` at RVA720BF5. The executable's relocation table identifies it as IMAGE_REL_BASED_HIGHLOW, targeting RVA127C988. The guard now compares the exact relocated address. This was a guard implementation error, not a different executable or failed gameplay factory. The game never loaded the Creature fixture and closed normally through Alt-F4; host/wrapper exit0 and personal hashes unchanged. Initial fullscreen restoration required a fresh window binding before its screenshot was available.

Bridge 0.0.11 contains that guard correction. `player-binding-02` and `player-build-03` exit 0. The two ABI signatures are unchanged from the passing focused host check, which was not repeated. `player-provenance-01/02` retain 28 source/artifact hashes for 0.0.10/0.0.11. Fresh `player-02` starts game 28892/host 21592/engine 32968, passes the new guards and registers all 19 hooks. Its completed native result follows.

## Completed native player-02

Expected: original factory and owner initialization create an independently retained B player without changing A's manager pointers, native state or item storage; explicit removal, recreation and shutdown clean up the native registrations/references. Observed: **both creation/removal generations pass**. Native noun IDs 7426/7427 receive unique game IDs 684709250/684709300. A remains native player 1 / unique game ID 684349579. Both new players have ten selection groups, distinct CRGItems storage and 312 item definitions. Native RemoveOwner executes once each, clears items/groups, and no listener requires extra cleanup. The two tested retirement reasons are `explicit_stop` and `bridge_dispose`; scene-exit retirement is NOT RUN.

The exact console sequence is `sporemp_actors setup`, `sporemp_actors players`, `sporemp_actors players off`, `sporemp_actors players`, then native Quit/Don't Save. There is no duel, engage, damage, reward or advancement command. A and B remain at native health 10. A's goal progress/social/combat values remain 0, goal total remains 1,000, and its unlocked-item list remains 53. Both B constructors have native goal total 1, zero goal/trait progress and zero unlocked items. The initializer has not provided B with a complete native stage context, and no values were copied or granted to make it appear to do so.

The complete `player-02/native/actors-28892.jsonl` contains 2,141 contiguous records on engine thread 32968, zero foreign callbacks and a healthy trace stop/detach status 0. Three live resource/configuration notifications execute B's original callback; two such notifications arriving during removal are explicitly rejected while native cleanup proceeds. The installed policy for other registered campaign messages was not exercised by a cheat or Space action. Bridge initialize/dispose each occur once on the same thread. Game and guarded wrapper exit 0. Loaded game, bridge, SDK and injector hashes match. Personal postcheck retains all 29 files; the closed 27-file fixture snapshot `local/m03-fixtures/0912-post-player02` confirms unchanged Games and creation contents.

Native trace SHA-256: `ac6c3808d4abd51a18db34181c235f7c0b0290f752343107193f7a88e09e2556`. Loaded bridge 0.0.11 SHA-256: `c5b7ddb14b9b2e184a6b61fd3eecb0b93d5a9fb3ee7db5e63f66ecb44204ab2c`. Tested sources are preserved under ignored `local/m03-rewards-0912/source-0.0.11`; [provenance](provenance-0.0.11.json) records all 28 identities. No changes to the tested native source followed the run.

The actual WGC capture is 60 seconds, recorder exit 0 without timeout. Its first frame was extracted and inspected before commands. The nine reviewed samples at 0/8/16/24/32/40/48/56/59 seconds show the original scene, healthy A, B setup and the first native-player confirmation while world NPCs animate. The recording ends after first creation. Explicit retirement, recreation and quit are covered by the complete native trace and live Computer Use observations, not continuous video. Native frame timing was not measured. Raw recording and inspected contact sheet remain ignored; [visual review](visual-review.json) records their hashes and limits.

`player-trace-analysis-01`, `fixture-post-player02` and `capture-review-01` exit 0. The first lifecycle summary attempt, `player-summary-01`, exits 1 because it incorrectly treats every loaded Windows module as one of the four build artifacts. That reporting error changes no native evidence. The corrected summary checks the game's trace identity and the three loaded payload DLLs explicitly; other module hashes remain host inventory. `player-summary-02` exits 0 and writes [native-player-summary.json](native-player-summary.json). Documentation update `docs-01` exits 0. No full test suite or combat replay was added.

## Remaining native stage/reward dependency

`stage-initialize-01`, `stage-config-01` and `stage-items-01` are successful static exports (exit 0). Requested D43E76 is inside **D43DE0**, not a callable entry. Case0 computes the goal total from native floats at 1582E44/40/3C/38 and writes cPlayer+10F4. Calling the whole routine for B would also repeat input, display, time-of-day and world initialization. D42520 is display/input/config refresh; D2E980 resets the global 0x50-byte CreatureGameData and has no player receiver. Neither is installed as B initialization.

The existing native `GetEvoPointsToNextBrainLevel D2E380` supports an explicit level 0–3 and reads the same four native configuration floats; only its -1 path consults the avatar. Action 061B1320 reaches the collection handler D47FC0, which still fetches manager avatar/player despite receiving a payload. Its helper D3B640 filters mouth definitions by native consequence/diet and calls 596D70. Actual instructions at 596D70 set item status 4 (NotUnlockable) and remove the list entry; this is not a starting-parts grant. No reward or item adapter was installed from a decompiler guess.

Minimal remaining reproduction: `setup`, `duel 2` in the retained combat run produces a native B kill but no DNA callback. The new `setup`, `players` reproduction establishes native B lifetime while showing goal total 1 versus A's 1,000. The missing capability is owner-scoped native stage/reward processing through amount calculation, goal/combat traits, queued display and deferred world/species evolution. The next smallest step is to bind that state and the actual asynchronous damage beneficiary before one prepared reward encounter; another unchanged fight is unnecessary. Active Stop/scene cancellation, nonzero resource costs, other stages and server-only-avatar activation remain explicitly unqualified. The complete M00–M20 plan is unchanged.

## Configuration and identity

Pinned executable SHA-256: `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`; preferred base 0x400000. SDK commit: `cbf9206b9a823f0911cd9be0217104a49d72380b`; injector commit: `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. Windows 11 Pro 10.0.26200, Ryzen 7 9700X (8 cores/16 threads), RTX 4080 SUPER driver 32.0.15.9649; 32-bit original Creature campaign on the GOG GA 3.1.0.29 executable, 108-file pinned content/configuration inventory. Disposable account SID `S-1-5-21-1000000000-2000000000-3000000000-1007`. This is the previously measured developer isolation setup; normal player Play flow is unchanged. Commands, source/artifact hashes and per-run launch/completion records are retained in this bundle; sealing indexes them without reexecuting tests.
