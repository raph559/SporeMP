# M04 native save, recovery and launcher acceptance — 2026-09-13

Status: **VERIFIED for the recorded M04 configuration**. Original-game load/save, saved-actor adoption, operator/player lifetime, concurrent file-access isolation and actual launcher controls close the bounded worker milestone alongside the retained original crash-containment case. Qualification is the pinned Creature fixture, two registered OS accounts and the current rendered, unminimized Windows desktop. The root reviewed the assembled evidence and accepted this bounded result. This does not implement multiplayer networking or qualify unlimited worker capacity.

The user explicitly authorized desktop use while away from the PC. The root agent operated only selected original-game and launcher windows. Other agents inspected metadata and source files; they did not operate native processes. Native actions below came from IPC requests and original engine execution. Computer Use handled the launcher and original menus, including optional registration notices. No personal campaign scene was entered during the actual-player lifetime check.

## Original acceptance and current gates

The original M04 acceptance remains: “a worker runs the verified native action test without a human playing it; a player client can close without shutting down authority. Two worker processes operate on separate test workspaces without save/configuration cross-writes. Kill one and observe detection while the other remains healthy. Reload a controlled native checkpoint. State any required desktop or separate-user/VM setup honestly.” The critical gate remains: “do not plan unlimited workers around unverified launch or persistence isolation.” All original M00–M20 clauses must remain present in the final source freeze.

| Gate | Evidence in this session | Final result and remaining boundary |
|---|---|---|
| Worker initialization and scene load | Original 0.0.14 processes initialize the pinned bridge. IPC load moves epoch 1 to 3 and completes only after new native scene AI progress. | VERIFIED for the recorded current-desktop Creature fixture. Private desktop, service and other session arrangements are not newly qualified. |
| Unattended A/B action | Initial, restored, post-launcher-close, post-player-close and concurrent probes each submit wrong-owner/stale-epoch negative cases and distinct native jumps, matching returns to later landings. | VERIFIED for these recorded actions. The initial/recovered recordings have root visual review. No M03 combat/reward replay was required. |
| Controller/player lifetime | The recovered game continues after the actual launcher closes. A normal-account player is then launched through Play and closed; worker requests and native actions follow its recorded process exit. | VERIFIED for local launcher and actual-player process lifetime. Network-client disconnect/reconnect does not exist yet. |
| Two workers and concurrent save isolation | Separate registered OS profiles host games 31528 and 39716 concurrently. Both original engine threads write their own Games files with successful I/O overlap of 20.1228 ms, native save-call overlap of at least 414.7126 ms, no resolved peer/personal accesses and zero reported event loss. Both then exit cleanly and are sealed independently; closed personal files are unchanged. | VERIFIED for the recorded profile/save arrangement. Exact lifetime and path-alias audits, immutable content comparison and closed checkpoint crosscheck pass. Unknown ETW correlations and shared NVIDIA writes remain explicit. |
| Crash containment | Retained 0.0.13 deliberate crash: game 30140 terminated, supervisor 29916 exit 31/status crashed, while peer 21944 stayed healthy and completed actions. | VERIFIED for the retained original-process case. No new destructive replay in this session. |
| Native checkpoint and basic mod identities | Initial native save completes; a closed checkpoint is sealed and reloaded in a new generation. A/B native IDs, herds, species and archetype fingerprints match; restore records zero created nouns. New valid actions work and wrong owner/stale epoch are rejected. | VERIFIED for basic Creature actor adoption from the sealed checkpoint. Native mismatched-sidecar negative execution is NOT RUN; HOST validation is separate. Full player/campaign/reward persistence belongs to later milestones. |
| Shutdown | Initial and recovered 0.0.14 workers exit through the original game loop, including recovered game 38432 exit 0 at 01:20:14.321Z after WPF Stop request 17. Final 0.1.8 WPF Stop exits concurrent games 39716/31528 cleanly at 01:32:09.651Z / 01:33:24.019Z. | VERIFIED for these completed runs, with concurrent checkpoint sealing and correlation passed. |
| Desktop and resource requirements | Prior native observations establish unminimized current-desktop operation, unfocused actions, minimization stalls, recovery before timeout and modal AI pauses. This session uses that arrangement. | VERIFIED as an explicit operating requirement, not unattended service/headless support. Lock/disconnect/RDP/VM behavior, GPU utilization, native frame timing and broader capacity remain NOT RUN. |
| Actual launcher workflow | WPF Start launches an original worker; closing the launcher after Start leaves it running. Normal Play launches the actual player. A real Stop reaches the restored worker. Launcher 0.1.8 retains the open selection menu across polls for 15.661 seconds and both real concurrent Stops complete cleanly. | VERIFIED by `ui-visual-review.json` and correlated host/native logs. Start was exercised with 0.1.7; 0.1.8 selection/status/Stop and unchanged backend launch semantics are the qualified final increment. Active-Start closure remains HOST tested only. Prepared profiles require an elevated developer operator; no elevation prompt is added to normal Play. |

## Identity, source increments and starting state

The original executable is `C:/Games/SPORE/SporebinEP1/SporeApp.exe`, the recorded GOG GA 3.1.0.29 Win32 candidate, SHA-256 `dc04aee5a3debc3f1ad4c1a937460e99a29b9bd3bc285008be83615dd5e59a37`. The native campaign exercised is Creature. SDK commit is `cbf9206b9a823f0911cd9be0217104a49d72380b`; injector commit is `26adca9a2578b5bb32ba2eac90d96bd9ac7d48a9`. Bridge version is 0.0.14, SHA-256 `2554f3250f7985c661713b543b284ba24cc2d9265d8786ac80e4fe8a473e26f0`; SDK DLL SHA-256 `c018b51c56c537e739f24319dcfeb5cfeeefa97ded0ca91bf5729755c190ba11`; injector DLL SHA-256 `938cf3b106a459174ebfe31803b4d84238b07b004dc5c6e2da5fec61672eab55`.

Initial runs use NativeHost SHA-256 `cee20dd04f68833fb65a38f680296b18487b029c90c8baaff0860c0e8934a9e4`. The initial source/artifact identity is `../2026-09-13-m04-completion/provenance.json`. `launch-fix-provenance.json` records launcher 0.1.7 and the `start-worker.ps1` inherited-output fix, preserving all native sources/artifacts and all 84 original milestone clauses. Its source archive is `local/m04-acceptance/launch-fix-source-017.zip`, SHA-256 `b0b460844cf12445dbf993ddabed2acb5b19e5ec9041182abcc5509b7437c271`.

Normal Play caused the pinned SDK to create its documented `SporebinEP1/spore_log.txt`. The next worker failed strict inventory validation before a game was created. `post-player-content-inventory.json` identifies only that added 97-byte runtime log; all immutable content hashes remain unchanged. The guard now permits only that exact regular file after safe-path validation, while diagnostics fingerprints it separately under `runtime_artifacts`. Directories, reparse points, other locations and unknown siblings remain rejected. The concurrent runs use the rebuilt NativeHost SHA-256 `1559d4540bfdcbb02a40c07f50c048461881dc3e57dcfe6585ffca86d0e2a4a2`; bridge/SDK/injector are unchanged. Runtime-log build/test/validation command records are retained. `final-runtime-provenance.json` records the new host and 0.1.8 launcher artifacts with only the explicitly pinned native_host.cpp change among 39 native sources. Documentation is excluded from that runtime freeze; the separate complete source/archive snapshot is recorded in `final-provenance.json` after documentation settles, as described in the final acceptance section below. No older provenance is rewritten.

`preflight.json` confirms the initial completion identities and closed 27-file backups of both owned test profiles. Worker 01 SID ends 1008; worker 02 ends 1009. Backup manifests are `62f172af183986159040218ce4389efc86b0bdee5a34444d0873d41a0ceaa766` and `54595aac7fb551270100d8620b1dc95daed38b3d16c6dc3d4146eaca38536f76`. Worker paths and actual mutual write/delete denial probes are retained in the preceding M04 isolation evidence; environment-variable redirection is not the basis of isolation. Personal backups and all native save payloads remain local and ignored.

Fresh hardware/OS and closed-state evidence is `final-closed-state.json`, consistent with `../2026-09-13-m04-native/environment.json`: Windows 11 Pro 10.0.26200, Ryzen 7 9700X (8 cores/16 logical processors), RTX 4080 SUPER driver 32.0.15.9649, 33,407,430,656 bytes RAM. The final census has no relevant game, worker-host or launcher processes, and WPR reports that it is not recording. The qualified operating arrangement is the current signed-in rendered Windows desktop, worker windows unminimized, and separate owned standard-user profiles. This is not service, private-desktop or headless qualification.

## Original native runs

| Worker and generation | Game / supervisor | Native result |
|---|---|---|
| 01 / `bebef0e725be4e489beba9addc909248` | 30176 / 12036 | Initial IPC load, A/B jumps, original save and clean exit. `native-correlation-01.json` passes with 817 contiguous records, zero foreign callbacks and expected loaded payloads. |
| 01 / `aab39dc011774d5c9c01a7703fb8b04f` | 38432 / 6876 | Sealed checkpoint load, zero-created-noun A/B adoption, three action pairs spanning launcher/player closure; clean game exit 0. `native-correlation-02.json` passes with 4,673 contiguous records. |
| Actual player / `player-9020148160ee470b` | 38004 / 38660 | Normal-account Play, empty game arguments, no profile redirection, optional registration only, selected-window Alt+F4; game exit 0 at 01:17:43.199Z. |
| 01 / `9f0d24d7007246a2975e6e2090b1ec8f` | 31528 / 37744 | Restored native actors, action probe and concurrent save request 9; WPF Stop request 10, clean game exit 0 at 01:33:24.019Z; fresh sealed checkpoint. Full native correlation passed. |
| 02 / `c3e4ad8624fe4fe6ba0ba92b0236441b` | 39716 / 39640 | Native scene load, distinct A/B action probe and concurrent save request 7; WPF Stop request 8, clean game exit 0 at 01:32:09.651Z; fresh sealed checkpoint. Full native correlation passed. |

The initial load completes after 2,140 ms, epoch 1 to 3, with 29 new native AI entries. Save request 7 records both actor fingerprints immediately before the original save call; native return reports success after 531 ms in epoch 3. `checkpoint-01.json` binds the closed native backup and trace to A native ID 232/herd 208 and B native ID 7425/herd 7424, with exact species keys and archetype.

The recovered load completes after 2,110 ms in epoch 3 with 30 new native AI entries. `restore-01.json` matches the sealed checkpoint and records `adopted_existing_nouns`, actors 1/2 and `created_nouns: 0`. The new process does not use setup to manufacture replacement actors. The successful native save/reload is distinct from mod-identity adoption; both are required and recorded. This only restores the basic bounded actor identities exercised here, not all independent progression state.

## Launcher and actual-player independence

`actions-after-launcher-close-01.json` records fresh actions by the same recovered worker after the actual launcher window closes. This follows the 0.1.7 fix that redirects the detached supervisor's inherited output streams so Start can finish while authority continues. Closing an idle launcher after a completed Start is the exercised sequence.

The actual player's host records `--play`, account `Developer`, `profile_redirection: false`, and empty display/game arguments. Its original process starts at 01:16:45.632Z. The bridge initializes/disposes once on the same native thread; the host records clean game exit 0 at **01:17:43.199Z**. The root observed intro/registration and closed only the selected player window; it did not enter a personal campaign scene.

The post-player-close action script starts just before the process exit. Its wrong-owner request is also just before exit. The two valid jumps, however, are requested at **01:17:43.378Z** and **01:17:44.529Z**, respectively 179 ms and 1.330 s after the original player exits. They are accepted once each and have matching original returns/landings in the worker trace. Worker app/AI totals subsequently reach 22,251/649,454 before clean shutdown. `player-lifetime-after-close.json` preserves the ordering; no action was replayed to improve it.

The attempted live before-close snapshot began too late and failed after a bounded 30-second poll. No live player process token SID or exact OS command line was captured by that helper. Current account SID mapping is explicitly labeled as a mapping, and retained ETW original process lifetimes are used for identity/time correlation. A later after-close helper first failed because the root had already cleanly stopped the worker; the corrected report represents that later proven clean stop. Both failed command records remain.

## Concurrent save and personal-file attribution

`concurrent-save-01.json` records two verified live original processes under separate registered accounts. Save requests 9/7 both return native success in epoch 3. Relative native QPC origins are bounded by host QPC samples around each command/ack; the intersection of latest possible save entries and earliest possible returns proves **at least 414.7126 ms of overlap**. No point-estimate clock alignment is used. The copied trace snapshots are explicitly parsed/re-encoded complete records, with their representation and hashes recorded.

Before/after manifests show personal sources unchanged across the concurrent-save interval. Both live workers write their own native checkpoint state. `durability: NOT_VERIFIED_LIVE_FILES` remains the honest classification of the live probe; subsequent clean stops and fresh `checkpoint-concurrent-01.json` / `checkpoint-concurrent-02.json` seal closed backups separately. `native-correlation-concurrent-01.json` passes both full traces, both action reports, the recovered identity report and the two newly sealed checkpoints against final runtime provenance. The original checkpoint is not overwritten. Qualification uses the actual concurrent ETW operations, exact process lifetimes, OS denial probes, immutable content comparison and closed artifact checks together.

`concurrent-isolation.json` identifies **488 successful own-Games writes initiated by each original engine thread** within the concurrent-save command window. The worker-01 Games activity spans 01:28:16.7266980–01:28:17.2246816Z; worker 02 spans 01:28:16.7186341–01:28:17.2290633Z. These activity envelopes overlap by 497.9836 ms. More decisively, two successful `stars.db.tmp` Write request-to-OperationEnd intervals themselves overlap by **20.1228 ms** on the shared ETW FILETIME clock. This is actual concurrent original-game file I/O, distinct from the conservative native call-span measurement.

Exact kernel SporeApp Start/End intervals identify games 31528/39716 and parents 37744/39640. They exclude 764/8,039 events outside those lifetimes, including numeric PID reuse. Both complete lifetimes have no resolved peer/personal open mentions or requested/completed mutations. `concurrent-path-alias-audit.json` independently resolves actual Windows short names: `SPOREM~2` is worker 01 and `SPOREM~3` is worker 02. Long and short peer/personal names have no resolved open/requested/completed/failed paths. The raw worker-02 short-name write remains in the primary report and is its own profile, not a hidden cross-profile operation.

Trace extraction reports `process_trace_status: 0`, `events_lost: 0`; summaries retain no loss markers or uncompleted mutation requests. Unresolved file writes are **103/102**, registry SetInformation correlations **3,263/3,132**, and undecoded events **1,109/1,093**. Property decode errors are zero. Both games also mutate shared NVIDIA `nvAppTimestamps`/`CaptureCore.log` and their own project diagnostic logs; failed I/O remains visible. These observations do not claim that all global configuration is isolated or that all events were decoded. All **29 closed personal files** match the concurrent-phase baseline, and the candidate executable/content roots are unchanged. The observed profile/save isolation is accepted within these stated limits.

`concurrent-checkpoint-crosscheck.json` independently verifies each sidecar's request/epoch/actor fingerprints, the parsed live trace prefix against the later closed original trace, clean game/worker exit, and all 16 closed Games files against the preserved backup and current stopped profile. The Games digests are `dd23c69a1a35ad1c9a06f4d103014511f49f547fb17ce303dc405ffcd85d2940` for worker 01 and `f361377b1bc4fa4a41b3ab621e0fdea85ce787c48cea375afdfa563b2925a332` for worker 02. This is a read-only HOST file audit of native artifacts, not another native execution. These two exact new concurrent artifacts were not reloaded; earlier sealed-checkpoint recovery supplies the separate native reload evidence. Clean closure and hash integrity do not prove power-loss durability or full B player/reward persistence.

Before the final Stop of worker 02, the root observed its native AI counter plateau at 361,572 for about 47 seconds while app updates continued. The launcher correctly displayed `simulation_stalled`; its previous actual scene image showed starvation and 8 HP. The causal reason for the later native AI plateau is not established by those observations and must not be invented. WPF Stop still reached the original loop and the game exited cleanly. This bounded unexplained stall remains an operating limitation; these tests do not demonstrate unlimited autonomous campaign progress.

The original personal-before comparison passes after the initial worker phase. It fails after normal-account Play because ten personal files differ; the failure and original baseline are retained, without restoring personal data or substituting a new baseline. `personal-player-diff.json` identifies all ten changed paths, including native Games data as well as caches/preferences. `personal-player-attribution.json` then applies exact kernel Process Start/End intervals to avoid numeric PID reuse: all ten changed paths have observed successful normal-player mutations, seven have completed player writes and three have rename/delete evidence. Neither worker has resolved successful mutations or open mentions for those personal paths. Rename/delete evidence does not establish every replacement byte, and unresolved worker writes remain 102/83 with registry SetInformation counts 3,179/3,230. Unknown-schema events and failed I/O are retained. This is bounded writer attribution; it is not a claim of an unchanged personal tree after normal Play or complete decoding of all ETW activity.

## Visual review and its limits

`ui-visual-review.json` records the root's completed visual review of **12 hashed artifacts**, including both extracted contact sheets, the restored scene, actual-player registration screen, both simultaneous worker scenes, final 0.1.8 drop-down and both stopped worker states. The initial capture visibly shows the native menu, black load transition, loaded Creature scene and separate A/B jumps. The restored capture shows the two restored actors and jumps around video seconds 56–59. The root reported no frame substitution. This review is an agent observation of original captured pixels; the trace analyzer does not grant visual acceptance.

Only the foreground original-game screenshots listed in that report are accepted. Earlier occluded-window screenshots contained launcher pixels and are excluded. WGC was checked against the actual game before native probes. The final 0.1.8 drop-down remained open from 01:31:18.735Z to 01:31:34.396Z, then accepted Worker 02 selection. Actual WPF Stop showed Worker 02 stopped while Worker 01 stayed active, followed by Worker 01 stopped. Closing during an active Start was HOST tested, not timed against a native Start in this session. Settings Start opens the original engine; the developer controller separately sends the fixed-save load command.

`local/m04-acceptance/capture-load-actions-01/capture.mkv` is an actual 83.233-second, 1280×720 H.264 recording, despite its 120-second requested maximum. Initial load is around seconds 10–13 and action inputs/results around 28–31. `capture-restored-01/capture.mkv` is 90 seconds and begins after initial restore/adoption and the first restored action pair. It covers post-launcher-close actions, but neither recording covers the later player closure. Full-size extracted PNGs and contact sheets are under each capture's `review-frames-01`; extraction commands, pinned FFmpeg hash, video properties and limits are in `review-media-and-preserved-logs.json`. Capture cadence is not native frame-time measurement.

Closed player host/bridge logs and matching launcher metadata are preserved under `player-native-01/`. Closed recovered worker host/actor/bridge/status files are preserved under `worker-restored-native-01/`, with source/destination hash equality. Raw game/save payloads, ETLs and videos remain in ignored local storage.

## Commands, tests and preserved failures

`run-command.ps1` refuses existing evidence paths. Each `NAME.command.json` records the exact executable/argument array, start/end UTC, expected exit and observed exit; the corresponding log retains output. Compound native probes record each nested command, response and native evidence separately. WPF/Computer Use actions are observations and original host logs, not invented shell commands.

Key passing records include `preflight-01`, `load-01`, `actions-01`, `save-01`, `seal-01`, `load-restore-01`, `actions-restored-01`, `actions-after-launcher-close-01`, `actions-after-player-close-01`, `analyze-native-01/02`, `player-lifetime-after-02`, `prepare-review-media-01`, `load-restore-concurrent-01`, `load-concurrent-02`, `actions-concurrent-01/02`, and `native-concurrent-save-01`: expected/observed exit 0. Action probes retain expected internal rejection exit 5. Native process exit/trace results remain separate from helper exit codes.

The following failures remain: `personal-after-player-01` expected 0/observed 1 for the ten changed personal files; `player-lifetime-before-01` expected 0/observed 1 for the late snapshot; `player-lifetime-after-01` expected 0/observed 1 for an already-cleanly-stopped worker. The rejected post-player worker generation has an original pre-launch inventory error from the new SDK log. The old-bridge analyzer rejection fixture is retained separately. None was fixed by repeating native gameplay solely to produce cleaner evidence.

The 0.0.14 integration Release build and focused HOST targets passed before native execution: 184 worker, 34 native ABI, 15 actor and 30 then-current launcher assertions, plus 96 Python tests. The runtime-log change passes 18 diagnostic tests and the NativeHost build. `launcher-build-018-01` is a zero-warning/error Release build; `affected-tests-018` passes two CTest targets, 39 launcher assertions and 98 Python tests. These are HOST/FIXTURE evidence, not original-game acceptance. No unnecessary full M03 gameplay replay was performed.

## Final acceptance and next step

`acceptance.json` assembles all nine verified M04 gates from the native runs, closed artifact audits, visual review, exact-lifetime ETW and retained crash/desktop evidence. The root accepted the bounded concurrent isolation report and marked M04 VERIFIED while preserving all 84 original milestone clauses. Native mismatched-sidecar negative execution remains NOT RUN/HOST tested; no extra native replay was performed. The exact new concurrent sealed artifacts were not reloaded, whereas the prior sealed native checkpoint was successfully reloaded and adopted twice. These distinct evidence classes and the unexplained bounded AI stall remain visible.

All relevant native games, worker hosts and launchers are closed; WPR is stopped, as recorded by `final-closed-state.json`. The root records the final complete source/archive provenance after all owned documents are settled in `final-provenance.json`, then independently verifies that freeze and the completed command/log in `final-verification.json`. Those closure records are identified here without embedding their future hashes. No historical source snapshot or native evidence is rewritten by that freeze.

The next milestone is **M05 — separate authoritative simulation from replica presentation**. It is not implemented by M04. Internet multiplayer, arbitrary checkpoint support, complete progression persistence and unlimited worker planning remain outside this acceptance.
